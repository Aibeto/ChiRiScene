# 2026-09-20

## 一轮：修复「启用 DOWN 模式后调度仍在运行」

**根因**：DOWN 的释放清单覆盖不到「写 sysfs 但不属于任何 governor」的那类调用面——
`chiri::scheduler::CpuScheduler::apply_system_tweaks`（cpuidle governor、IO 调度器/预读/合并、
屏蔽 cpu_boost 输入升频，以及本轮 WIP 新加的内核 Sched 段 sched_migration_cost_ns /
sched_nr_migrate）此前完全无停摆感知：启动时先于 DOWN 判定下发，之后每次 meta 热重载由
config_watcher 线程无条件重放。直接后果就是「开了 DOWN，调度仍在被改」——它们不在任何
governor 的快照链里，`release()` 清单碰不到，停摆期残留整段。

**改法**（三层）：
1. `src/chiri/scheduler.rs` 加 `[snapshot]`：静态 OnceLock<Mutex<HashMap<路径, 原值>>>，首次写某
   节点前记录原值（重复下发不覆盖，否则把上一次 tweak 的值当成系统原值）；IO 的 queue/scheduler
   读出来是候选列表、当前值带方括号，写回前必须剥壳。新增 `restore_system_tweaks()`（drain 快照
   逐个写回 + 计数日志，快照空则 no-op）。四个下发点改走 snapshot_write / snapshot_nodes。
2. `src/chiri/mod.rs`：DOWN 进入分支调 restore_system_tweaks；退出分支在重接管后补发一次 tweaks
   （为此 clone 了 tweaks_sys_path）；启动下发与 config_watcher 下发加 `crate::down::is_down()`
   门控（那条线程拿不到循环内的 halted，只能读进程级原子标志）；down_watcher + on_startup 上移到
   首次 tweaks 之前（否则「重启后仍停摆」的那次启动会先写一遍）。
3. 循环内三处隐式门控改显式：fas_mgr.tick()、1s 的 fas 兜底/自愈块、触摸事件消费、fast_lock.tick()
   （改为 halted 时返回 None），以及 panic 自愈后的「按当前模式重接管」块——原只靠 mode 恰为 down
   才安全（get_clg_cfg 对未知模式 enabled=false），模式停在 vector/lab 就会把刚释放的接管重建回来。

**i18n**：新增 system-tweaks-restore（带 count 参数）与 system-tweaks-skipped-down，zh/en 同步。

**验证**：`cargo check -p chiri --target aarch64-linux-android` 通过（Windows 本机不能跑 default
target：netlink-sys / aya-obj 报 libc 缺符号，属既有环境限制，检查必须带 --target）。WebUI 未改动、
未跑 vitest。机上行为未实测。

## 二轮：未提交改动全量审查（含自查）

**自查出的缺陷并已修**：停摆判定与下发跨两个线程（`config_watcher` 与调度循环），存在
「判完 `is_down()` 后被另一线程切进 DOWN」的窗口 → tweak 会写进停摆期。修法：把判定下沉到
唯一入口 `CpuScheduler::apply_system_tweaks`，新增静态互斥闸 `tweaks_gate()`，apply 与
`restore_system_tweaks` 都在闸内复查停摆标志；mod.rs 两处调用点去掉门外重复判定。

**前一轮 WIP（用户改动）审查结论，未动手**：
- `Sched` 段（三份 SoC feature.yaml + config.rs + scheduler.rs）口径齐全、白名单到位、Yumi 不受影响；
  小问题：`SchedTuning` 的 `Serialize` 无消费方、`applied` 计数的是发起写入而非成功写入、
  root `module/config/feature.yaml`（非 SoC 兜底档）没有 Sched 段 → 兜底默认关，与三 SoC 不一致。
- `monitor/config.rs` 规则键剥冒号：`com.x` 与 `com.x:remote` 同时存在时后者静默覆盖前者，建议补冲突 warn。
- `app_detect.rs` 运行期回退只对 `app_modes` 生效，FAS/特调白名单仍用原始包名
  （`com.x:remote` 命中规则却进不了白名单），语义不一致，待用户定口径。
- `module/system.prop` 的 `persist.sys.horae.enable=1→0` 与本次任何改动无关，提交前确认。

## 二轮续：子进程名统一预处理为主包名（用户纠正我上一版）

我上一版做成「两种粒度区分 + 运行期两级回退链」，用户否掉：**`com.xx:push` 本质上是
`com.xx`，预处理归一后放进整个调度计算没有任何问题**，下游应走原本的算法与流程。
终版：① 前台名在唯一入口 `app_detect::set_current_package` 剥掉 `:` 后缀（`app-detect-pkg-normalized`
debug）——此后 `get_current_package()`/事件/devimp/通知/亲和全是主包名，FAS 与特调白名单的
「用原始包名」问题一并消失；② 规则键在 `deserialize_app_modes` 同样归一，冲突时保留后者并
打 `rule-key-conflict`（原实现静默覆盖）；③ `determine_mode` 回到精确查表，删除回退链。
教训：**归一要做在唯一入口、而不是在每个消费点各加一次回退**，后者等于让两套粒度语义并存。
i18n：`app-detect-pkg-normalized` / `rule-key-normalized` / `rule-key-conflict`（zh/en 同步）；
`app-detect-rule-fallback`、`rule-key-subprocess` 已随回退链删除。编译通过。

## 三轮：memory 与 AGENTS 同步

按仓库规范（`凡属架构约定/契约/命令口径的变更，docs/agents/ 与 .codebuddy/memory/ 都要同步`）对齐：
`02-convention.md` 的 DOWN 补漏条目改为「判定收口在 `apply_system_tweaks` 内 + `tweaks_gate()` 互斥闸」
（原写的是「调用点加 is_down 门控」，重构后已不成立），教训正文移到 `04-hard-lessons.md` 并留指针；
`03-chiri.md` 的触摸升频条目与 `Sched` 条目各补一句「停摆期不下发、进入时还原」；
`01-overview.md` 目录结构给 `chiri/` 补 `scheduler.rs`；根 `AGENTS.md` 的 03 行注明 DOWN 的下发/还原出处。
`MEMORY.md` 同步停摆口径 + 记入三项未提交 WIP（Sched 段兜底缺省、白名单不用剥冒号包名、system.prop 变更）。

**文档**：新增 `docs/agents/02-convention.md` 的「DOWN 停摆补漏（2026-09-20）」条目，含教训——
凡是「写 sysfs 但不属于某个 governor」的调用面，release 清单覆盖不到，新增调整必须自己接停摆语义。

## 四轮：安装期电池校准 + DOWN 可观测性

**A. `module/customize.sh` `[oplus-detect]` → `[battery-detect]` 重写**
- 定位 meta 抽成 `locate_meta_file()`（active_config.chr → ro.soc.model → 放弃），sed 提为全局 `SED_CMD`；
  主入口 `apply_battery_defaults()` 先定位 + 取 sed，再二选一：私有节点可用 → `apply_oplus_switches()`，
  否则 → `apply_standard_divisor()`。改的是 `$MODPATH`（modules_update 暂存）里当前机型那份
  meta.yaml：完整安装由安装器落地、热更新由 `cp -r` 覆盖，两条路同源（用户明确要求两条路都要做）。
- 标准节点校准：读 `/sys/class/power_supply/battery/voltage_now`，n 位 → 除数 = 1 后跟 n-1 个 0
  （4382→1000、4382000→1000000），`voltage_divisor` 与 `current_divisor` 同值；首位非 3/4、
  非纯数字或节点不可用就不写（宁留默认让用户在 WebUI 改）。daemon 运行期仍不猜量级，只有安装器
  装机那一刻按节点量级写一次——与「去除内置启发式」的口径不冲突。
- 修双电芯误判：原 `cut -d',' -f 12` 判非空，字段不足 12 个时 cut 把整行透传 → 单电芯机型被判成
  双电芯；改为先数逗号确认 ≥12 字段、第 12 个字段为正数（与 `read_oplus_bcc` 同口径）。
- 热更新末尾「恢复备份」改为只在保留配置那条路执行：选了覆盖却把旧 `config/meta.yaml` /
  `rules.yaml` 盖回去会顶掉刚写入的校准（回退机型生效配置就是根上那份），而 ChiRi 机型的生效配置
  `config/<soc>/meta.yaml` 本就不在这条备份链里，行为本就不一致。
- 双语文案新增 MSG_VOLT_CHECK/APPLY/FAIL，并修正 MSG_OPLUS_NO_SED 的 zh 文案（原写成「检测为非
  oplus 机型」）、两条 NO_META/NO_SED 文案补上「校准倍数」。

**B. DOWN「疑似未生效」核查结论**：用户贴的日志里 `[Tweaks] DOWN 停摆期间跳过一次性系统调整下发`
证明 DOWN 在调度线程启动**之前**就已识别（on_startup + down_watcher 排在首次 tweaks 之前），
「调度先于 DOWN 识别」的假设不成立；日志之后空白是因为停摆中周期块大多被 `!halted` 门控、
事件在 `match` 前 continue，而**开机即停摆时状态机不走「进入停摆」分支**（`halted` 初值 =
`is_down()`），于是全程零停摆字样，与「线程没起来」同形。改法：`src/chiri/mod.rs` 启动即停摆打
`down-boot-halted`（warn），并新增 `DOWN_HEARTBEAT_INTERVAL`(300s) 心跳 `down-heartbeat`（info）。
i18n zh/en 同步两个键。`cargo check -p chiri --target aarch64-linux-android` 通过。机上行为未实测。

**文档**：`03-chiri.md` 加「安装期自动校准」条目（含双电芯判据与备份恢复的修正），
`02-convention.md` 加「DOWN 的可观测性」条目。`MEMORY.md` 二次精简（仍超注入上限，再压叙事）。

## 五轮：DOWN「仍未解决」——启动块残留写入

用 code-explorer 全仓审计「停摆期仍写 /sys·/proc 的调用点」，结论：**循环内已无残留**
（周期块全部 `!halted`、事件在 `match` 前 continue、monitor/rhine 各线程零 sysfs 写），
问题全在**进入循环之前的启动块**：

1. `corectl_mgr.force_online_all()` → 写 `cpuN/online=1`（为 CLG/fast_lock 初始化铺路，
   而停摆时它们根本不初始化）→ 加 `!halted` 门控。
2. `GovernorGuard::cleanup_residue()` → 见到 `performance` 就写回 schedutil，**这正是用户
   日志里那三行 WARN：停摆期 ChiRi 仍在写调度节点**。改：停摆期走新增的
   `report_residue()` 只报不写（`governor-residue-down`，zh/en 同步），理由——「上一轮
   ChiRi 的残留」与「厂商/内核默认就是 performance」进程无从区分，写了就把基线改成
   ChiRi 认为的样子。为此在 `governor.rs` 抽出 `each_residue_policy()` 供两条路径共用。

**已知边界（写进 02-convention）**：所有 release/restore 都依赖**进程内**快照，只重启
daemon（不重启设备）时上一轮写过的 `scaling_max_freq`/governor/cpuset/uclamp/core_ctl/
一次性 tweaks 本进程无从还原——干净基线必须重启设备（或后续做快照持久化）。
另：非 ChiRi SoC 分支（`src/scheduler/`，Yumi）**完全没有 DOWN 支持**（`on_startup`/
`down_watcher` 只在 chiri 分支起），待用户定是否要接。`cargo check` 通过。

## 六轮：用户贴新日志——daemon 层已证明生效，剩下的多半是「手动重启」残留

新日志（16:24）已带 `down-boot-halted`，说明新二进制装上了；且**三行
`检测到残留 performance…已恢复 schedutil` 消失**（上一轮改成停摆期只报不写后，本次
也没有 policy 处于 performance）。逐条证据表明 daemon 层 DOWN 已生效：tweaks 跳过、
无 `scheduler-clg-init`、无亲和/core_ctl 接管、无 governor 写入；app_detect 那条
`模式变更 -> default` 是 monitor 线程自己的判定（照常跑），调度循环在 `match` 前就
continue，之后本就不该再有调度日志。

关键线索：日志首行「上一轮会话存活不足 30 秒」= 用户是**手动重启调度（Action）**而不是
重启设备 → 上一轮 ChiRi 写过的 sysfs（`scaling_max_freq`/governor/cpuset/uclamp/core_ctl/
一次性 tweaks）全部残留在系统里，新进程快照为空、停摆也不还原 → 用户看到的系统仍是
「被 ChiRi 改过的」，这就是「DOWN 看起来不生效」最可能的原因。**口径：要干净基线必须
重启设备。**

改动：心跳 300s → **60s** 并带上已持续分钟数（`halt_since`，启动即停摆时取 now、进入
停摆时置位、退出时清空；`down-heartbeat` 加 `{ $mins }`，zh/en 同步）——停摆期日志完全
静止，用户只能靠这一行区分「停摆中」与「进程已死」，间隔太长等于没有。`cargo check` 通过。
已请用户回贴：`current_mode.chr`、`policy*/scaling_governor|scaling_max_freq|cpuinfo_max_freq`、
`status.csv` 末行、`down.chr` 的实际值，用于定位到底还有谁在改系统。

## 七轮：用户口径澄清——「只要 ChiRi 停止调整，其他调度会立即接管」

→ 之前那条「要干净基线必须重启设备」对用户场景**不必要**（那只影响基线纯净度，不影响
另一个调度接管：它会自己覆盖 ChiRi 的残留值）。据此把停摆期最后几处**启动期**干预全部
显式门控（不再依赖「内存模式恰好是 down」这个脆弱不变量）：
1. `corectl_mgr.force_online_all()`（写 `cpuN/online=1`）→ `!halted`（六轮已做）；
2. `GovernorGuard::cleanup_residue()` → 停摆走 `report_residue()` 只报不写（五轮已做）；
3. **本轮新增**：启动接管 `fast_lock.init()` / `cpu_governor.init_policies()` 包进
   `if !halted`——原来靠 `current_mode=="down"` 时 `!= "vector"` 且 `get_clg_cfg("down")
   .enabled==false` 间接跳过，feature.yaml 里若出现名为 `down` 的模式段就会在停摆期开机
   直接锁频/接管 CLG。
**停摆期 ChiRi 唯一还会动系统的动作**：启动时 `GpuGuard::new()` → `FastWriter::new` →
`utils::enable_perm` 给 GPU 频率节点 chmod 0664（只改权限不改值，且放开权限不阻碍其他
调度；退出停摆后仍需该对象，故未门控——已向用户说明）。`cargo check` 通过。

## 八轮：Rust 性能优化调研 → 评估报告（只评估未改码）

产物 `.codebuddy/docs/2026-09-20-rust-perf-optimization-report.md`。结论要点：

- **先补基线**：仓库无 benches；建议把 `/proc/self/stat` 的 utime+stime 按 1s 采样，
  作为**新列追加到 status.csv 末尾**（合契约），每次优化都用同一份 CSV A/B 比 daemon CPU 与功耗。
- **P0 在 eBPF**：① 五个 percpu map（CORE_LAST/IDLE/BUSY/CUR_TID/CUR_TGID）合一个
  `CoreState` 结构 → 每次 sched_switch 5 次查找降 1 次、用户态每采样 5 次 syscall 降 1 次；
  ② `THREAD_RUN_TIME`(32768) 每次开关无条件累加，但只有 TGID 主路径失败时的降级路径消费
  → 加 `Array<u32>` 开关门控；③ 三个纯遥测 tracepoint（尤其 sched_wakeup，比 sched_switch
  还热）无条件挂载 → 改为按需 attach/detach（需先确认 status.csv 的 wakeups 列消费者）。
- **P1 日志 syscall**：`SelfHealingAppender::append`（logger.rs:113-156）每条日志 6 次
  syscall（create_dir_all + metadata stat + open + write + flush），改持持久化句柄 + 用已有
  `note_write` 字节记账替代 stat，**自愈重建语义必须保留**（当初弃 RollingFileAppender 就为这个）。
- **P2 分配**：`i18n::t()` 每条日志 RwLock+Fluent+String（无参消息可预格式化缓存）；
  devimp 每行 40 列 String+join（可复用缓冲 + itoa）；CLG 每 tick `Arc::new(to_vec())` 属微优化。
- **P3 运行时/构建**：两个 `tokio::runtime::Runtime::new()` 默认多线程（8 核≈16 线程）而各自
  只跑一个定时器循环 → 改 `new_current_thread()`；`itoa`/`bytes`/`futures`/`glob` 在 src 无 `use`
  （可直接删）；`lazy_static` 与 `once_cell` 并存 → 统一 std `LazyLock`/`OnceLock`；
  `opt-level="z"` 建议与 `"s"`/`3` A/B（含体积），`lto=true`→`"thin"` 省 CI 时间。
- **禁区**：`panic="abort"` 不可用——`catch_unwind` 是 panic 自愈地基（chiri/mod.rs:974、
  scheduler/mod.rs:269、monitor/mod.rs:34）。
- 另：`MEMORY.md` 按系统要求做了一次精简重写（合并冗余叙事，口径与同步点全保留）。

## 九轮：补充「零影响」Rust 语言级优化清单（约束：不得影响任何现有功能/业务）

产物 `.codebuddy/docs/2026-09-20-rust-zero-impact-perf.md`。分 A（机械等价）/B（需同步调用点）/C（有语义风险，不做）。
调研中确认已做对的：`soc_hint`/`matched_soc_hint` 用 OnceLock 缓存（common.rs:118,156，注释已写明原因）、
`fas_enabled`/`scenemode_enabled` 是原子量、`FastWriter` 持 fd、`log_enabled!` 门控 format!。

A 档（可逐个独立提交）：
- **为比较而 clone**：`chiri/mod.rs:1477/1516/1614` `mode_clone.lock().unwrap().clone()=="fas"`
  （1s 周期块）→ 改 `.as_str()==`；`mod.rs:1520` `active_pkg().map(str::to_string)` 只为比较 → 删。
- **访问器返回克隆**：`app_detect.rs:75 get_current_package()` / `:361 last_determined_mode()` 每次
  lock+clone → 另加 `with_current_package(|s| ..)` 热路径用（原函数不动，零调用点风险）。
- `logger.rs:858 DevRow([String;40])`，`new()` 里 40 次 `NA.to_string()`+`join` → 改 `Cow<'static,str>`。
- `affinity.rs:550 orig_group: String` 只被赋常值（1080/1472）→ 改 `&'static str`。
- `affinity.rs:994-1002` 每轮多次 `ranges.*.clone().collect()`、`1141/1253` 逐线程 `perf_pool.clone()`
  → 建一次缓存 + 传 `&[usize]`（选核结果不变）。
- `cpu_monitor.rs:256` 每 tick `vec![0.0;n]`、`cpu_load_governor.rs:950` 每 tick `Arc::new(to_vec())`
  → 复用缓冲/双缓冲（`Arc::get_mut`；Worker 不跨 tick 持有）。
- `opt-level="z"` 下内联保守 → 给 `percpu_total`/`max_util`/`find_nearest_freq` 加 `#[inline]`。

C 档禁区（会改语义）：换 HashMap hasher（改迭代顺序 → 影响 `special_tuned_entries` 匹配优先级与
导出行序）；`sort_unstable_by` 仅 `cpu_monitor.rs:473` tgtop 日志排序可用，其余需先确认；
`panic="abort"`（毁 catch_unwind）；换 parking_lot（去毒化语义，代码依赖 `into_inner()` 续服）；
热路径加值缓存（与 40ms/160ms 实时投喂契约冲突，只允许缓存「进程内恒定」的核心区间与缓冲复用）。

工具：建议 `cargo clippy -p chiri --target aarch64-linux-android -- -W clippy::perf -W clippy::nursery`
扫 `redundant_clone`/`needless_collect`/`ptr_arg`（只是报不改）。

## 十轮：审查并综合两份性能报告 → 终版

产物 `.codebuddy/docs/2026-09-20-rust-perf-report-final.md`（**取代**前两份过程稿，过程稿保留备查）。
审查共修正 10 处，其中 3 处结论级：

1. **K3 降级为「需决策」**：`wakeups`/`migrations` 被 WebUI 直接消费
   （`webui/src/data/status-csv.ts:118-119` 解析第 18/19 列）→ 默认摘除 sched_wakeup 探针会改
   界面数据，不属零影响，要动须先与用户确认（如加 meta 开关，关时写 `-`）。
2. **K2 开关条件改正**：原写「FAS 激活且主路径失败时置位」→ 应**与 `FAS_FG_UTIL_ENABLED`
   同条件**（cpu_monitor.rs:81-83）；降级路径只在该开关打开时才可能被调用（:326 门控），
   等失败才开户会多绕一拍，是行为差异。
3. **C1 结论加强**：fps_monitor 的 mio 轮询在**独立 std 线程**（fps_monitor.rs:279），
   其 tokio runtime 上只剩一个 `watch` 桥接任务（:268）→ 几乎空转，可 current_thread 或去 tokio。

其余修正：日志每条是 **5** 次 syscall 不是 6（`flush()` 对 File 无 syscall）；cpu_monitor 每 tick
6 次 bpf 读（3 个计数 map 是 2s 一次，cpu_monitor.rs:425-433）；`DaemonEvent` 约 200B；
K1 补 aya 约束（`PerCpuArray<T>` 需 `T: Pod` → `#[repr(C)]` + bytemuck Pod/Zeroable）；
hasher 风险更正为 `app_modes` 等 HashMap 的迭代顺序影响导出行序（`special_tuned_entries()` 是切片，
不受影响）；A1 频次精确化（1477/1516 同一 if-else 链在 1s 巡检块 mod.rs:1469，1614 在 2s 热保护块，
且 clone 在条件求值里、与是否 fas 模式无关）。

新增风险提示 2 条：i18n 预格式化缓存必须在 `load_language` 切语言时整体重建；
`write_to_file`（utils.rs:21-34）去掉 chmod 回写会改变 sysfs 节点权限 → 对外可见，默认不动（U2 搁置）。

终版 24 项统一编号（K1-K3 / U1-U3 / A1-A7 / B1-B3 / C1-C5）+ 禁区 7 条 + 落地顺序 10 步。

## 十一轮：二轮源码复核修正终版（仍未动源码）

对终版逐条回读源码，又修正 6 处（写入终版的 `[verify]` 节），并新增 `[scale]` 收益量级节：

- **V1 A4 结论改为「纯多余」**：`set_tid_affinity(tid, cpu_ids: &[usize])`（affinity.rs:412）形参本就是切片，
  故 1141/1253 的 `perf_pool.clone()`、1661 的 `perf_pool.to_vec()` 三处不必要，删掉传 `&perf_pool` 即可，
  零签名改动 → 升为全清单最干净的 P1。
- **V2 A3 Cow 方案降级**：`DevRow::set(idx, impl Into<String>)`（logger.rs:879）+ 多处传**非 'static** 的
  `&str`（如 `devimp_event` 的 `set(D_DECISION, kind)`）→ `[Cow<'static,str>;40]` 不是局部改动。
  改为两步：先只复用 join 的目标 String 缓冲（零类型改动），Cow 留待后续并须处理调用点生命周期。
- **V3 A5 收益上调**：真正大头是 1402-1412 `bg: Vec<(i32,String)>` 每 2 轮对每个后台 tid
  `group.to_string()`（可达数百条），改 `Vec<(i32,&'static str)>` 后与 1472 的 clone 一起消失。
- **V4 B1 结论加强**：`ConfigReload` 两个消费方都忽略 payload（scheduler/mod.rs:483-485 `let _ = new_rules;`、
  chiri/mod.rs:2465 `_new_rules`），发送方 app_detect.rs:416 → Box 化零风险，枚举尺寸改由 ModeChange 决定。
- **V5 U1 补风险**：`note_write` 累计的是本会话写入量，外部删/截断日志后不归零 → 不能纯用它替代
  `metadata`，正确做法是「记账为主 + 低频校验」。
- **V6 A1 锁范围已验证**：临时 MutexGuard 在 if 条件求值结束时 drop，改 `.as_str()` 只把持锁延到比较结束；
  三个条件里其它调用（`fas_enabled()`/`fas_mgr.is_active()`）不再取该锁，无重入。

## 十二轮：回答「这些调整会不会影响 devimp 和 log」

核查后把影响面写进终版。**唯一会改 devimp/日志数据面的是 K3**：摘探针会让 status.csv 第 18/19 列
（logger.rs:1282-1284）、devimp snap 行的 wakeups/migrations/freq_trans 三列
（logger.rs:1379-1381，DEVIMP_HEADER 含这三列）变成 `-`，991 摘要 telemetry-summary 变成 0，
WebUI `status-csv.ts:118-119` 读到 null。schema 与行数不变，只是取值口径变 → 维持「待用户决策」。

其余：K1/K2 改记账方式不改 util 数值；U1 只动 daemon.log（DevimpWriter/StatusWriter 本就
持持久句柄、互不影响）；U3/A1/A2/A4/A5/A6/B1/B3 完全不动输出。
**实现时最易改坏输出的三项**：A3（devimp 行构造，需字节级 diff）、B2（sample_one_tid 字段口径
改错会动 place/aff 行的 comm/util 列）、C4（自写 logger 必须复刻行格式与轮转）。间接影响：C1 若采样
周期漂移会改 devimp tick 行数，需真机验证；A7 i18n 缓存若漏在 `load_language` 失效，日志文本会停在
旧语言（devimp 不受影响——它是 CSV 数值，列名是常量不走 i18n）。
验收方法：开 dev_record 同场景录两份样本，diff devimp CSV（忽略 ts 列）与 daemon.log（忽略时间戳）；
U1 另需手删 daemon.log / logs 目录与写满触发轮转三次验证。

## 二十三轮：devimp 新增 CPU/GPU 实际频率与调速器（40 列 → 48 列）

**新增 8 列，追加在末尾**（不动既有索引；与 status.csv 加 fps 列同一手法）：
`cpu_cur_khz` / `cpu_max_khz` / `cpu_min_khz` / `cpu_governor` /
`gpu_cur_khz` / `gpu_max_khz` / `gpu_min_khz` / `gpu_governor`（索引 40-47）。

**语义区分（关键）**：既有 `cur_freq_khz`/`max_freq_khz` 是**调度器写入的决策值**（TunedGovernor 的
scaling_max / 硬件最高）；新列是**内核当前实际值**（scaling_cur/max/min_freq + scaling_governor）。
离线分析时两者对照才能看出「写了多少 vs 实际跑多少」。

**采集**：`crate::chiri::cpu_freq_snapshot()`（mod.rs，按 get_cpu_policies 遍历 policy）与
`crate::chiri::gpu::devfreq_snapshot()`（gpu.rs，Adreno kgsl-3d0 + devfreq gpu/kgsl/mali 节点，
当前频率兼容 `cur_freq` / `gpuclk` / `clock`，上下限兼容 `max_freq` / `max_gpuclk` / `min_freq`）。
**只在 `devimp_active()` 时调用**（每秒一次 sysfs 读，常态零开销），且**不做缓存**——这些值会被
内核 governor 与 TunedGovernor 随时改写。仅 snap 行填充，其余行类型留 `-`。

**格式**：多 policy / 多节点以 `;` 分隔，每项 `policy<id>:<值>` 或 `<设备名>:<值>`，读不到写 `-`。

**改动 4 文件**：logger.rs（HEADER + DevRow 40→48 + D_* 40-47 + devimp_snap 8 参数）、
chiri/mod.rs（cpu_freq_snapshot + 调用点传参）、chiri/gpu.rs（devfreq_snapshot）、
mdocs/TechAnalyze.md §9.4（列数与字段名同步 + 语义说明）。
**校验**：cargo check 通过；脚本核对 HEADER 48 列 / DevRow 48 / D_* 索引 0-47 无缺失无重复。

**analyze web（`analyze/index.html`）同步**（用户提醒补做）：该页**按列名动态解析**，新列不登记也能显示
（未收录列名走名字哈希取色兜底），但为观感一致已补三处：`COLOR_MAP`（8 色）、`SHORT_NAMES`
（cpu_cur/cpu_max/cpu_min/cpu_gov + gpu_*）、`HEADER_TIPS`（说明含「与 cur_freq_khz 决策值列的区别」
与 `;` 分隔格式）。node 校验内联脚本语法通过、8 列各 3 处登记。
**WebUI（`webui/`）不需要改**：它只列 devimp 目录的文件名、不解析行内容（`contract/sources.ts` 只有
`listDevimp`），改的是 devimp 而非 status.csv。

## 二十二轮：诊断 `handle_cpufreq_transition` 挂载失败（8550）

**根因**：该探针挂 tracepoint `cpufreq/cpufreq_transition`，aya attach 需打开
`/sys/kernel/tracing/events/cpufreq/cpufreq_transition/id` —— 8550 厂商内核**没编译 cpufreq tracepoint**
（`CONFIG_CPU_FREQ_TRACEPOINTS` 未开）→ 文件不存在 → attach 失败，日志里报的就是这个路径。

**影响面（已用数据核实）**：三个遥测探针是逐个独立 try（cpu_monitor.rs:111-138），失败只 warn 一条，
**不影响主探针 sched_switch 与任何调度决策**。唯一后果是 `freq_trans` 列无数据：
status.csv 第 21 列 229 行**全 0**、devimp snap 行 571 行**全 0**，而同行的 wakeups/migrations 正常
（227/229、571/571 非零）。**坑：写的是 `0` 而不是 `-`，看起来像「频率从未切换」，实际是「没有探针」**——
任何用该列判断调频活跃度/热限频切换的读数都会被误导（2026-09-20 视频功耗分析里这列不可用）。

**可选修法**：① 先核实是否只是路径差异（`/sys/kernel/debug/tracing/events/cpufreq/`）；
② 确无则把该探针从 tracepoint 换成 **kprobe**（`__cpufreq_driver_target` / `cpufreq_freq_transition_begin`），
不依赖 tracepoint 编译开关，代价是函数名随内核版本变需探测；③ 或接受现状，但建议
「探针未挂载时该列写 `-`」以免误读（与 K3「关探针写 `-`」口径统一）。**均未实施，待用户定。**

## 二十一轮：实施 V1/V2/V3/V5（视频功耗调参 + special 可调参数，用户确认「1235 都做、别一刀切、headroom 直接 0.95」）

**新增三个 special 可调参数（全部可选，缺省即回退，配置在 tuned_profiles.yaml）**：
- `perf_ceil`（0.1~1.0，默认 1.0）：CLG 早有、tuned 只有 floor 缺 ceilings —— 这是「单独压某簇天花板」缺的那块。
- `per_cluster`（键 little/big/prime；字段 headroom/perf_floor/perf_ceil/hysteresis/down_hold_ms/
  util_smoothing 全 Option）：解决三簇一刀切。
- `migration_cost_ns`（Option<u64>）：接管期写 `/proc/sys/kernel/sched_migration_cost_ns`，release 按快照恢复、
  未配置则完全不碰该节点（feature.yaml 的一次性 200000 不受影响）。

**关键坑（本次唯一的行为陷阱）**：`SpecialTunedConfig::normalize` 原先把 headroom clamp 在 **(1.0, 2.0)**，
即**下限 1.0** → 任何 <1 的收紧值（如 0.95）都会被静默抬回 1.0，参数在省电方向等于失效。
已放宽为 `(0.5, 2.0)` 并写明「<1 收紧 / =1 原样 / >1 放大留余量」；`for_cluster` 内部同口径。

**落地参数**（playback）：headroom **0.95**、util_smoothing **0.25**、perf_ceil 1.0、
`per_cluster: little{0.90, ceil 0.65} / prime{0.80, ceil 0.50}`（big 不覆盖 → 跟随 0.95）、
migration_cost_ns 400000。按日志 util 估算：little cap 72%→57%、big 57%→48%、prime 29%→17%，
prime 尖峰 0.61 被 ceil 0.50 截断。

**改动 4 文件**：`src/chiri/config.rs`（字段 + ClusterTunedOverride + EffectiveTuned + for_cluster）、
`src/chiri/tuned.rs`（按组取参数 + target_ratio clamp(floor,ceil) + apply/restore_migration_cost）、
`module/config/normal/tuned_profiles.yaml`、`tuned_profiles-example.yaml`（字段说明同步）。
不写新字段时行为**逐位等同改动前**（默认 1.0/空表/None）。`cargo check`（android target）通过、无 warning。
报告已追加 `[done]` 节（含预期效果表与回退路径：先回 1.0，再整体回 1.05/0.35）。

**待真机 A/B**：batt_p avg/p50（目标降 ≥0.2W）、是否还落进 70% 软限频、capfreq 三簇是否按预期下移、
fps/卡顿（一票否决）、migrations 是否下降。

## 二十轮：8550 视频场景功耗分析（devimpbin/8550/logd_0920-210959.tar.gz）

产物 `.codebuddy/docs/2026-09-20-8550-video-power-analysis.md`（只分析未改码）。分析方法：devimp CSV
离线回放（2326 snap / 63379 tick / 2200 core / 375 tgtop），按模式与分钟聚合 + Pearson 相关性。

**视频画像（playback=bili，2232s）**：batt_p avg **2.83W**（p50 2.59 / p95 5.61 / max 11.22），
batt_temp avg 39.7（峰值 41.4 → 触发 `batt_soft_temp_c 41` 的 soft cap 70%），cpu_temp avg 53.4（max 89.6），
gpu_busy avg 8.6，migrations ≈557/采样、wakeups ≈971/s。
参照：同日志 default（桌面/切换）avg 4.63W、gpu 22.1、cpuT 68.7 → **视频不是最耗电场景，但是长稳态**。

**关键读数**：tick 的 `cur_freq_khz` 是 TunedGovernor 写的 **scaling_max 上限**（非实际频率）；
util 列是决策用的 EMA 值（`util_smoothing`）。
- little(0-2, 2016MHz)：cap 70.6/72.4/**100**，util 0.63；**各核 util 58.9/53.7/54.0，p95 98-100**
- big(3-6, 2803MHz)：cap 56.6/56.7/85.8，util 0.50；各核 util 30-39
- prime(7, 3187MHz)：cap 35.8/**29.2/83.8**，util 0.27/0.21；**core7 util p50=0（一半空闲）**
- tgtop：bili 42.3% / **dolbycodec2 29.4%** / surfaceflinger 25.1% / system_server 13.3% / media.hwcodec 10.8%

**相关性（batt_p vs，按分钟 n=36）**：capfreq_little **+0.884** > gpu_busy +0.851 > capfreq_big +0.798
> cpu_temp +0.704 > capfreq_prime +0.362（共线强，只用于排优先级，不当因果）。

**五个问题**：① 小核负载倒挂（util 54-59% 反高于 big 30-39%，却给到 72% 上限、p95 100%）—— 头号驱动；
② prime 空转（p50=0）却维持 29% 上限、尖峰推到 p95 84%；③ 电池 41.3℃ 触发 70% 被动限频；
④ GPU 强相关但 ChiRi 无省电手段（`gpu.rs` 只能锁最高频，方向相反）；
⑤ 迁移 557/s，而 `feature.yaml` 把 `sched_migration_cost_ns` 降到 200000（更爱迁移）——对稳态负载是反的。

**现状配置已做对的**（别重复改）：`module/config/normal/tuned_profiles.yaml:29-35` playback 已配
headroom 1.05 / floor 0 / hysteresis 0.04 / down_hold 100 / **util_smoothing 0.35** / **boost_affinity false**。
playback 激活时 **CLG 停用、TunedGovernor 接管**（daemon.log 佐证），故调频走 tuned 而非 CLG 参数。

**方案**：V1 headroom 1.05→0.95（纯配置先试 1.0；预估 little cap 72→60%）；V2 util_smoothing 0.35→0.25
（压 prime 尖峰）；V3 加 per_cluster 覆盖 + perf_ceil（tuned.rs 目前无 perf_ceil，CLG 有）；
V4 GPU 按场景设上限（高收益高风险，须测掉帧）；V5 playback 期间调高 migration_cost 到 400-500k 并恢复；
V6 不建议动 `batt_soft_temp_c`（安全阈值）与 dolbycodec2/surfaceflinger（app/系统行为）。

## 十九轮：审查上一次提交（738c52f「完成多轮 Rust 性能优化落地」）

逐文件回读该提交的全部 15 个源码/构建文件 diff，**功能语义等价性成立**；发现并修掉 3 处一致性问题：

1. **E2 的脏标记会被 `||` 短路漏消费**（affinity.rs）：`if t % ONLINE_EVERY_ROUNDS == 0 || len 不符
   || DIRTY.swap(false)` —— 前两个条件成立时 swap 不执行，标记残留到下一轮多刷一次。改为**条件外先
   swap 再判断**（`let hotplug_dirty = ...swap(false, Relaxed);`）。
2. **`src/main.rs:40` 注释过时**：仍写「日志全走 log4rs/devimp 落盘」→ 改「daemon.log/devimp」。
3. **app_detect.rs 注释结构**：`// 提取核心检测逻辑`（普通注释）后紧跟 `///` doc，区块标记混排 → 合并为一条 doc。

审查确认无问题的关键点：
- **U1**：`append_verify`(8KB) / `append_open`(含 metadata 校正) / 轮转前先丢句柄 / `note_write` 锁外调用与
  字节口径（恒为编码长度）均与设计一致；未到阈值的删除窗口 ≤8KB 是已知有意权衡。
- **C4**：`format_line` 逐字段复刻（`libc::localtime_r` 本地时间、`Level::as_str()`、剥 `chiri::` 前缀）、
  `enabled()` 走 `log::max_level()`、`update_level` 用 `set_max_level`；`log4rs`/`parking_lot` 已从
  Cargo.lock 消失。lock 残留核实：`once_cell`/`bytes` 是 **aya 0.14 的传递依赖**，`chrono`/`serde_json`/`itoa`
  已不在 chiri 依赖树（`cargo tree -i` 报 no match，属其它 workspace 成员）。
- **K1/K2**：两侧 `CoreState` 布局一致（`#[repr(C)]`、32B、无 padding、字段同序）；探针读改写序与原实现一致；
  `thread_acct_enabled` 走 Array 查找；`update_percpu` 删除后无残留引用。
- B2 字段口径、B3 泛型、A4/A5/A6、A1/A2(`Arc<str>`)、B1(`Box`)、E1（明确不做内容缓存）逐条核对一致。
- 全仓 grep 无 `log4rs`/`OnceCell`/`BufferWriter`/`LineEncoder`/`LOG_HANDLE` 代码残留（仅注释提到原实现）。

修正后 `cargo check`（android target）通过、无 warning。修正足迹 3 个文件（affinity.rs / main.rs / app_detect.rs）未提交。
**仍未做**：C1（tokio current_thread）、真机 A/B 与 U1/C4/E2 的真机专项验证。

## 十八轮：完成 U1 / B2 / C4 / E1 / E2（logger 架构级改动 + 两处范围收敛）

- **U1**：`SelfHealingAppender` 改 `state: Mutex<AppendState> { file, size, since_verify }` + `append_verify`
  （8KB 巡检）+ `append_open`（`create_dir_all` 只在这里 + `metadata` 校正尺寸）；稳态每条日志 5 次 syscall
  → 1 次 write。轮转前先丢句柄；`note_write` 仍在锁外、字节口径恒为编码长度。**有意权衡**：外部删除的
  检测窗口 ≤8KB（最坏丢 8KB 日志）。
- **C4**：**移除 log4rs**，自写 `log::Log`（`ChiriLogger` + `log::set_boxed_logger` / `set_max_level`）；
  行格式改成纯函数 `format_line`（`libc::localtime_r` + `strftime` 本地时间、`Level::as_str()`、模块剥
  `chiri::` 前缀）。Cargo.toml 删 log4rs、`log` 开 `std` feature（`set_boxed_logger` 必需——第一次编译
  报 E0425 已修）。
- **B2**：`sample_one_tid` 单次遍历取 utime/stime + 计数字段数，去掉 `Vec<&str>` collect。
- **E1 缩范围**：原「cgroup tasks 内容未变则复用上轮判定」**实施前被否**——稳态本就只有 1 次 cgroup +
  1 次 cmdline 读（倒序命中即返回）；且冷启动期 pid 先入组、exec 后 cmdline 才可读，按内容缓存会漏检
  这类前台切换。改为去掉 `collect` + 反向迭代 `split_whitespace()`（`DoubleEndedIterator`）+ 复用
  cmdline 路径缓冲。
- **E2 缩范围**：只做 `cpu` hotplug（uevent 置 `monitor::CPU_HOTPLUG_DIRTY` → affinity 在线核刷新条件加
  `dirty.swap(false)`，**周期兜底保留**，机型不广播 uevent 时行为不变）；**`power_supply` 不做**——要配
  缓存才有开销收益，会让 status.csv 的 charge 列滞后，属数据面变化而收益仅微秒级。
- 验证：`cargo check`（android target）通过、无 warning；LSP 0 报错。真机未实测（U1 三条自愈场景、
  C4 行格式字节级一致性、E2 的 cpu uevent 是否广播，均需真机或 CI 产物）。
- 报告同步：总表 5 行状态、[impl] 追加 5 行、[equiv] 追加 5 行、[pending] 标注 U1 已实施、[evt] 更新 E1/E2。

## 十七轮：完成 B3（write_nodes 泛型化，非改窄签名）

关键实现决策：**没有**把 `write_nodes` 改成 `&[(&str, &str)]`（原报告方案）。理由——路径运行时拼接的
调用点（`/sys/block/*/queue/*`、cpuset/cgroup 组、`cpuN/online`）必须持有 String，强改会让调用方
先建一层 `Vec<(&str,&str)>` 中转，**反而多一次分配**。改为 `AsRef<str>` 泛型后两类调用点都零成本：
- 拼接类（`chiri/scheduler.rs` IO、`scheduler/scheduler.rs`、core_ctl、affinity×3）保持
  `Vec<(String,String)>`，**零 diff**；
- 字面量类改 `(&str,&str)` 数组，彻底去掉 `to_string()` 与 Vec 分配：
  `chiri/scheduler.rs` 的 touch_boost 四项 + `scheduler/fas/policy_mgmt.rs` 的 perfmgr 两项（按 Yumi
  零权重口径改的，只做类型适配未动逻辑）。
配套：`CpuScheduler::snapshot_nodes` 同口径泛型化（`P: AsRef<str>, V`），否则 touch_boost 的字面量
数组传不进去。`write_nodes` 返回值仍是 `Vec<String>`，日志口径（`{path}`）不变 → 行为等价。
验证：`cargo check`（android target）通过、无 warning；LSP 0 报错。
报告同步：总表 B3 → 已完成 · 等价；[impl] B3 行写清「为什么是泛型而不是改窄签名」；[equiv] 加 B3 等价性结论。

## 十六轮：Yumi 权重归零 → 更新报告所有相关说明

用户指出 B3 仍标「降级」不合理，并重申 yumi 进入 0 权重（即使波及也不管）。据此更新报告四处：
1. 顶部新增「权重口径」声明（Yumi / `src/scheduler/` 不再是否决理由）；
2. 总表 B3 完成列 → 「未做 · 待实施（Yumi 权重归零，不再因波及否决）」；
3. [rust] 节 B3 条目补：调用点分两类（运行时拼接路径仍需先持有 String，只能省中间层克隆；字面量路径可彻底去掉
   `to_string()`）+ 不再缩范围，`src/scheduler/` 两处一并做类型适配；
4. [impl] 节 B3 行由「降级不做」改「恢复待实施」，理由改为「收益仍小但不再为零」。
其余项不受影响：K3 的阻塞原因是 WebUI 数据面（非 Yumi），E4 是时延取舍，都不因本口径改变。
未改任何源码。**遗留冲突**：AGENTS.md / docs/agents/01-overview.md 仍写「`src/scheduler/` 勿改动其逻辑」，
与新口径冲突，待用户确认是否同步。

## 十五轮：Android 事件触发器调研 + 报告落档（纯文档）

产物：报告新增 `[evt]`（调研）与 `[pending]`（U1/C1 定稿方案 + U3 量化结论）两节；总表增补 E1-E4，
并把 U1/C1 备注改为「未做 · 方案已定稿（见 [pending]）」、U3 补「cpu_monitor 侧评估后不做」。

调研结论（长期有效）：
- **监听层主体已事件化**：screen = netlink uevent、config/down/rhine = inotify×3、fps = RingBuf+mio。
  剩余纯轮询只有 `pid_watcher`(500ms) 与 `app_detection_loop`(亮 1.5s / 息 1s)；
  `cpu_monitor`(160/40ms) 与 `telemetry`(1s) 受「eBPF 采样本质 / status.csv 1s 契约」约束，不该事件化。
- **E1（推荐先做，零新依赖）**：app_detect 每轮先读 cgroup `tasks` 原文与上轮字符串比对，未变则复用
  上轮判定、跳过全部 `/proc/<pid>/cmdline` 读；稳态 N 次读降为 1 次，行为等价。
- **E2**：uevent 扩展 `cpu`（hotplug，收益偏响应性）与 `power_supply`（充电状态，微秒级/秒）。
  需真机确认这些子系统确实广播 uevent（现有经验：多数驱动不发，见 screen_detect.rs:476-481）。
- **E3（研究项）**：`/dev/socket/logdr` 订阅 events buffer 的 `am_focused_activity`，延迟约 10ms，
  但需实现 liblog reader 协议（150-250 行）+ 真机验证 SELinux 可达性 + tag 名的版本差异。
- **明确否决（留痕以免重评）**：cgroup v2 `cgroup.events` 的 poll 只看 `populated` 0↔1，感知不了
  同组内 A→B 前台切换（且目标机型仍是 cgroup v1）；PSI `poll()` 与 1s 连续采样契约冲突；
  thermal uevent 广播依赖驱动、不可靠；timerfd + epoll 全量合并改造面大、收益主要是线程数。
- **E4 待拍板**：`pid_watcher` 并入 app_detect 可省一个 500ms 线程，代价是前台 PID 广播时延
  从 ≤500ms 变 ≤1.5s（影响 FAS 反偷跑门控与 CPU 采样切 PID 的时延）。

报告状态如实化：U1/C1 代码**尚未落地**，总表「完成」列标为「未做 · 方案已定稿」。
本轮未改任何源码。

## 十四轮：等价性复核 + 报告总表更新（纯文档，未动源码）

对已落地的 10 项逐条回读改后源码复核：**全部等价，未发现需要改的逻辑**。结论写入
`2026-09-20-rust-perf-report-final.md` 新增的 `[equiv]` 节，并给总表补齐「完成」列
（10 项「已完成 · 等价」、B3「降级」、A5b「跳过」、其余「未做」）。

复核得出两条口径更正（已同步 [kernel] / [review] #8 / [plan] 第 4 步三处旧表述）：
1. **K1 两侧天然同批**：eBPF 产物由 build.rs 编译后经 `include_bytes!` 嵌入 daemon 二进制，
   不存在 daemon/探针版本错配 → 原「跨二进制契约需同步」作废。
2. **Pod 实装**：用户态用 aya 0.14 自带 `unsafe trait Pod`（aya-0.14.0/src/bpf.rs:45），
   内核侧 aya-ebpf 0.2.1 的 `PerCpuArray<T>` 无 trait 约束 → **不需要 bytemuck**，未新增依赖。
3. 如实记录的唯一边界差异：`CORE_STATE.get_ptr_mut` 失败时探针提前 `return Ok(0)`（原实现会继续
   尝试写 3 个 percpu；per-cpu array 场景不可达）。
4. K2 复核要点：开关与 `FAS_FG_UTIL_ENABLED` 同源同生命周期 → 「降级路径可能被调用」⟺「记账已开」；
   写开关在 attach 之后、采样循环之前，而第一 tick 本就只建基线，无可见差异。

验证：表格 6 列对齐、旧表述无残留（仅 [equiv] 保留更正说明）；本轮**未改任何源码**
（`.codebuddy/docs/` 本身 gitignore，`git status` 里仍只有上一轮的 11 个源码文件 + memory）。
另注：`.codebuddy/plans/*.md` 是计划工具自动生成的未跟踪文件，非本轮产物。

## 十三轮：实施性能优化 11 项（K1/K2/U3/A1/A2/A4/A5/A6/B1/C2 已做，B3 降级、A5b 跳过）

逐批 `cargo +nightly check -p chiri --target aarch64-linux-android` 通过，最终无 error / 无 warning
（原 `unused dependency futures/glob` 警告随 C2 消失）；`yumi-ebpf` 用
`cd yumi-ebpf && cargo +nightly check --target bpfel-unknown-none -Z build-std=core` 单独验证通过。
**注意：本机 build.rs 无 bpf-linker → 走 write_ebpf_stub 占位**（日志同时打印「跳过 eBPF 编译」与
「✅ yumi-ebpf 编译成功」两条，前者才是真相），eBPF 真编译与 verifier 检查只能靠 CI。

落地要点（与报告方案的偏差已写进报告新增的 `[impl]` 节）：
- **K1**：五个 percpu map 合并为 `#[repr(C)] CoreState`（32B；u64×3 + u32×2，无 padding）；
  旧的 5 个 map 名全仓已清零。**无需新增 bytemuck**：用户态 aya 0.14 自带
  `unsafe trait Pod`（aya-0.14.0/src/bpf.rs:45），内核侧 aya-ebpf 0.2.1 的 `PerCpuArray<T>` 无 Pod 约束。
- **K2**：`Array<u32> THREAD_ACCT`，探针 busy 分支记账前读一次；用户态在 `FAS_FG_UTIL_ENABLED`
  置位处写 1；旧产物缺该 map 只告警（探针保持恒记账 = 原行为）。
- **A2 改实现方式**：报告原提「闭包入口」，实际用 **`Arc<str>` 快照**（`CURRENT_PACKAGE` 改存
  `Arc<str>` + 新增 `current_package_arc()`）——闭包会把锁持有期拉到覆盖整段 1s 巡检逻辑（内含
  多处 IO/日志）；mod.rs:1520 的 `active_pkg().map(str::to_string)` **保留**（去掉会与后续
  `fas_mgr.activate(&mut)` 借用冲突，这正是原代码先 to_string 的原因）。
- **C2 修正**：logger 的 OnceCell 必须用 **`std::sync::OnceLock`**（std 没有 `sync::OnceCell`，
  一次改错已修）；tokio 保留 `rt-multi-thread`（`Runtime::new()` 是多线程调度器）。
- **B3 降级不做**：所有调用点路径都是运行时拼接 String，唯一字面量点每次只省 4 次分配且仅冷路径；
  改签名波及 8 文件含约定不可改的 src/scheduler/。**A5b 跳过**（3 只小 Vec/2s）。

报告：`2026-09-20-rust-perf-report-final.md` 追加 `[impl] 实施记录`（含逐项偏差表、验证、文件清单）。
改动 11 个文件：Cargo.{toml,lock}、src/{common,i18n,logger}.rs、src/monitor/{cpu_monitor,app_detect}.rs、
src/chiri/{mod,affinity,cpu_load_governor}.rs、yumi-ebpf/src/main.rs。**未做**：真机 A/B、K3、WebUI（未触碰）。

`[scale]` 按频率分 L0(每次上下文切换)/L1(每日志行)/L2(每 tick)/L3(每 1-2s)/L4(按线程数批量)：
**只有 K 组与 U1 值得单独安排 A/B 测量**；A1/A2 这类每秒一次小分配 <0.01% CPU，属代码卫生不单独立项。
落地顺序据此重排：A4→A5 → U1 → K1+U3 → K2 → C1→C2 → A3→A7 → 顺手项 → B 组 → C 组 → K3 待决策。
