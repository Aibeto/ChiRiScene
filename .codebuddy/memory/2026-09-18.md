## 日志页改造：快照加功耗/充电列 + 双视图跟随底部

用户要求（在 LogsView 的 151 行 WIP 基础上增量，未动其余）：① 快照加功耗与充电状态列；② daemon.log 自动滚底 + 回到底部按钮，滑动后停止跟随；③ 快照窗改子滚动，按钮从回顶改回底。

**逻辑审查结论**：status.csv 22 列契约里本就有 charge（索引4）与 batt_power_w（索引17），data/status-csv.ts 也已解析（charge/battPower）——纯展示层改动，daemon 未动。

**实现**（LogsView.svelte）：两个子滚动窗（terminal__body 与 snapshot-wrap 加 max-height:60vh + overflow:auto + overscroll contain）共用跟随逻辑——\ 在 DOM 更新后滚底、依赖**整个 visible 数组**（尾部窗口行数恒定也必须滚）；程序性滚底触发的 scroll 用 24px 阈值识别不误退跟随；滑离底退跟随（滚回底不自动恢复）、点按钮才恢复；切源重置 follow；StateBox 时不渲染按钮。快照表改**时间升序**（原 newestFirst）——与 daemon 方向一致，「回到底部」才有意义；新增列：功耗（fmt(battPower,2,'W')）、充电状态（charge 四态映射 logs.charge.*，未知 —）。i18n 两语同步：action.toTop→action.toBottom、logs.status.power/charge、logs.charge.*（4 键）、unit.watt。

验证：svelte-check 0/0、vitest 76 全过；残留扫描（scrollTop/to-top/action.toTop/回到顶部）0 命中（LogsView 内 scrollTop 均为新逻辑的 DOM API）。遗留：state.svelte.ts 的 statusRowsNewestFirst getter 无引用（WIP 文件保守未删）。

## PowerAVG 功耗参考/平均（状态页右栏 + 高级设置开关）

用户需求：状态页第一卡片右侧（与左侧并排）显示耗电参考/平均功耗；daemon 每次读到功耗就递推（参考值 = 相邻两值取半）并写 PowerAVG.chr；高级设置开关切换参考值/平均值（默认参考值），平均 = (上次留存均值×次数+当前值)/(次数+1)；注意 WebUI 两态切换显示与文件缺失/空的处理；计算加在 csv 写入流程里、不并行。

**daemon**：meta 新增第九字段 power_avg（全链路：MetaYamlFile/ExternalMetaOverrides/parse_disk_meta/chiri::config::Meta/Config::load 两处合并/4 个 meta.yaml 模板）；logger.rs 加 POWER_AVG 进程级静态 + power_avg_update(Option<f32>, bool)（参考/平均两口径共用 (值,次数)，次数两模式都累计；缺测跳过；tmp+rename 原子写 PowerAVG.chr）；chiri/mod.rs 在 status_log_snapshot 之后顺序调用（不并行）。

**WebUI**：paths 注册 powerAvg；新文件 data/power-avg.ts（parsePowerAvgWatt，缺失/空/非法→null）+ contract/power.ts（readPowerAvg，absent/空归一为无值不报错）；state 加 powerAvgWatt/powerAvgUsesAverage(读 metaSnapshot)/setPowerAvg（直写 meta 不走草稿）+ loadOverview 并行读取；OverviewView 第一卡改两栏（左 daemon 信息 + 右耗电块，isChiri 门控，口径标签随 meta 每秒刷新）；ConfigView 高级区加开关（disabled 于 metaValid/pending）；meta.ts 字段表+校验；i18n 两语（config.powerAvg/hint、overview.power.ref/avg）。

**关键审查点**：旧 meta.yaml（8 字段）→ daemon sync 覆盖为 9 字段（既有机制）；覆盖前 metaValid=false → Switch 置灰、writeMetaFields 内部也拒写（'未找到字段'不会触发）；loadOverview 每秒含 loadCommon → meta 快照天然新鲜；contract.test.ts 合法样本补 power_avg（3 红→绿）。验证：cargo check 通过、svelte-check 0/0、vitest 76 全过。AGENTS.md 五处同步（九字段/同步点清单/落盘清单/实验室段）。

## 全量未提交代码审查（46 文件 + 4 新文件，~1700 行）

范围：本会话改动（特调/DOWN/LogsView/PowerAVG）+ 既有未提交工作（打包链 pack.sh/logger 归档/导出 UI、yumi→chiri 改名链、mdocs、构建配置）。

**修了 6 处**：① **[P1 真 bug] pack.sh**：export 开始清理漏 \.mode 与 .tar.part——上次 xz 中断的残留 MODE 会让本轮「无 xz 设备」的导出被判压缩中（s:tar 不打印）直到轮询超时；补清理 + 两处 mv 失败路径补删 .part；② 命名：contract 的 startDevimpExport→startExport（导出的是 logd 归档，state 同步）；③ 注释：export.ts 失败码注释对齐 pack.sh 契约（3=logd 不可进/4=打包压缩失败/5=无归档）、logger.rs「返回 zip 名」→tar 名；④ 健壮：进度解析正则容忍 wc 前导空格；⑤ mock 样本 ziped_*.zip→*.tar。

**验证过没问题**：改名链代码级 yumi 残留 0（仅 deviceKind 枚举与 README/ebpf 刻意保留）；pack.sh 错误码契约/LF 行尾/Git-bash 语法 0；logger 归档链（staging 前缀兼容、orphan 回收、短会话丢弃、watchdog.pid 保留）设计完整；mod.rs 的 lab 修复（contingency fast_lock 硬锁、thread_bind 门控 lab_static、三 governor 释放时序）语义正确；i18n 测试更新（注释剔除/desc 按需/family 枚举）合理；meta 九字段/PowerAVG/LogsView/DOWN 修复均一致。

验证：cargo check 通过、svelte-check 0/0、vitest 76 全过、pack.sh bash -n 通过。

## 导出压缩格式 xz → gzip（用户：xz 太慢）

同步面 7 处：① pack.sh——GZ_BIN 探测（gzip→busybox gzip）、echo gz、清理目标 .tar.gz、gzip -6 -f 就地压缩（默认删源、-f 防覆盖交互）、注释全改；② contract/export.ts——target 改 .gz、探测标记 s:gz、ExportPhase done-xz→done-gz、头注释（含「2026-09-18 由 xz 改回 gzip」历史说明）；③ state.svelte.ts——done-gz 判定（3 处）+ 注释；④ i18n zh/en——「已导出 tar.gz」「设备不支持 gzip，已保留未压缩 tar」；⑤ mock——轮询正则顺带修正（devimp_→logd_、.tar.xz→.tar.gz）；⑥ AGENTS.md 导出段整段重写（旧「xz 三级回退/排除本次运行文件/devimp 源」描述已过时，一并清掉；补 .mode 清理注意与失败码契约）；⑦ MEMORY 打包条目。

行为不变部分：无 gzip 时保留未压缩 .tar（phase done-tar）、失败码契约 3/4/5、.mode 标记机制、原子 .part+mv。

验证：pack.sh bash -n 0、svelte-check 0/0、vitest 76 全过、xz 残留扫描仅剩 export.ts 头注释的历史说明（有意保留）。

## Stardust 独立成模式家族 + en.ts 按 zh 对齐

**① stardust 家族**（用户要求：独立、不并入 CLG、调度侧不动）：mode.ts 的 ModeKind 由 'down' 改 'stardust'、describeMode 的 down 分支 kind 同改、家族注释更新（2026-09-18 独立成族）；两语 locale 键 mode.family.down→mode.family.stardust（'Stardust' 同值）；i18n.test family 枚举同步；AGENTS 三处（DOWN 段 kind 引用 / 家族段 / 家族重申段）。OverviewView 的 t(\mode.family.\\) 自动生效。

**② en.ts 按 zh 最新值对齐**（zh 是用户 WIP 不动）：config.powerAvg 'Power averaging'→'Show average power' + hint 简化（跟 zh「显示平均功耗而非参考功耗」）；apps.tag.special 'Tuned'→'Special scenarios'（特别场景）；logs.level.all 'All'→'ALL'；logs.status.charge 'Charge'→'Power state'（跟「电源」且不与 Power 列混）；overview.export.title '(Beta)'→'(Experimental)'、desc 改「Pack the logd history archives to…」；state.refreshFailed 'retrying'→'will retry automatically'。

验证：svelte-check 0/0、vitest 76 全过、旧键残留 0（family.down/kind:'down'/Tuned/Power averaging）。

## scenemode 去向确认（用户问询）+ AGENTS 过时描述修正

**结论：功能活着，只是没有 UI 模式身份**。① 调度层：mod.rs 息屏轴完整活跃——息屏事件记 screen_off_at（doze 天花板 0.30）、SystemLoadUpdate 分支 60s 预判 + delay（默认 300s）后热切 CLG 到 scenemode 配置（ceil 封顶/不主动升频），亮屏复位恢复；FAS 任意实例/特调优先跳过；饱和退出 + 300s 冷却；scenemode_enabled=false 热重载退出已激活的。② UI 层：scenemode 不是 current_mode 值（独立息屏轴）→ 模式卡片/家族标签永不为它点亮（stardust 家族标签只在 down 时出现）；UI 入口只有配置页「息屏场景模式」开关 + 实验室接管项（LAB_TAKEOVER）。③ AGENTS 修正两处：L262 的「[已暂停] 息屏切换/亮屏恢复均已停用」与 L310 的「切换路径已暂停」——与本文件 L338（stardust 段自己写着「2026-09 曾短暂暂停后恢复」）及代码矛盾，已改为现状描述。

**修正（用户纠正：DOWN 不是 Stardust）**：家族定稿为四族——CLG / stardust（= scenemode 的家族位：独立息屏轴不产生模式值，但按用户要求「不管实际看不看得到都要注册」——kind 'stardust' + mode.family.stardust + mode.scenemode/desc + describeMode 识别 'scenemode' 字面值）/ **down（独立家族，恢复 kind 'down' 与 mode.family.down）** / rhine。mode.ts（ModeKind 并存 down+stardust、down 分支恢复、新增 scenemode 分支、注释）、两语 locale（family.down 恢复 + mode.scenemode 新增）、i18n.test（枚举 7 项）、AGENTS 三处、MEMORY 更正。首版把 down 并入 stardust 属误读，已纠正。

**Stardust 家族成员确认**：仅 \scenemode\ 一个（独立息屏轴）。daemon 侧注释口径已统一——config.rs 的「stardust 家族（scenemode/down）」修成「（scenemode；down 是独立家族）」（mod.rs 两处注释本就是（scenemode）口径，无需改）。webui/mode.ts 定稿：kind 'stardust' 仅 scenemode 命中；down 走 'down'。

## mode.*.desc 全部注释（用户要求）

两语 locale：vector/contingency/babel/down/scenemode/fas/special/unknown 的 desc 全部行注释（zh/en 对称；CLG 三档此前已注释）；mode.unknown.missing 保留（missing 分支仍用）。mode.ts：descKey 一律置空串（LAB_CATALOG 去 descKey 字段 + 注释留痕；describeMode 五分支与 lab 分支 descKey: ''；头注释说明）——UI 对空描述跳过渲染（OverviewView 既有条件渲染），不会回退显示裸键名。i18n.test：删 desc 存在性断言（注释同步）。顺手把 en 的 family.special/lab 对齐 zh 最新值（'Special'/'RHINE'）。验证：svelte-check 0/0、vitest 76 全过；未注释 desc 残留 0（mode.ts 内 descKey 引用仅存注释行）。

## ak-ui 规范审查 + 内置字体

**审查基准**：ak-ui skill（design-language/tokens/quality-checklist）。**结论**：整体合规性高（app.css 品牌映射/全局 :focus-visible/44px 控件/ prefers-reduced-motion/ak-adapt 深色适配/按钮全 token 化/原生 <dialog> 焦点陷阱 ✓；强度 system + 日志区局部 terminal ✓）。

**内置字体（用户授权）**：设备普遍缺 Noto Serif SC → 内置子集（zh 文案 487 字，156KB woff2，OFL + LICENSE 同目录）；app.css 加 [fonts] @font-face（'ChiRi Serif'）+ :root 映射 --ak-font-serif/ command（command 回归衬线语义）；Panel 标题应用 --ak-font-command。工具链：jsdelivr 下 SubsetOTF → npm i --no-save subset-font + 一次性 mjs 脚本（完事删）。

**修掉的 token 漂移（6 处）**：ConfirmSheet backdrop rgba→color-mix(canvas 72%)；ToggleField #000/#fff→--ak-color-black/white；OverviewView 进度条 0.4s ease→--ak-motion-slow/--ak-ease-standard；LogsView 按钮阴影→--ak-shadow-panel、终端深底→组件级 --terminal-surface 变量。

**报告未动**：窄屏横滑（用户 WIP 有意）、StateBox 脉冲时长/小间距字面量（无对应 token，轻微）。

验证：svelte-check 0/0、vitest 76、vite build ✓（字体以 hash 资产打进产物：noto-serif-sc-bold-subset-D6bogFnb.woff2）。**局限**：无浏览器实测——窄屏布局/键盘焦点/对话框开合/加载态需人工过一遍。

## 后台降权（UI/视频优先，压低后台运算）

**需求**：UI 始终视觉流畅，压低后台运算/内容计算进程的亲和与核心占用权重（不禁止用核），看着流畅 + 极大压功耗。

**实现**（daemon 侧调度特性）：① config.rs —— AffinityConfig 加 background_uclamp_max_pct（默认 50，0=关；normalize clamp）；② affinity.rs —— BG_DEMOTE_GROUPS=[background, restricted]（刻意不含 system-background：媒体/音频/画中画可感知）、apply_bg_uclamp_max（写 cpu.uclamp.max=50.00，幂等）、restore_bg_uclamp_max（写回内核默认 max）；apply 汇合点 boost/normal 两态都调、release 还原；③ 三份 SoC feature.yaml 显式 50 + 注释（root feature 无 Affinity 段，走 serde 默认）；④ AGENTS 三条同步。

**设计要点**：uclamp.max 是软权重（需求钳制）——后台优先小核、EAS/schedutil 随之回落，空闲时仍可上大核；UI 侧沿用现状（top-app/foreground 收窄+钉核、default 轻量前台保护）、视频走 playback 特调/前台进程；后台 promote 保留（能效兜底，占核权重已被压低）。

验证：cargo check 通过（无新警告）。**待实机**：功耗收益与后台完成度（后台变慢是预期，需确认无功能受损）。

## SystemUI/桌面全核保护 + 最近 10 任务代码审查

**任务 1**：affinity_blacklist.yaml 追加 com.android.systemui + 桌面三项（默认 com.android.launcher / com.android.launcher3 + 小米 com.miui.home，不按机型判断直接添加，含注释区「UI 视觉进程」）。黑名单原生语义 = 全部线程保持全核、不做任何迁移与钉核——即「始终占用所有核心」。cargo check 通过（yaml 重编）。

**任务 2（审查，排除 i18n）**：抽查本会话最后 10 个任务的核心文件——affinity.rs（后台降权 +36：组常量/两函数/apply 汇合点/release 还原，结构正确）、app.css（[fonts] + token 映射，build 已验证字体进产物）、mode.ts（家族/scenemode 分支/descKey 全正确）、pack.sh+export.ts（gzip 链，前轮已全量审）、logger.rs+mod.rs+config.rs+common.rs+down.rs+app_detect.rs（PowerAVG/DOWN，前轮已审）、state/LogsView/OverviewView/ConfigView/meta.ts（多轮验证）。

**两个低优先注记（未改）**：① apply_bg_uclamp_max 每 2s 无条件写（try_write_file 无同值跳过）——cgroup 写开销小，若在意可加值缓存；② @font-face 只声明 font-weight 700，Panel title 是 600——CSS 字体匹配会选 700 字形，观感正常，如需严格可改声明为 600 700。

验证全绿：cargo check / svelte-check 0-0 / vitest 76。

## ak-ui 原语复用扩展（用户要求尽可能多用官方类）

ak-ui 共 223 个类。对照后本轮替换 2 处：① OverviewView 导出进度条 → ak-progress__track/fill（官方深色原语，自带斜纹刻度；删掉自绘 export__fill 样式、清掉 Unused CSS warning）；② StateBox → ak-notice 底座（kind→ak-notice--danger/--warning，结构补 __body/__title/__message，信号条交给 ::before，loading 脉冲移到 ::before；app.css ak-adapt 段加深色适配——官方是浅色纸卡）。

**剩余候选（未动，观感变化明显，待用户选择）**：Segmented→ak-segmented（官方 0.625rem 等宽终端风，中文字号会变小）；ConfirmSheet→ak-dialog；LiveStatus→ak-status；Panel 壳→ak-card；ToggleField→ak-choice--switch。

验证：svelte-check 0/0、vitest 76、vite build 通过。

## WebUI 样式复用重构（用户授权：允许调整现有样式以更多复用 CSS）

**背景**：用户先问「为什么 CSS 写在 .svelte 里不直接导入」——答复要点：全局层（ak-ui style.css + app.css）本来就是导入式；组件内 `<style>` 是 Svelte 编译期作用域样式（自动加 hash 类、天然隔离、删组件不留孤儿），两层按作用域分工，正是 Svelte 惯例。随后用户授权「允许调整 UI 现有样式以更多的复用 CSS 样式」。

**做法**：app.css 建立公共层——[utilities] 扩展（u-stack / u-field / u-actions / u-row-tight / u-note / u-mt-2 / u-mt-3 / u-danger / u-scroll / u-list-reset）+ [surfaces]（u-surface：边框+面板底+右上切角，Panel 与 ConfirmSheet 弹层共用）+ [signal-map]（[data-signal] → --signal 单点映射，取代 Panel 与总览模式卡片各自 5 组的重复映射）+ [lists]（u-notice：暖色短竖条清单，总览导出须知与实验室须知共用）。逐文件删掉重复块并复用：Panel CSS 88→55 行、Overview 164→96、Lab 139→80、Config 67→23、Apps 60→37、ConfirmSheet 77→67（对比 HEAD，含用户 WIP 基线）。

**两条级联规则（重要，已写 MEMORY）**：① Svelte 作用域类编译后带 hash 类（如 `.mode__lock.svelte-xxx`），比全局单类高一级特异性 → 局部类可安全覆盖全局工具类，不依赖 CSS 顺序；② 纯工具类之间同特异性，app.css 内 `.u-note` 必须排在 `.u-mt-*` 之前（margin:0 是简写，靠后写的 margin-top 决胜）——`vite build` 产物已核对顺序。

**归一化（1px 级，用户已授权）**：说明小字统一 0.75rem/1.5（原 0.6875/0.72/0.8125 混用；commit__state 有意保留正文色与 u-muted 的「未改动」态拉开对比）；u-notice li 统一 0.75rem（实验室须知原 0.8125）；删 .shell__exit/.lang/.bar__back 冗余 min-height: 2.75rem（= --ak-density-control-height，已核实 ak-ui token 值）。

**验证**：svelte-check 0/0、vitest 76 全过、vite build 成功（EXIT=0）；产物核对：u-note < u-mt-3 < u-danger 顺序正确、[data-signal]{--signal 存在、.mode__lock 带 hash 类、.u-surface 存在、无 .stack/.export__bar/.rules__note/.commit__actions 残留。

**遗留（如实报告）**：state.svelte.ts 的 statusRowsNewestFirst getter 仍无引用（WIP 文件未删，同前）；剩余小合并候选（.power__label 与 .mode__id 的 overline 风格、.apps__pkg 与 .files__item 的极小字）未动，收益小。

## 直接复用 ak-ui 官方原语（用户：尽可能直接使用 ak-ui/style.css）

**做法**：先加载 ak-ui 技能（design-language / tokens / quality-checklist / headless-components 四份参考），盘点 223 个官方类，逐组件对照后把外壳全部换成官方类、自研皮收敛成 [ak-adapt] 适配。

**组件映射**：Panel→`ak-card` 全套（官方排版：1.25rem/900 衬线标题 + 0.875rem/68% 描述；只留信号条与头部骨架）；ConfirmSheet→`ak-dialog` 全套（原生 dialog + 官方 backdrop/入场动画；danger 用官方变量 `--ak-dialog-signal` 内联注入；sheet__* 全删）；LiveStatus→官方 `ak-status`（本地样式删光）；Segmented→`ak-segmented`（radiogroup 的 aria-checked 由适配层映射官方 aria-pressed 选中态）；ToggleField→官方 `ak-choice--switch`（组件内 :global 覆盖全删，适配转全局）；StateBox→官方 notice 排版（本地只留信号分支/脉冲/断行）；功耗读数→`ak-gauge`；导出进度→`ak-progress` 组合（进度走官方 --ak-progress-value）；字段组→`ak-form-stack`+`ak-field`（自研 u-field/u-surface 已从 app.css 删除）。

**有意的语义决策**：ak-gauge 去掉 conic 进度环（无可换算百分比，空环=虚假量程，设计语言禁止装饰性遥测）；ak-segmented 项高提至 44px、字号 0.75rem（官方 2.25rem/0.625rem 不满足触控与中文可读——质量清单明确反对「游戏式密度」的小字）；`.btn` 系保留自研 adapter（官方按钮 150×50 固定 + 重阴影 + 硬编码浅色 modifier，控制台行内布局必须适配）。

**验证**：svelte-check 0/0、vitest 76、vite build 通过；产物核对 [data-ak-ui] 适配规则齐全、无 u-surface/u-field/sheet__/power__ 残留；启动 dev server(5199) 并 preview 交给用户。**未完成**：浏览器视觉走查（agent-browser 未安装，npx+Edge 的截图方案被用户跳过）——按技能要求如实标注，四屏观感需人工过一眼。

## 用户 7 项（UI 修复 / 模式审计 / 满量程 / 无衬线 / 左对齐 / 内嵌 webui / nofix）

**修**：① 顶栏状态芯片——官方 `.ak-status` 的 `min-height` 是 **content-box**（实测 81×63 vs 按钮 43×44）→ 适配改 `box-sizing: border-box` + `padding: 0 var(--ak-space-3)` + `justify-content: center`，标签换 `--ak-font-sans` 0.75rem/600；② 快照表内距写在 `tr` 上被忽略（列糊死，实测每格 0px）→ 移到 `th/td`（12px）+ sticky 表头落 `th`，全部 `text-align: left`；③ 模式卡片 **UI 无 default 假定**（`describeMode('')`→unknown；用户看到的 default 是 `current_mode.chr` 的陈旧内容——运行期状态文件停机不清理，daemon 只在调度线程启动时重写自愈）→ Overview 补 `modeMissing` 分支（「尚未产生模式记录」、不渲染家族行，与实验室页同口径）；7 家族键两语齐全；④ `--ak-font-command` 改 `var(--ak-font-sans)`（衬线退居 `--ak-font-serif`，暂无引用）；⑤ 家族/子模式显示核对 OK。

**新**：⑥ meta 加两个可选字段：`power_max_w`（默认 12，写入侧校验 1..=200，越界 daemon 回退默认不判文件非法）+ `nofix`（默认不进配置）；WebUI `OPTIONAL_FIELDS`/未知键放行/数字写值/缺行顶层追加（`appendTopLevelField`）；高级设置加数字输入（ak-field + ak-input）；耗电 gauge 进度环 = 功耗/满量程（内联 `--ak-gauge-value`，撤掉「去环」适配）；i18n `config.powerMax*` 三键两语；⑦ **内嵌 webui**：根 `build.rs` 生成 dist 全量清单（`include_bytes!`，无新依赖，dist 缺失→`EMBEDDED=false` 降级）+ `src/webui_asset.rs::restore_webroot`（逐文件比对、只覆盖缺失/不一致）+ main 在 logger 初始化后调用；**nofix** 由 `common::read_nofix_flag` 在 `sync_meta_snapshot` 之前判定（必须前置，否则开关自身先被覆盖），true 时跳过 webui 还原与 meta 自愈（rhine 用户主动写不受影响）；FTL 新键 `main-nofix-skip`/`main-webui-restored`/`main-webui-not-embedded`。

验证：cargo check ✓（0 新警告）、svelte-check 0/0、vitest 76、vite build ✓；浏览器实测（npx agent-browser + Edge）：芯片 81×44（=EN 高）、快照 th/td 12px/left/sticky、config 数字输入 value=12、build.rs 产物 `EMBEDDED=true` 5 条目（index.html + js/css/字体）。

## 逻辑审查二轮（用户：审查代码逻辑是否正确）+ nofix 门控补全

**发现并修复 1 个真缺口**：nofix 只门控了 main 启动的 `sync_meta_snapshot`，**热重载路径漏了**——`chiri/mod.rs:615` 与 `scheduler/mod.rs:173` 的 config_watcher 在每次 meta 变更时仍会自愈覆盖；`sync_rules_snapshot`（main 启动的 rules 展示副本还原）同样属覆盖类操作未门控。修复：`common.rs` 加进程级 `NOFIX: AtomicBool`（`set_nofix`/`nofix_active`，与 FAS_ENABLED 同范式——高频路径只读原子量不读磁盘），main 在 `read_nofix_flag` 后置位，三处 sync 全部改读标志；webui 还原保持 main 本地判断（等价）。

**其余逻辑逐项复核为正确**：build.rs 合并版（断言 + eBPF + webui 清单三功能共存，EMBEDDED=true）；`read_nofix_flag` 前置时序；镜像 `LAST_DETERMINED_MODE` 三同步点（337 静态/450 亮屏清/550 判定写）；`power_avg_update` 与用户口径逐字一致（首读直存、参考=(旧+新)/2、平均=(旧×次数+新)/(次数+1)、缺测跳过、tmp+rename 原子写）；webui `powerMaxWatt` getter 恒 >0（无除零）、`--ak-gauge-value` 夹 0~100；`appendTopLevelField`/`replaceTopLevelField` 数字渲染与缺行追加；nofix 下 WebUI 开关写入仍能热重载（门控只挡自愈写、不挡重载本身）。**边界声明（写入 AGENTS）**：运行期状态文件（down.chr/rhine.chr/current_mode.chr/PowerAVG.chr/active_config.chr 的自愈与输出）与 rhine 用户主动写不受 nofix 影响。

## 三轮追加（用户复查）：未运行态 + 耗电进度条

**「调度未启动仍显示有模式/运行中/锁被持有」的根因**：probeLiveness 本身正确（flock 内核随进程死亡释放，HELD=确有进程持锁）；用户看到的是**预览默认 `?daemon=running` 模拟态**。修了两处真实问题：① 模式卡片在 `daemonState=stopped` 时不再把 current_mode.chr 的陈旧值当当前模式——显示「调度未运行」（新键 `mode.unknown.stopped`）、家族/id 隐藏、删 `mode__stale` 提示块与 `overview.mode.stale` 键；② mock `?daemon=stopped` 不再播种 watchdog.pid（与存活态自洽）。**耗电块**：ak-gauge → 官方 `ak-progress__track/fill`（量程=power_max_w，内联 `--ak-progress-value`），右栏紧凑（u-note 标签 + 1.125rem 数值 + 12px 条），撤 [ak-adapt] gauge 适配。

复测：stopped 态 chip=已停止/模式=调度未运行/家族 null/id —/无 gauge；running 态 chip=运行中/default/12px bar（fill 0，mock 无 PowerAVG 数据）。svelte-check 0/0、vitest 76。

## 四轮修复（用户 8 项）

1. **power_avg 转可选**：daemon `MetaYamlFile` 加 `#[serde(default)]`（9 必填 → 8 必填 + 3 可选 power_avg/nofix/power_max_w）、WebUI 从 META_FIELDS 移入 OPTIONAL_FIELDS——老 9 字段文件不再判非法/反复警告/被覆盖；**规则确立并写入 AGENTS：后加字段一律可选（serde default），避免一次扩充就「用户设置全丢」**。功耗值本身来自 PowerAVG.chr，daemon 跑起来才写入（占位 — 是正常的）。
2. **daemon.log 撑出屏幕**：`.u-stack` 加 `grid-template-columns: minmax(0, 1fr)`——网格项默认 min-width:auto，nowrap 日志把整列/整页撑开；实测 pageScrollW 375 ≤ innerW 390。
3. **列表无滚动条**：`.u-scroll` 显式细滚动条（`scrollbar-width: thin` + `scrollbar-color` + webkit 伪元素双写，兼容新旧内核），实测 thin。
4. **回到底部按钮点击不消失**：只在 `!followDaemon/!followStatus` 时渲染（回底恢复跟随即消失），实测 true→点击→false 且容器已到底。
5. **按钮在子块外**：新增 `.pane { position: relative }` 包裹终端/快照，按钮改 `position: absolute` 锚在所属滚动子块右下角（原 `position: fixed` 悬浮在页面固定位置）；实测 parent 为 pane。
6. **左上角 ✕ 变后退**：`ksu.ts` 声明并封装管理器 `exit()`（原生关闭 WebUI），App 优先 `exitApp()`，仅缺失该 API 时才 window.close()+history.back（原实现只有 fallback → 表现成返回上一页）。
7. **导出轮询假失败**：`wc -c < 缺失文件` 是**输入重定向错误**——由 shell 直接打到 stderr（命令里的 `2>/dev/null` 覆盖不到）、真机 mksh 判定整条命令失败 → 轮询报「查询导出进度失败」；改为全部 `[ -f ]` 守卫再读 + `b/d/t` 初值 0，并放宽判定（errno≠0 但输出含 `b:` 时照常解析）。
8. **i18n 句尾句号**：zh 11 处 / en 10 处删除（句中句号保留；lab.lock.hint 这类多句只删末位）。

验证：cargo check ✓、svelte-check 0/0、vitest 76、浏览器四项实测（宽度/滚动条样式/按钮出现-消失-位置）。

## 五轮修复（meta 误判非法 / 功耗无提示 / 导出命名）

**① meta 契约放松为「字段全可选」（根因链）**：老文件缺字段 → daemon 判非法 → 启动自愈整体覆盖 + 追加警告（设置丢失）→ WebUI 也因「缺少字段」拒绝写入 → 表现「切平均功耗不生效 + 重启后开关回关 + yaml 出现已恢复默认」。修复：`MetaYamlFile` 全 `Option<T>`、`ExternalMetaOverrides` 全 Option（`derive(Default)`，无手写 impl）、`parse_disk_meta` 改「出现即校验」（loglevel/language 白名单、name/author 非空、power_max_w 越界回退 12；未知键仍拒）；`chiri/config.rs` 新增 `apply_meta_overrides`（None = 不改动）替换两处逐字段赋值，`scheduler/config.rs` 同口径；rhine 三处消费改取「生效值」（`meta.x.or(内嵌默认).unwrap_or(true)`）。WebUI：META_FIELDS 变「已知字段全集」（11 项全可选）、删 OPTIONAL_FIELDS、「缺少字段」判定删除、写侧缺行一律追加（不再区分必填/可选）；contract.test.ts 断言改为「缺字段合法、未知字段非法」。

**② 功耗缺失提示**：`readPowerAvg` 增加 `missing`（缺失/空/非法一律 true），state 加 `powerAvgMissing`，Overview 第一卡下方红字 `overview.power.missing`（实测 rgb(229,72,77)）。

**③ 导出命名/计数**：`pack.sh export` 的 dest_base **不带扩展名**（脚本生成 `<base>.tar` → `<base>.tar.gz`）；export.ts 传了带 `.tar` 的基准 → 产物变 `logd_X.tar.tar.gz`、轮询找 `logd_X.tar.gz` 永不命中（卡 99% + 已压缩 0.0 MB）——改传无扩展名；`TOTAL` 改「文件数 + 1」（tar -v 含根目录条目 `./`，原 11/10）。

验证：cargo check ✓、svelte-check 0/0、vitest 76（改 1 条断言）、vite build ✓、浏览器实测红字与告警。

## 六轮修复：uclamp ENOENT 刷屏

**现象**：daemon.log 持续 `chiri::utils Failed to write to /dev/cpuctl/restricted/cpu.uclamp.max: No such file or directory (os error 2).`，用户称老版本没有。

**根因**：后台降权 `apply_bg_uclamp_max`（默认 `background_uclamp_max_pct = 50`，HEAD 与当前一致）对 `BG_DEMOTE_GROUPS = ["background", "restricted"]` 每 2s apply 各写一次；机型的 `/dev/cpuctl/restricted` 由 Android init 按需创建、该机没有 → 每轮 ENOENT；`utils::try_write_file` 对任何错误一律 `log::warn!("Failed to write to ...")` → 刷屏。**核对 HEAD：`apply_bg_uclamp_max` 与默认值都未变**，所以「以前没有」更可能是当时日志等级被 warn 过滤（meta 曾被自愈覆盖）或那段时间没跑 affinity，而非代码新增。

**修复**：① `src/chiri/affinity.rs` 新增 `write_bg_uclamp_max(group, val)`——组目录不存在直接跳过（`apply_bg_uclamp_max`/`restore_bg_uclamp_max` 都改走它；**不缓存探针**：restricted 组可能在运行期出现，现查现写才对；已有 `sys.cpuctl_background_exist` 之类启动期探针不适用于此）；② `src/utils.rs::try_write_file` 把 **ENOENT 降级为 debug**（节点缺失 = 内核/机型没有该调优节点，不是写入失败），其它错误仍 warn——一类刷屏的通用兜底。

验证：cargo check ✓（无新警告）。需重装 daemon 生效。

### 十一轮（同题返工）：撤回存在性预判 + 多节点 DEBUG/WARN 口径

**用户追问**：① 这个报错是否意味着调度跑不起来？② 不要消除报错与方法（撤回我加的存在性守卫），③ 把它改成 DEBUG 级，④ 所有「多节点、各机型可用性不同」的地方统一：逐节点失败 DEBUG、全部备用失效才 WARN。

**语义确认**：不影响调度运行。`apply_bg_uclamp_max` 只把 background/restricted 两组的 `cpu.uclamp.max` 钳到 50%，写入结果不参与任何控制流（返回值被忽略、不 propagate）；该机 `background` 组存在、降权照常生效，只是 `restricted`（Android 由 init 按需创建、该机没有）这一路无效 → 功能局部降级，不是故障。

**改动**：`utils.rs` 新增 `write_nodes(items, what) -> Vec<String>`——逐节点失败 debug、全部失败 warn（`NODE_FAIL_WARNED: Mutex<Vec<&'static str>>` 去重，写成功即复位）、非 ENOENT/ENOTDIR 就地 warn，**不做 exists 预判**。改造点：affinity（bg-uclamp-max、cpuset-cpus、cpuset-restore、lab 组回写、boost 前台组、release 快照、exclude/restore）、chiri+scheduler 两侧 io-tuning、touch-boost-disable 4 候选、perfmgr-disable 2 候选、corectl-reassert-offline；`write_cpuset_cpus` 单组入口删除（无调用方）、`affinity-write-failed` i18n 随之停用。telemetry 读侧：电池候选全失效 → 新 ftl 键 `telemetry-battery-unavailable`（一次 warn、恢复复位），GPU 候选全缺 → `telemetry-gpu-unavailable`（一次 warn）。ftl 同步 zh/en。

验证：cargo check ✓、clippy 仅剩既有风格类告警（`is_multiple_of` 等 37 条，非本次代码）。**未改造（留作后续）**：`chiri/fast.rs` tick 重写、`chiri/gpu.rs` lock/release、`screen_detect`/`touch_detect` 只读枚举——它们各自已有 warn 或属只读探测，不在本轮「写入节点」口径内。

## 十二轮：日志页级别条与行距

**用户要求**：① 级别选项条缺 TRACE 档（「其实就是 ALL 改名」）；② 选项条允许左右滑动应对窄屏；③ daemon.log 窗口里「级别 ↔ 模块」之间过宽。

**改动**：① `LogsView` 的 `levelItems` 首项标签由 `t('logs.level.all')`（'ALL'）改为字面量 `'TRACE'`（id 本来就是 TRACE，只是文案没对上），`logs.level.all` 键从 zh/en 删除。② `Segmented.svelte` 新增 `scroll?: boolean` → 容器渲染 `data-ak-scroll`（**项目侧钩子，不是上游修饰类**）；`app.css [ak-adapt]` 新增变体：容器 `overflow-x: auto; overscroll-behavior-x: contain`，项 `flex: 0 0 auto; min-width: auto`（覆盖基础适配的 `flex:1; min-width:0`）——五档在窄屏按内容宽度排列、整条滑动。③ `.log__level` 列宽 `3.4rem` → `2.3rem`（5×11px 等宽 0.6em + 0.06em 字距 ≈ 2.27rem）：日志每行是独立 grid，只有固定列宽才能让模块列对齐，3.4rem 的富余全部堆在级别与模块之间（即用户说的「中间过宽」）。

**验证**：svelte-check 0/0、vitest 84、vite build ✓；产物核对：`\.ak-segmented\[data-ak-scroll\]` 容器与项规则在、`.log__level` 2.3rem 在、3.4rem 与 `logs.level.all` 零残留、JS 内含 `data-ak-scroll` 与 TRACE 档。

## 十三轮：自审（token-efficient-coding）

按技能口径重查本轮全部改动（utils::write_nodes 与 6 处调用点、affinity 各组循环、两侧 io-tuning、touch-boost/perfmgr 候选、corectl reassert、telemetry 两条 warn、心跳线程、放电口径、前端级别条/日志列宽/颜色/去重）。**发现并修 2 处**：① `write_cpuset_cpus_items` 原先委托 `write_cpuset_paths_items` → 两个语义不同的操作（组应用 vs 快照回写）共用告警键 `cpuset-restore`，「一边成功清掉另一边失效记录」会让 2s 热路径反复报同一条 warn ⇒ 改为独立键 `cpuset-cpus`；② `chiri/mod.rs` 放电门控的局部变量 `batt_power_w` 与同名方法易混 ⇒ 更名 `discharge_power_w`。其余项（空列表不打 warn、ENOTDIR 判定、Mutex 仅局部使用、心跳线程无 panic 路径、`data-ak-scroll` 特异性覆盖、放电口径不污染 status.csv）复核为正确。验证：cargo check ✓。

## 十四轮：en.ts 对齐 zh.ts（humanizer 文件模式）

逐键对照 219 条，修正 16 处英文：**旧版遗留描述**——`overview.stop.confirm.message`（还是「先停看门狗、再停进程」）、`overview.stop.confirm.effects`（多了「被信号杀死/建议重启」等 zh 没有的信息）、`lab.current.hint`（还是旧的功能说明，zh 已换成潘多拉魔盒的俏皮话）；**语义漂移**——`overview.power.ref/avg` 缺「放电」限定（zh 参考/平均**放电**功耗）、`overview.stop.confirm.recover` 漏「在模块启用的情况下」、`overview.export.notice.session` 漏「历次重启」、`apps.tag.fas` 漏「数量」；**名实不符**——`lab.title/entry` 应为 `RHINE`（zh 就是 RHINE）、`overview.device` 设备匹配、`overview.module(.author)` 模块信息/维护、`logs.status.pkg` 前台包名、`apps.search` 应用名、`logs.empty` 暂无内容、`action.copied` 复制到剪贴板、`mode.family.unknown` 大写、`config.scenemodeEnabled.hint` 去句尾句号、`overview.export.title` 小写 (experimental)、`lab.lock.hint` 尾句改为「无视风险强制关闭」。风格：无破折号（仅注释里的占位符 `—`）、无句尾句号、去被动与 AI 腔。

验证：键集合 219=219 完全一致、svelte-check 0/0、vitest 84、vite build ✓。**注意**：i18n 测试只校验键存在，译文语义无守卫 → 已在 MEMORY 记「zh.ts 为唯一语义源，改文案必须两边同步」。

## 十五轮：PowerAVG.chr 启动清空

**问题**：daemon 重启后 `PowerAVG.chr` 仍留着上次运行的值（文件是输出记录、不读回），WebUI 会把它当有效读数展示——旧值配新会话的仪表盘。

**改动**：`logger.rs` 新增 `power_avg_reset()`（`common::write_file_no_panic(&path, b"")`，**置空而非删除**：保留文件、WebUI 归一为「无值 + missing」）；`main.rs` 在 logger init 之后、panic 钩子之前调用（`[power_avg]` 块）。属运行时输出，**不受 nofix 约束**（nofix 只管二进制内容对外部文件的覆盖）。递推状态本就是进程级静态（重启从零），这次只补上磁盘侧的残值。

**顺带**：`overview.power.missing` 文案（zh/en）补齐放电口径——空文件现在也可能是「一直插电没采样」，原文案只说「未运行过时不会产生」。

验证：cargo check ✓、svelte-check 0/0、vitest 84、vite build ✓；AGENTS.md 对外暴露条目补「启动清空」。

## 十六轮：参考功耗公式改 10:1 加权

**用户要求**：修改参考功耗计算公式，内存中保存的上一次平均功耗需要先乘 10 再参与计算。

**改动**：`logger.rs` 参考分支 `(value + p) / 2.0` → `(value * REFERENCE_WEIGHT + p) / (REFERENCE_WEIGHT + 1.0)`，新增 `const REFERENCE_WEIGHT: f32 = 10.0`（与平均模式同形，权重 10:1：单次异常采样只占 1/11）。首个样本（count==0）仍直接取 p；平均模式不变。**同步了 7 处公式文档**：common.rs::MetaYamlFile.power_avg、chiri/config.rs::Meta.power_avg、logger.rs 两处注释、四份 meta.yaml 模板注释、AGENTS.md 功耗口径条目、webui `data/power-avg.ts` 与 `state.svelte.ts` 注释。

验证：cargo check ✓、svelte-check 0/0、vite build ✓；无 Rust/前端测试断言旧公式（改前已核对）。

## 十七轮：平均模式排除息屏样本

**用户要求**：息屏功耗不加入平均放电功耗，但参考值里要保留。

**改动**：`chiri/mod.rs` 采样门槛由「仅放电」变为 `charge_state == "discharging" && (use_average || is_screen_on)`（`use_average` 从 config 读一次，避免重复加锁），不满足传 None → 递推整体跳过（不推进留存次数、不写文件）。`logger.rs`/`common.rs`/`chiri/config.rs`/四份 meta.yaml/AGENTS.md/前端 `power-avg.ts`+`state.svelte.ts` 共 8 处口径文档同步（注明「参考含息屏、平均仅亮屏放电」）。

**已知交互（如实报告）**：留存次数两模式共享（用户此前明确要求「切到平均时即有历史次数可用」），所以 ref 期含息屏的样本会 seed 进切换后的平均值；要让平均值只从切换点起算需另加复位。UI 标签仍是「平均放电功耗」，实际口径已是「亮屏平均放电功耗」——是否改标签待用户定。

验证：cargo check ✓、svelte-check 0/0、vitest 84、vite build ✓。

## 十八轮：常驻状态通知（shell / cmd notification）

**用户要求**：添加通过 shell 发送通知的支持 + 持久化通知；① 标题 = 当前前台进程名/包名；② 详情 = 模式、家族、子模式、温度、功耗；③ 功耗按「高级设置」的选择用参考值或平均值。

**实现**：新增 `src/notify.rs`（[content] 组装 + [post] 投递）：直接 spawn `/system/bin/cmd notification post -S bigtext -t <标题> -c chiri-status -o chiri-status <正文>`（**不经 `sh -c`**，参数不经 shell 解析）；`Snapshot{pkg, mode, batt/cpu temp, power_w, use_average}` 由 1s 循环提供。模式展示名/家族派生与 WebUI `data/mode.ts` 同口径（特调集合取编译期嵌入 special_tuned modes，OnceLock 缓存），子模式仅在 != 展示名时输出（复用模式卡片的去重口径）。`logger::power_avg_update` 改为返回当前留存值 `Option<f32>`（通知不必回读文件）。1s 循环每 5s 调一次 + **内容不变不重投**；投递命令行三级候选（渠道+常驻 → 去渠道 → 最简），单条失败 debug、全失败 warn 一次 + **60s 冷却**。ftl 新增 12 键（zh/en，含索引行补 `[notify]`）。**关闭调度**是 SIGKILL、daemon 无清理时机 → WebUI `stopScheduler` 追加 `cmd notification post -d chiri-status` 撤销（best-effort 静默）。

**验证**：cargo check ✓、notify.rs clippy 零告警（修掉一处 collapsible_if）、ftl 12 键 zh/en 各 1 条、svelte-check 0/0、vitest 84、vite build ✓。**待实机验证**：`-c/-o/-d` 与通知渠道在各 ROM 的可用性（候选链 + 冷却已兜底）；Yumi 未接入。

## 十九轮：通知正文改单行

**用户要求**：通知拼接成一行，使用 `·` 隔开各个参数。

**改动**：`notify.rs` 新增 `const SEPARATOR: &str = " · "`，`body()` 由 `lines.join("\n")` 改为 `lines.join(SEPARATOR)`；模块头与 `body` 文档、AGENTS.md、MEMORY.md 同步为「单行 · 分隔」。ftl 的五个 `notify-line-*` 键保持「标签：值」形态（用户要求保留各参数标识），温度字段内部仍用 `/` 分电池与 CPU——若也用 `·` 会与参数分隔符混淆。折起时系统自行截断、展开（bigtext）看全，故单行不影响可读性。

验证：cargo check ✓。

## 二十轮：通知去掉字段标签

**用户要求**：不要保留参数标签。

**改动**：ftl 的五个 `notify-line-*` 由「标签：{ $x }」改为纯值模板（`{ $mode }` / `{ $batt }/{ $cpu } °C` / `{ $watt } W（{ $kind }）`），并加注释说明「只出值，由 notify::SEPARATOR 拼接」；`notify.rs` 模块头与 `body()` 文档同步。形如 `default · CLG · 38.5/45.2 °C · 1.36 W（参考）`——顺序固定、单位自解释。保留：功耗的「参考/平均」后缀（用户第 3 条要求的口径提示，不算字段标签）。

验证：cargo check ✓。

## 二十一轮：功耗口径后缀也去掉

**用户要求**：也去掉（指上一轮保留的「（参考）/（平均）」后缀）。

**改动**：`notify-line-power = { $watt } W`（zh/en），死键 `notify-power-ref` / `notify-power-avg` 从两份 ftl 删除；`Snapshot.use_average` 字段与 `body()` 里的 kind 分支一并删除（该字段只服务这个后缀），1s 循环的调用点同步去掉该字段（`use_average` 局部变量仍用于 `sample_ok` 判定，保留）。最终一行形如：`default · CLG · 38.5/45.2 °C · 1.36 W`——**值本身即口径结果**，用户第 3 条要求（按高级设置决定用参考还是平均）由取值实现，不再靠标注体现。AGENTS.md / MEMORY.md / 模块头文档同步。

验证：cargo check ✓、`notify-power-*` 零残留。

## 二十二轮：通知的家族行去重

**用户反馈**：特调就是 Special，为什么会多一个（通知里 `特调 · Special · akmode` 同一个词出现两次）。

**根因**：WebUI 模式卡片早有去重规则（家族行只对 clg/lab/stardust 显示），`notify.rs::body()` 里我无条件输出家族 → 两侧口径不一致。

**改动**：新增 `family_adds_info(family) -> bool`（只认 CLG/RHINE/Stardust），`body()` 只在它成立时 push 家族行；`body()` 文档与模块头同步说明「家族与子模式都按信息量去重」。结果：`default · CLG · 38.5/45.2 °C · 1.36 W`（CLG 保留）、`特调 · akmode · 38.5/45.2 °C · 1.36 W`（Special 隐藏）、`FAS`/`down` 只剩模式名、`vector · RHINE · …`（RHINE 保留）。

验证：cargo check ✓、notify.rs clippy 零告警。

## 二十三轮：通知里删掉「特调」类名

**用户要求**：特调两个字删了。

**改动**：`notify.rs::mode_label` 去掉特调分支——特调模式直接用模式 id（`akmode`/`playback` 才是具体模式，类名「特调」不含新模式信息）；子模式行随之与展示名相同、被既有去重规则隐藏；家族行本就对该类隐藏（`family_adds_info`）。ftl 死键 `notify-mode-special` 从 zh/en 删除。特调模式的通知现在是：`akmode · 38.5/45.2 °C · 1.36 W`。**WebUI 卡片未改**（仍显示「特调 / akmode」——卡片是「类名 + 具体 id」的设计，且 i18n 测试按枚举校验 `mode.special` 存在）；要一起改成只显示 id 需同时动 `data/mode.ts` 与该测试。

验证：cargo check ✓、`notify-mode-special` 零残留。

## 二十四轮：通知开关（可修改项）

**用户要求**：给通知加个开关，在可修改项里加。

**契约（meta.yaml 新字段 `notify`，默认 true）**：严格层 `common::MetaYamlFile.notify: Option<bool>` + `ExternalMetaOverrides.notify` + 合并映射；`chiri::config::Meta.notify`（`#[serde(default = default_true)]`，热重载即时生效）；四个 meta.yaml 模板加 `notify: true` + 注释；WebUI `META_FIELDS`/`WRITABLE_FIELDS`/布尔校验列表/`validateFieldValue` 四处 + ConfigView 一个 ToggleField（走草稿 + 保存，与 fas/scenemode/threadBind 同款）+ i18n `config.notify(.hint)` 双语。

**行为**：`chiri/mod.rs` 1s 循环一次读锁取 `(power_avg, notify)`；`notify: false` 时调 `notify::cancel(counter == 0)`——**首轮无条件**撤销（清上一次运行残留的通知），此后每 tick 幂等（`LAST_POSTED` 为空即返回，不起进程）；开启时保持 5s 周期投递。WebUI `stopScheduler` 里的 `-d` 撤销仍保留（覆盖 SIGKILL 场景）。

**顺带**：`common.rs` 严格层的头注释过时（仍写「八个字段必填」）→ 改为「字段全部可选 + 出现即校验」，并把「新增字段四处同步」点名列清（避免下次漏改 WebUI 判未知键→整体重置）。

验证：cargo check ✓、四份模板 `notify: true` 落地、svelte-check 0/0、vitest 84、vite build ✓。

## 二十五轮：息屏判定改投票仲裁

**用户要求**：两个节点返回息屏即确认息屏；注意无效节点的处理；不再用旧的保守性驳回逻辑。

**改动**（`src/monitor/screen_detect.rs`）：删掉 `screen_on_reading_from_any_node`（任一节点亮屏即驳回，各源读数是内联第三套口径）与只服务它的 `FB0_BLANK` 常量；新增 `tally_screen_nodes()`（枚举候选节点、跳过已退役、统一走 `read_screen_state` 读，不可读的跳过）+ `ScreenVotes{off,on,on_node}` + `screen_off_confirmed()`（`OFF_QUORUM=2`：OFF 票 ≥2 确认；有效节点仅 1 个时按一票算；0 个有效读数不确认）。翻转路径与稳态 10s 复核都改用该判据：稳态只在「票不够 + 至少一个节点报亮屏」时打点、退役主节点、强制改判亮屏；票型仍是息屏则只清计时器。i18n：zh/en 的 `screen-off-vetoed` 改为「息屏票不足」口径，新增 `screen-off-unconfirmed`（无有效读数时 debug）。AGENTS.md 的两阶段判定条目四处同步（②/接受条件/⑤/权衡说明）。

**验证**：cargo check ✓（无新告警）、旧函数零残留。**待实机**：多节点机型上的息屏/亮屏时序（该文件无 Rust 测试，纯 sysfs 逻辑，只能上机看 daemon.log 的 votes 行）。

## 二十五轮补：采样门槛两模式互换（实现与文档背离）

**用户提醒**：注意息屏仲裁这一改动对平均功耗取样判断的影响 → 顺着查采样门槛，发现 **`chiri/mod.rs` 的 `sample_ok` 两个模式正好互换**：原写 `charge_state == "discharging" && (use_average || is_screen_on)`，实际效果是「平均收息屏、参考丢息屏」，与注释 ②③ 和用户口径（平均值=亮屏放电；参考值含息屏）完全相反；`logger.rs` 的文档与 MEMORY 里记的口径都是对的，只有表达式错。十七轮起就这么写着（git HEAD 也是），一直没被察觉。

**修正**：`(!use_average || is_screen_on)`——平均 = 放电 && 亮屏，参考 = 放电（含息屏）。修正后**参考值（默认档）不再看屏幕状态**，即息屏仲裁的改动对默认功耗口径零影响；只有平均模式的样本数随仲裁判 OFF 的时长变化（误判息屏只是少收样本，误判断亮屏会把息屏样本混进均值；新仲裁要求两票 OFF，比旧规则更不易误判断亮屏）。跳过时 `power_avg_update` 返回上次留存值，常驻通知展示不受影响。注释与 MEMORY 同步修正（MEMORY 里原本把错误表达式当成口径记录了）。**可见变化**：默认「参考功耗」从此包含息屏低功耗样本（整夜占多数），读数会往下走；10:1 加权下移动慢。验证：cargo check ✓。

## 二十六轮：审查未提交改动（通知 + 仲裁 + 门槛）

**范围**：20 个文件（+315/-99）＝常驻通知（新 `src/notify.rs` 241 行、meta `notify` 字段、四模板、WebUI 契约/配置页、ftl 9 键）＋ 息屏仲裁重写 ＋ 功耗门槛修正 ＋ 文档/记忆。

**修掉 4 处**：① `chiri/mod.rs` 的 `cancel(telemetry_log_counter == 0)` 恒 false（自增在同一行之前）→「启动首轮清残留通知」永不执行，改为自增前取 `first_tick`，顺手去掉 `telemetry_log_counter > 0` 冗余条件；② `notify.rs` 两处 `Command::status()` 默认继承 stdout/stderr——`cmd` 旗标不支持时把用法文本打进 daemon.log（候选失败是预期路径），补 `Stdio::null()`；③ `notify.rs:206` clippy `needless_borrows_for_generic_args`（`.args(&args)`→`.args(args)`）；④ `AGENTS.md` 两处文档漂移：可写字段 6→7（补 notify）、「八字段必填 + 3 可选」改为实际口径（字段全部可选，只有类型/白名单/未知键判非法）。

**核对通过**：meta 字段四处同步齐全（`MetaYamlFile` 带 `deny_unknown_fields`，漏加会让用户新键被判未知→整文件重置）；ftl 双语键齐、索引同步；`cmd notification` 参数与 AOSP 语义一致（`-o` 独立旗标 + 位置参数 `<tag> <text>`，`-d <tag>` 撤销），WebUI TAG 与 daemon `TAG` 对齐；去重口径与模式卡片一致、文案无句尾句号；`power_avg_update` 改返回 `Option<f32>`（跳过时返回留存值，通知不回读文件）；失败 60s 冷却 + 只 warn 一次；无 panic 路径。验证：cargo check ✓、notify.rs clippy 干净（mod.rs 剩余告警在 106-197 行，属既有）、svelte-check 0/0、vitest 84。

**留给用户判断（未动）**：① Yumi 上开关空转——配置页没按 `app.isChiri` 门控（页面/日志页已有此惯例）；② 通知投递在调度线程同步等待 `cmd`（约 50-200ms/5s），要彻底隔离得起独立线程。

## 二十七轮：通知投递移入独立线程

**用户要求**：投到独立线程（接上一条②）。

**改动**（`src/notify.rs`，块索引 `[content] [dispatch] [worker] [post]`）：调度循环侧 `update()` 只组装标题/正文并 `send(Msg::Post{title,text})`，`cancel(force)` 同理发 `Msg::Cancel`；`[dispatch]` 用 `OnceLock<SyncSender<Msg>>`（**懒启动**线程 `notify`，起不来只留发送端 → `try_send` Disconnected → 退化成不投递）+ `sync_channel(1)` 容量 1 + `try_send`：worker 忙就丢本次（下个 5s 周期再送），**绝不阻塞调度线程**。`[worker]` 串行处理消息，去重（与上次成功内容比对）、60s 失败冷却、进程创建全在线程内（原 `update`/`cancel` 主体拆成 `handle_post`/`handle_cancel`）。命名注意：新静态最初叫 `CHANNEL`，与既有的**通知渠道 id 常量 `CHANNEL`** 重名（编译报 E0428）→ 改名 `WORKER_TX`。文档同步：模块头 + `[dispatch]`/`[worker]` 块注释、`chiri/mod.rs` 调用点注释、AGENTS.md 通知条目（写明通道容量 1 与非阻塞语义）。

**验证**：cargo check ✓、notify.rs clippy 干净。**未做**：Yumi 侧的 `app.isChiri` 门控（仍待用户定）。

## 二十八轮：白名单正则条目解析错位（用户报的两条告警）

**用户贴的日志**：`[Tuned] 白名单模式 (?i)arknights 没有对应参数组…回退 akmode 段` + bilibili 同款。

**根因**（不是漏配参数组）：`src/chiri/special_tuned.yaml` 行格式 `匹配器:模式列表:回退模式`，而 `re:` 前缀自带冒号；`common.rs::parse_special_tuned` 直接 `line.splitn(3, ':')` → `re:(?i)arknights:akmode:akmode` 被切成 `["re", "(?i)arknights", "akmode:akmode"]`：包名成了字面量 `"re"`（`matches()` 只对 `regex.is_none()` 走名字比较 → **正则条目从来没命中过**：B 站 HD/国际版、方舟台服/Mod 变体一直未被接管），模式名成了正则体（→ 告警），回退模式成了 `"akmode:akmode"`。精确条目（`tv.danmaku.bili:playback:playback`）解析正常，所以只有两条正则告警 ✓ 与用户贴的日志吻合。

**修复**（`common.rs`）：先 `strip_prefix("re:")` 再 `splitn(3, ':')`；`package` 仍存原文（`re:...`，只用于日志；WebUI 导出只取精确条目）；无效正则仍跳过告警。`notify.rs::is_special` 的注释前提同步清理（模式名不再含正则体）。注释里写明正则体不得含冒号（yaml 约定）。

**行为变化（要说清）**：修好后 B 站变体走 `playback` 组、方舟变体走 `akmode` 组——正则兜底条目**首次真正生效**。验证：cargo check ✓、clippy 无新增（common.rs 报的几处都在 16-642 行的既有位置）。

## 二十九轮：电池读数来源重构 + 设置页二级化

**用户要求**：① 优先读厂商私有电压/电流节点（目前只有 OPlus：`/sys/class/oplus_chg/battery/bcc_parms`，多参数逗号分隔，下标 6=电芯电压0、8=电流、11=电芯电压1），不存在才回退 Android 默认节点；② 加 OPlus 设备开关（默认关）；③ 加双电芯功能，分倍电压/倍电流，对各自参数翻倍，默认关，**开启 OPlus 开关时关闭这两个并置灰禁启**；④ 加单位校准（默认单位 V/A/W、默认除 1000），删掉原有内置校准；⑤ OPlus 双电芯开关单独一个、挂在 OPlus 开关下；⑥ 所有电流/电压/功率设置移到单独二级菜单页。

**Rust**：`src/monitor/telemetry.rs` 新增 `[options]` 块（5 个原子量 + `set_battery_options`，由 `chiri::config::Config::load` 写入、热重载生效）；`read_oplus_bcc(dual_cell)` 去掉量级启发式与物理范围门，字段按 mV/mA 解读 ×1000 存成 µV/µA 口径；新增 `read_standard_battery()`（标准节点抽成助手）；循环按 `oplus_chg` 选源、私有不可读回退标准，`!use_oplus` 时才应用倍压/倍流（互斥双保险）；`batt_voltage_v`/`batt_current_ma` 接单位校准（µV/1000/D、µA/D——D=1000 时与旧口径完全一致）；私有节点探测从启动期改为**开关打开后懒探测一次**，新增 `telemetry-oplus-bcc-missing` 告警键。配置链：`common.rs`（MetaYamlFile/ExternalMetaOverrides/parse_disk_meta + unit_divisor 越界回退）、`chiri/config.rs`（Meta 五字段 + apply_meta_overrides + load 末尾推给遥测层）、`utils.rs`（`DEFAULT_UNIT_DIVISOR` + `default_unit_divisor()`，遥测与配置共用一处默认值）、四份 meta.yaml 模板各 5 个新键（默认 false×4 / 1000）。

**WebUI**：新增二级页 `views/BatteryView.svelte`（读取来源 / 单位换算 / 功耗显示三块，ChiRi 门控 + 非 ChiRi 显示 StateBox）与 `components/NumberField.svelte`（首个数值输入：失焦/回车提交、非法值不回滚只标红、初值也由 effect 写入以避开 svelte 的 state_referenced_locally 告警）；`router.svelte.ts` 加 `'battery'`（SUB_OF→config，`isSubView` 改成 `id in SUB_OF`）；`App.svelte` 分支；配置页入口 Panel + 移走原高级设置里的功耗口径开关；`contract/meta.ts` 字段与校验（布尔列表 + unit_divisor 数字与 >0 校验）；`state.svelte.ts` 加 5 个 getter + `battPending/battError` + `setBatteryFields`（开启私有节点时同一笔写入清掉倍压/倍流）；i18n zh/en 各 28 键。

**验证**：cargo check ✓、clippy 无新增（telemetry/common 报的都是既有位置）、svelte-check 0/0、vitest 85（新增电池字段校验用例）、vite build ✓、本地 dev server 5174 已开（`#/battery` 预览）。**注意**：`oplus_dual_cell` 不开时私有节点只取下标 6（2S 机型约为整包电压一半，功率随之减半）——上机先确认这个开关。

## 三十轮：审查兜底 + 精简 UI 文案

**审查发现并修掉 5 处**：① `bcc_available` 原本「开关打开后只探一次」——开机瞬间驱动未就绪就永久回退标准节点；改成每 `BCC_PROBE_INTERVAL`(60s) 复查 + `Option<bool>` 跟踪未知态、**只在状态变化时打点**（缺失 warn 带独立去重，恢复后重新武装）。② `setBatteryFields` 的强制清空原本排在展开**之前**（`{voltage_double:false, ..., ...patch}`），调用方多传 `voltage_double: true` 会把互斥组合写进文件；改成展开在前、强制清空在后。③ meta 非法时开关全禁用却看不出原因 → 页面加 StateBox 显示 `metaProblems`。④ `NumberField` 输入了但没失焦提交的值会静默丢 → 加 `dirty`（`text !== String(value)`）显示待提交点（与 ToggleField 同款标记）。⑤ 私有节点打开时倍压/倍流显示文件里的值会与「不生效」文案矛盾 → 显示改为 `app.voltageDouble && !oplus`（反映真实生效状态）。

**i18n 精简**（用户要求）：28 → 22 键——删 `battery.entry`（复用 `battery.title`）、`battery.entry.oplus` / `.standard`（当前来源由开关状态直接可见）、`battery.written`（无信息量 toast）、`battery.source.hint` / `scale.hint` / `power.hint`（与字段级 hint 重复，`Panel` desc 直接不传）；`oplusChg` / `voltageDouble` / `currentDouble` / `powerMax` 四条 hint 去括号补语；新增 `battery.metaInvalid`（禁用原因，属必要信息）。zh/en 同步。

**验证**：cargo check ✓、svelte-check 0/0、vitest 85、vite build ✓（exit 0）、残留引用扫描为空。

## 三十一轮：安装时询问是否保留配置

**用户要求（第一次做法被否）**：最初在脚本最前部加了 `[config-preserve]` 段（无条件删掉 staging 里 live 已存在的 meta.yaml）→ 用户要求撤销，改为**模式选择（普通安装 / 热更新）之后**追加一个音量键提问：是否保留现有配置；**保留则 `rm -rf "$MODPATH/config"`**，不保留则维持现在流程。

**改动**（`module/customize.sh`，块索引 `[busybox] [i18n] [welcome] [hot-update-check] [volume-key] [config-keep] [mode-select] [hot-update-flow] [full-install]`）：① 删掉 `[config-preserve]` 段；② 音量键检测函数从 `if HOT_UPDATE_AVAILABLE` 分支内**提到顶层 `[volume-key]` 段**（两处共用；超时文案从「使用完整安装流程」改成通用「使用默认选项」）；③ 新增 `[config-keep]` 段 `ask_keep_config()`：音量上 = 保留（`rm -rf "$MODPATH/config"`，超时同支，推荐）、音量下 = 不保留（什么都不做，模板照旧覆盖）、多次按键 = abort 取消安装；④ 调用点两处：模式选择之后（完整安装与热更新共用）与「热更新不可用」的回退分支（首次安装没旧配置，保留也无害）；⑤ 新增 5 条 `MSG_KEEP_CONFIG_*` 双语提示。

**保留口径的兜底说明**：删掉的只是暂存副本，模块目录里用户的 `config/**` 原样保留；缺键由 `common::sync_meta_snapshot` 用内嵌默认补齐（字段全可选）、格式非法才整份覆盖 + 追加警告注释（`nofix` 跳过）。`config/` 里的 `i18n/*.ftl` 与 `normal/*.yaml` 是随包副本、运行期不读，选保留时停在旧版（已在注释里写明）。

**验证**：`sh -n` 语法通过 ✓；用真实函数体做功能测试（stub 掉 `ui_print`/`abort`/`detect_volume_key`）——音量上 → staging 的 `config/` 被删空 ✓、音量下 → 两份 meta.yaml 保持 ✓、错误码 2 → 走 abort ✓；块顺序与文件头索引一致 ✓。

## 三十二轮：五件收尾（按钮对齐 / 置灰 / 英文对齐 / 索引核对 / 安装自动开 OPlus）

**① 入口按钮右对齐**：配置页电池读数卡片的「打开」按钮原来在 `u-between` 里单子元素 → 靠左；换成自建 `.entry__row`（`display:flex; justify-content:flex-end`），避免和全局工具的优先级纠缠。

**② 常驻状态通知开关置灰**：`ConfigView` 的 notify `ToggleField` 去掉 `disabled={!app.metaValid}`，改恒 `disabled`（中文 hint 已是「功能未完成」）。

**③ 英文按中文对齐**（用户要求，不止 ts）：`en.ts` 改 5 处——`config.header`「配置头信息」→ Config header info、`config.notify.hint`「功能未完成」→ Not implemented yet、`config.powerAvg.hint` 去掉「仅放电时取样」的英文补语、电池页 7 条（entry.hint / oplusChg.hint / oplusDualCell.hint / scale.title / 倍压倍流 hint / double.mutex）按用户改写后的中文重写；安装脚本 `MSG_KEEP_CONFIG_UP/DOWN/YES/NO` 也跟着中文（用户把「保留」标成「不建议」）。检查：解析 zh/en 两文件比对键值与长度比 → ts 与 ftl 都没有「en 明显短于 zh」的残留；`.svelte` 里没有写死的中文（全走 `t()`）。

**④ OPlus 索引核对**：确认 `read_oplus_bcc` 用的是 `f.get(6)/f.get(8)/f.get(11)`——Rust 切片 0 基，与用户口径（0 基 6/8/11；若按第 1 项算则是 7/9/12）一致，**没有 off-by-one**；注释两处（telemetry.rs、AGENTS）补「0 基下标 / 第 7、9、12 个字段」说明。顺带观察：用户给的样本里下标 8 = 258（按 mA 解读 = 258 mA），且下标 12/13 = 507143/505370（按 µA 解读约 507 mA）——两处都像电流，已提醒用户核对。

**⑤ 安装脚本末尾自动开 OPlus**：`[oplus-detect]` 段 `apply_oplus_defaults()` + 末尾 `[ "$CONFIG_KEPT" != "true" ] && apply_oplus_defaults`；定位当前机型（active_config.chr → ro.soc.model 数字 → 放弃并打印）；sed 优先 busybox 再退 PATH；4 条双语新消息。**验证**：`sh -n` ✓，用真实函数跑三例（19 字段 → 两个开关都开 ✓；11 字段 → 只开 oplus_chg ✓；节点不存在 → 不动 ✓）。已知缺口：热更新路径以 `abort` 结束、走不到末尾，「热更新 + 覆盖」不会自动开。

**验证汇总**：sh -n ✓、cargo check ✓、svelte-check 0/0 ✓、vitest 85 ✓、vite build ✓（exit 0）、临时测试目录已清理 ✓。

## 三十三轮：全量审查未提交改动（24 文件 / +715−186）

**查出并修掉 4 处**：① **Rust 引用了不存在的 ftl 键** `config-scenemode-parse-failed`（`chiri/config.rs:955,977`，HEAD 里也缺——**既有 bug**，日志会打键名本身）→ 两份 ftl 各补一条；② **meta.yaml 直写竞态**：`setPowerAvg` 与 `setBatteryFields` 各有本地 pending 标志，两笔并发时「读-改-写」互相覆盖字段（丢更新）→ 加共享锁 `state.metaWritePending`，两个 setter 都先查后置、finally 释放，电池页 `busy`/`canWrite` 跟着这把锁走；③ `NumberField` 提交等值文本（1000 → 1000.0）时值不变但仍显示「待提交」标记 → 提交时归一文本；④ `[full-install]` 注释说「不执行文件操作」与实际（尾部 `[oplus-detect]` 会改 staged meta.yaml）不符 → 改注释。

**核对通过（未改）**：ftl 键齐（脚本扫 Rust 里 256 个 kebab-case 键引用，zh/en 全有）；webui 引用键抽查的"缺失"全是 `set('loglevel')` / `absent('unsupported-env')` 这类非 i18n 调用的假阳性；`customize.sh` 的 `CONFIG_KEPT` 两个分支都赋值（尾部 guard 不会误跑）、sed/cut/getprop 缺失都有打印兜底；telemetry 选项经 `Config::load` 推进原子量（载入失败保持旧值，不会半新半旧）、越界值 `i32::try_from` 失败回退标准节点；私有节点开着但读失败时倍压/倍流不生效（与 UI 置灰一致）。

**如实列出、未动的既有风险**：a. 热更新 + 覆盖走不到脚本末尾 → 不会自动开 OPlus 开关；b. `down.chr` / `rhine.chr` 不在「保留」范围（完整安装会清掉用户的停摆/实验室状态）；c. 「保留配置」选项被标成「不建议」，但超时/无按键默认仍走「保留」；d. 配置页 `commitDraft` 没进 `metaWritePending` 锁（跨页竞态窗口极小）。

**验证**：cargo check ✓、svelte-check 0/0 ✓、vitest 85 ✓、vite build ✓（exit 0）。

## 三十四轮：入口按钮对齐 / 删除历史日志 / 功耗条百分比

**① 入口按钮仍然靠左（上一轮是漏了 CSS 规则）**：`ConfigView` 里只把 `class="u-between"` 改成 `entry__row`，**没加对应样式**，等于把布局全丢了 → 补 `.entry__row { display:flex; justify-content:flex-end }`（`.ak-card__content` 只是 `margin-top`，块级容器，flex-end 够用）。

**② 「删除历史日志」按钮从来不存在**（`deleteLog`/`logs.delete`/`clearLogs` 在代码与 git 历史里都查不到）→ 按「导出卡片的另一半」实现：契约 `sources.ts::clearArchives()`（`rm -rf` 掉 `logdDir` + `devimpDir`；devimp 由 daemon 启动重建、logd 下次归档自动出现，当前会话的 `logs/` 不动）→ `state.deleteArchives()`（pending/error + 成功后 `loadLogs()` 刷新）→ `OverviewView` 导出卡片底部加危险按钮 + `ConfirmSheet` 二次确认 + 错误行 → i18n 三键（zh 先写、en 对齐）。

**③ 功耗条加百分比数字**：条旁边显示 `NN%`，这样「条是空的」能直接区分是读数为 0/缺失还是渲染问题。核查结论：CSS 没问题（上游 `.ak-progress__fill` 读的就是 `--ak-progress-value`，项目里没有覆盖，两个设置点都在 `OverviewView`），所以更可能是**数值本身接近 0**——与「单位校准」的倍率问题同源。

**待用户确认（已提问）**：a. 「OPlus 私有节点检测未生效」指安装脚本没改 meta，还是 daemon 没走私有节点；b. 单位校准的预期语义（当前实现 V 与 A 各除一次 → 功率 ∝ 校准⁻²，加一位 ÷100；用户期望线性）。

**验证**：svelte-check 0/0 ✓、vitest 85 ✓、vite build ✓、新增 i18n 键 zh/en 齐 ✓。

## 三十五轮：校准拆两个（用户选 C）+ 让 OPlus 检测一定打印

**① 单位校准拆成 `voltage_divisor` / `current_divisor`（用户选 C）**：Rust 侧 `telemetry` 原子量拆两个（各带 `sane_divisor` 兜底）、`batt_voltage_v`/`batt_current_ma` 分别用、`batt_power_w` 仍 = |mA|×V（W = V×A 自洽，不再出现「改一位功率移两位」的平方效应）；`MetaYamlFile` **保留旧键 `unit_divisor` 字段** + `parse_disk_meta` 用 `f.voltage_divisor.or(f.unit_divisor)` 当电压校准兜底——不这么做，已装机的老 meta.yaml 会被 `deny_unknown_fields` 判非法、整份重置（WebUI 的 META_FIELDS 也保留旧键，否则界面会把它当未知键报错）。四份模板、契约（字段/校验/`validateFieldValue`）、state（两个 getter，电压那支兼容旧键）、电池页（两个 NumberField）、i18n 双语、测试用例同步。

**② OPlus 检测「没有 print」的两个原因都堵上**：a. `apply_oplus_defaults` 只有成功才打印 ✗ → 现在**入口先打「正在检查 OPlus 私有节点…」，节点缺失再打「本机没有…，开关保持原样」**（不再出现「静默什么都不发生」）；b. 函数原本定义在文件末尾、只在完整安装的收尾调用 ✗ → **定义提到 `[oplus-detect]` 段（`[config-keep]` 之前）**，调用改到 `ask_keep_config` 的「不保留」分支里 —— 这样**普通安装与热更新两条路都会检测**（热更新以 abort 结束、走不到文件末尾，这是上一轮遗留的缺口），且热更新时修正发生在 `cp -r` 之前，改的就是要复制过去的那份文件。

**验证**：cargo check ✓、`sh -n` ✓、svelte-check 0/0 ✓、vitest 85 ✓、vite build ✓；用真实函数跑两例——节点在（19 字段）→ 打印 CHECK/ON/DUAL 且 meta 两项都改 true ✓；节点不存在 → 打印 CHECK/SKIP 且 meta 不动 ✓。

## 三十六轮：倍率全链路审计 + PowerAVG 为空的诊断

**① 倍率审计（结论：没有隐藏倍率）**：`节点 → 报告功率` 上的全部因子＝① 私有节点存储 ×1000（mV/mA → µV/µA，只为与标准节点同口径；随后被电压的 `/1000` 与电流的 `/D_c` 精确抵消，D=1000 时净效果为 1）；② 标准节点固定 `/1000`（µV→mV，ABI）；③ `voltage_divisor` / `current_divisor`；④ `voltage_double` / `current_double` 的 ×2（仅 `!oplus_chg`，默认关）；⑤ `W = |mA| × V`（`batt_power_w`）。`logger::power_avg_update` 内部只有加权递推（`(旧×10+新)/11`、`(旧×n+新)/(n+1)`），无任何缩放。默认配置下与改动前逐位一致。

**② PowerAVG.chr 始终为空的三个可能 + 全部可见化**：文件只在样本被接受时写；接受条件＝① `charge_state == "discharging"`；② 平均模式还要 `is_screen_on`；③ `batt_power_w()` 有值。发现真问题：`read_battery_charge_state` 原本**精确匹配** `Charging/Discharging/Full/Not charging`，内核写别的大小写或拼写就归一成 `-` → **永远不算放电 → 文件永远是空的**（且完全静默）。修：改成小写化后比对（容忍大小写/空白），不认识时 warn 一次并打出**原文**（`battery-status-unknown`，恢复后重新武装）。另外新增 `power-avg-skip` 日志：取样被跳过时打印原因（`not-discharging(...)` / `screen-on` / `no-reading`），同一原因只打一次、写入成功后清空——界面只看得到「文件为空」，日志现在能指到具体哪一条。ftl 中英各补两键。

**验证**：cargo check ✓（无新告警，含一处语句上误用文档注释的修正）。**给用户的零成本自查**：看 `logs/status.csv` 的 charge 列，是 `-` 就说明 status 节点取值不认识。

## 三十七轮：内置校准清零 + 未更新原因上界面

**① 去除所有内置校准（用户要求）**：链路现在是 `输出 = 节点原始值 ÷ 校准值`——删除标准节点电压的 `/1000`（旧 ABI 步）与私有节点读数的 `×1000` 归一（`read_oplus_bcc` 改为原样存、越界 `i32::try_from` 兜底回退标准节点）。因此两个校准值**完全决定单位**：标准节点（µV/µA）填 `1e6 / 1000`，私有节点 bcc_parms（mV/mA）填 `1000 / 1`；模板默认仍是 1000/1000（用户此前的口径），所以**开私有节点后必须把这两个值改成 1000/1**（否则电压/电流各差 1000 倍）。文档同步：telemetry 选项块与访问器注释、common/config 字段注释、四模板注释、WebUI 提示（zh/en）、AGENTS、MEMORY。

**② PowerAVG 未更新原因搬到界面**：`state.powerStaleReason`（只在取不到值时计算，读 `status.csv` 末尾 4KB）→ 依据最后一行的 `charge` / `screen_on` 推三条原因（非放电 / 平均模式息屏 / 读数不可用），`OverviewView` 优先显示它，读不到 status.csv 或没有行时回退 `overview.power.missing`（用户指定其只作兜底）。i18n 三键双语。

**验证**：cargo check ✓、svelte-check 0/0 ✓、vitest 85 ✓、vite build ✓（exit 0）。

## 三十八轮：审计 DOWN 是否真的停调度只留日志（结论：停得住）

**进入停摆**（`down.rs` 的原子标志翻转后，`mod.rs` 检出 `down != halted`）：按顺序释放 `cpu_governor.release()`、`ak_governor.release()`、`fast_lock.release()`、`fas_mgr.deactivate_all()`、`affinity_mgr.release()` + `lab_static_deactivate()`、`governor_guard.release()`、`gpu_guard.release()`、`corectl_mgr.set_power_state(false,false)`，然后 `mode_clone = "down"` 并写一次 `current_mode.chr`。

**停摆期的门控**（全部带 `halted`）：① `current_mode.chr` 自愈只在 `!halted` 时写（`down` 不会被覆盖）；② config 热重载只在 `!halted && is_screen_on` 时**下发**（`devimp` 采集开关无条件同步）；③ 热保护 / 2s 亲和块 `!halted`；④ 事件循环在 `recv` 之后、`match msg` 之前 `if halted { continue }`，只吸收屏幕状态（避免退出时按过期屏态下发布局）。

**照常运行的**（按「只留日志」的设计）：1s 遥测块（status.csv / daemon.log 摘要 / devimp / PowerAVG / notify）、eBPF 采集、app_detect、`ensure_watchdog_pid_file`。**唯一会写调度的 1s 分支**是 `fas_mgr.tick()` 的延迟退出再接管——已核实**不可达**：`tick()` 只在 `exit_deadline` 到期时返回 true，而进入 DOWN 的 `deactivate_all()`→`deactivate_active()` 会把它清空（fas_manager.rs）；已在 mod.rs 该分支补注释写明这个不变量。

**退出停摆**：先删 `current_mode.chr` → 恢复 `down_resume_mode`（进入前的实际模式）→ 按该模式重建 governor / FAS / 亲和 / core_ctl（含 contingency/babel 的 lab 分支）。

**本轮无逻辑改动**（只加一条不变量注释）：cargo check ✓。**上机验证法**：`down.chr` 写 `down` 后，`current_mode.chr` 应停在 `down` 不动、status.csv 仍在增长、`scaling_max_freq` 不再被改。

**需求**：缩小功耗显示的计算口径到仅电池放电时才计算。

**改动**：`src/chiri/mod.rs` 调用点加门——`charge_state == "discharging"`（`read_battery_charge_state()` 归一化值）才传 `tm.batt_power_w()`，否则传 `None` 让 `power_avg_update` 整体跳过（递推次数不推进、不写文件 → 文件保留上次放电值）。原因：`batt_power_w` 用**电流绝对值**，插电/充满/未充电时电流由充电链路决定，混入会把耗电均值抬高。`logger.rs::power_avg_update` 文档补「取样口径由调用方保证＝仅放电」；status.csv 的 batt_power_w 列仍写原始遥测，不受影响。前端注释同步（power-avg.ts / contract/power.ts）；i18n `config.powerAvg.hint` zh/en 补「仅电池放电时取样」。

验证：cargo check ✓、svelte-check/vitest 76 ✓（无 prettier 依赖，格式手工对齐）。

## 八轮：调度状态判据重写为心跳文件

**需求（用户）**：webui 的调度状态还是有问题，重写逻辑——daemon 每 15s 往 `LiveTime.chr` 写当前时间（分秒），WebUI 刷新时比对差是否 > 20s，超过即判「调度已关闭」。

**daemon**：`logger.rs` 新增 `LIVE_TIME_CHR` / `LIVE_TIME_INTERVAL_SECS=15` / `write_live_time()`（复用 `local_hms` 本地时间、`common::write_file_no_panic` 原子写、失败静默）；`main.rs` 在 `[chiri-module-starting]` 之后、`[channels]` 之前起独立线程 `live_time`（循环写+睡，永不 panic）——两套调度器共用，语义仍是**进程级存活**（与旧 flock 一致；调度线程卡死不带停心跳，如实说明）。

**WebUI**：新增 `data/live-time.ts`（`LIVE_TIME_FILE`/`LIVE_TIME_TOLERANCE_SECONDS=20`/`parseLiveTimeSeconds`/`liveTimeAgeSeconds`（1 小时取模）/`isLiveTimeFresh`/`nowSecondsOfHour`）；`contract/daemon.ts::probeLiveness` 改读心跳：缺失→stopped、非法→failed（界面「无法判定」+报错）、新鲜→running、过期→stopped；删 `hasFlock`/`setFlockAvailableForTest`；`state.svelte.ts` 删 `flockAvailable` 与 `hasFlock` 并发项；`OverviewView` 的 detail 三元简化为 `daemonError || t(daemon.<state>.detail)`；i18n 三条 detail 改心跳口径（zh/en）；`REL.daemonLock` 删除；dev mock 动态生成心跳 + 删 flock 模拟。

**验证**：vitest 84 全过（新增 live-time 8 例，其中一处期望值笔误 3530→3570 修正）、svelte-check 0/0、cargo check ✓（clippy 剩余告警均为既有文档类，非本次代码）、vite build ✓、残留扫描仅注释提及 flock。**文档**：AGENTS.md 补「存活心跳」契约条目 + 对外文件清单加 `LiveTime.chr` + 分层描述改写；MEMORY.md 接触点表 13→14 项。

**遗留**：心跳是进程级——调度线程卡死（不退出）时仍显示运行中（与旧 flock 判据同局限，如需覆盖要另做「调度循环 tick 数」类信号）。

## 九轮：模式卡片去重

**用户反馈**：当前模式卡片中子模式显示了两次。

**根因**：卡片三层 = 家族 / 模式名 / 原始 id。CLG 与实验室档的 `mode.*` 展示值就是 `current_mode.chr` 的原值（`mode.default`='default'、`mode.vector`='vector'）→ 模式名与 id 行印同一个词；`down` 档更严重（家族 'DOWN' + 'down' + 'down'）；FAS 档家族与名字同为 'FAS'。

**修复（OverviewView）**：① `showFamily`——家族行只对 `clg`/`lab`/`stardust` 显示（这几个的成员名看不出家族），其余家族的展示名本身即家族；② `showModeId`——id 行只在忽略大小写后与模式名不同时显示。另补 `.mode__family` 样式（原来没有规则，`<p>` 默认外边距顶开 `.mode__main` 的 0.2rem 行距）。

**验证**：svelte-check 0/0、vitest 84、vite build ✓（无 i18n/契约改动，纯展示层）。

## 十轮：DANGER 等字样配色

**用户反馈**：部分 DANGER 等字样颜色选取不恰当。

**根因与修复（都是「信号色只改一半」）**：
1. 弹层眉头字 `DANGER`/`CONFIRM`——上游 `.ak-dialog__eyebrow` 固定 `--ak-dialog-info`（信息蓝），而弹层左侧竖条是 `--ak-dialog-signal`（危险红 / 暖黄）→ 红条配蓝字。`app.css [ak-adapt]` 新增 `[data-ak-ui] .ak-dialog__eyebrow { color: var(--ak-dialog-signal) }`。
2. LabView「启用中」标签——本地只 patch 了 `border-color` 为危险色，`--ak-tag-signal` 仍是官方默认信息蓝 → 红框 + 蓝信号条。改用官方 `ak-tag--danger`（信号条 + 描边 + 文字成套），删掉 `[data-state='on']` 补丁。
3. ConfirmSheet「注意事项」竖条硬编码 `--ak-signal-action` → 危险弹层里红条红字间夹一条黄线；改为跟随 `var(--ak-dialog-signal)`。
4. 总览页「PowerAVG.chr 缺失或为空」说明用了 `.u-danger`（文件缺失是「调度没跑过」的正常形态）→ 降为 `.u-note` 次要色。

**验证**：svelte-check 0/0、vitest 84、vite build ✓；产物核对——`[data-ak-ui] .ak-dialog__eyebrow` 位于上游规则之后（同特异性、靠顺序生效）、`ak-tag--danger` 在产物中、`.note` 竖条用 dialog-signal、`data-state` 补丁零残留（剩余命中来自上游 `.ak-loading-screen[data-state]`）。

## AGENTS 同步 + 全量未提交审查（除 i18n）

**AGENTS 同步 5 处**：meta 契约（9 必填 + 2 可选 + 追加写入语义）、快照自愈 nofix 门控 + 新增「内嵌 WebUI 资产还原」条目、功耗条目补 power_max_w、WebUI 新增「样式分层（官方类直接复用 + ak-adapt + 单列表格内距规则）」、可写字段清单更新（7 字段 + power_max_w/nofix 手改）；[lessons] 加「整文件覆盖前先确认目标非已跟踪文件」。

**审查发现并修复 1 个真问题**：`build.rs` 在 HEAD 已存在（eBPF 构建 + assert_required_configs + module/config 重编触发），本轮被我的 webui 内嵌版整文件覆盖——cargo check 静默通过（ebpf 占位产物已在、断言消失不报错），CI/干净构建会断。已 `git checkout HEAD -- build.rs` 还原并**合并**两段功能（[webui-embed] 作为独立函数挂进 main），复验：config 断言 + eBPF 构建 + webui 清单（5 条目 EMBEDDED=true）全部在。

**其余结论**：脚本/xtask/module.prop 的 diff = yumi→chiri 改名收尾 + pack.sh chmod（此前轮已审，无新问题）；rust meta 链（common/config/utils/main）口径自洽；webui 新契约文件（power.ts/power-avg.ts）健壮（absent→null 不报错）；四份 meta.yaml 模板按 daemon 严格口径实测 VALID（9 必填 + power_max_w=12 + nofix 缺省）；webui 视图/组件为今日浏览器实测过的内容。**待实机**：nofix 跳过、webroot 还原、满量程手改后仪表盘。用户 WIP（feature.yaml/affinity/tuned_profiles/mdocs/.github 等）此前轮已审、本轮未再动。

**二轮补充（用户复查 + 2 项新要求）**：① 高级设置包 `.ak-form-stack` 强制一行一个——`.ak-choice` 是 **inline-grid**，不套栅格会横向挤成一行多个（可写配置面板原本就有栅格所以没事，高级面板没有）；② **删掉满量程输入框**（用户改口径）：`setPowerMax`/`powerMaxPending`/`powerMaxError`/`config.powerMax*` 文案全删，`power_max_w` 只在 meta.yaml 手改，`powerMaxWatt` getter 留给仪表盘换算；③ ak-status 补 `align-content: center`——官方两行网格（信号条跨行）会把 `align-content: normal`(stretch) 的多余高度分给空行，文字上浮不居中。复测：高级面板两开关 x=33 同列（y 差 60）、numInput=false、label 中心=芯片中心（垂直 34=34；水平组居中，信号点占左侧属设计）。
