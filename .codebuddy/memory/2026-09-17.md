# 2026-09-17

## 实验室卡片：副标题定为「模式名 + 描述」两层

用户问实验室模式卡片是否有副标题。现状是三层：`mode__name`（模式名）→ `mode__key`（裸 key，等宽小字 `vector`）→ `mode__detail`（长描述）。给了四个选项（保持现状 / 加英文短副标题 / 副标题用中文译名 / 改造 key 行），用户选「只要模式名 + 描述」。

改动（`webui/src/views/LabView.svelte`）：
- 删掉卡片里的 `<p class="mode__key u-mono">{key}</p>` 行
- 删掉对应的 `.mode__key` 样式块（7 行）

`key` 变量仍在用（`t()` 插值、`enableable()`、`onclick`），只是不再显示；`.mode` 的 grid `gap: 0.35rem` 让删行后的间距自然收紧，不需要补样式。`u-mono` 在页面其他位置（当前状态面板的设备 id）仍在用，保留。

验证：svelte-check 唯一报错仍是用户 WIP 的 `data/mode.ts`（`descKey`），与本轮无关。

## 审查：全量未提交变更（53 改 + 18 新，约 2500 行）

用户要求「审查所有变更未提交的代码」并用 token-efficient 方式。做法：并行派 5 个**只读**子代理按模块审（daemon 的 rhine/lab、down+logger、chiri/monitor/scheduler+配置模板；WebUI 的 lab/config、down/overview/基建），我只复核结论、修确认项——子代理的读取不进主上下文，这是这次唯一可行的成本控制方式。

### 修掉的（全部已验证）

1. **改名未贯通**（最严重）：`src/scheduler/config.rs` 还是 `pub fast: Mode` + `get_mode("fast")`，而 `feature.yaml` 段名已是 `vector` → `Modes` 没有 `deny_unknown_fields`，vector 段被**静默忽略**、CLG 拿不到档位参数。改字段名 + 匹配值；`module/config/feature.yaml` 文件头 `[fast]` → `[vector]`。
2. **off 在非锁定态不写回**：`out.forced` 只在锁定分支置位，重启后（tmpfs 标记已消失）用户写的 `off` 会永远留在文件里 → 界面停在「已提交强制关闭」中间态。改成以 `matches!(state, State::ForceOff)` 为准：无论有无标记都还原 + 写回内置模板。
3. **痕迹语义错**：运行时把 rhine.chr 写坏时 `watch_loop` 已先 `read_state` 修复过文件，`converge` 再读只剩 Off → 标记里记成 `close-rejected`（「有人请求关闭」）而不是 `state-reset`。在 `watch_loop` 补一次留痕（`lock_note` 有锁定守卫，不会凭痕迹造出标记）。
4. **DOWN 生命周期两处**（两个子代理独立命中同一处）：① 启动即停摆时启动块无条件 `apply_affinity_and_corectl`（normal 布局写 cpuset），而 `halted` 初值就是 `is_down()`、循环里那次「进入停摆」永不执行 → 写进去没人收；② 退出停摆只复位内存模式、不重新接管，而 ModeChange 只在「模式真的变了」时才重接管 → 前台没换就永远停在释放态。修：启动块 `if !halted`；退出时按模式重建 CLG / vector 锁频 + 亲和 / core_ctl（口径同 panic 自愈）。
5. **WebUI 与 daemon 的解析口径分歧**（lab + down 同源）：daemon 行内注释交给 serde_yaml（`vector # x` = vector）、引号用 `trim_matches` 逐边剥且**不 trim 内侧**；WebUI 之前判 invalid / 多 trim 一次。统一成「剥行内注释 + 逐边剥引号 + 不 trim 内侧」，两侧测试各补绊线用例。
6. **配置页接管置灰失效**：`ConfigView` 冷启动直接落 `#/config` 时 `app.isChiri` 还是 false（`loadOverview` 未 await）→ `loadLab` 被跳过 → 被接管的开关照常可切。改成 await 后再判。
7. **强制关闭的 pending 窗口**：`forceDisableLab` 先 await `commitLab`（内部已把 pending 置回 false）再等 400ms → 窗口内按钮复活、可与 daemon 收敛抢写 rhine.chr，且等待后没重读 meta。改成 `commitLab` 收 `settleMs`，等待落在 pending 区间内并补 `refreshCommon`。
8. **虚假覆盖**：`tests/lab.test.ts` 那条「刚性断言（改 yaml 忘同步就红）」其实写死字面量、改 yaml 不会红。改成真读 `module/config/rhine-init.yaml` 比对模式集合与每模式的 meta 影响项。
9. **死 key 与用户 WIP 的报错**：`lab.file` / `lab.status.invalid`（zh/en 都定义、0 引用）删除；`data/mode.ts` 的 `descKey` 被注释但 `describeMode` 在用（总览页描述空白 + svelte-check 唯一报错）→ 恢复四条 `descKey`（locale 里本来就有），svelte-check 现在 0 error 0 warning。
10. **说明书缺字段**：`module/config/config-example.yaml` 称「只有四个可写字段」且缺 `thread_bind`——照它写 meta.yaml 会因八字段不全被判非法整体覆盖。补字段与说明（四→六）。

### 报告但未修（需决策 / 低价值）

- **锁定期间手改 rhine.chr 换模式**：旧模式写过的 `global_mode` / `special_tuned` 运行时覆盖不会撤销（新 def 没写就不动），文件与实际会分叉。要修得先定义「换模式 = 按快照还原旧模式触及项」的语义（多一次热重载 vs 部分还原的复杂度）。
- `src/down.rs` 读失败（非 `NotFound`，如非 UTF-8）一律当缺失 → 用模板覆盖写回，会冲掉用户手改内容。
- `dev/mock-shell.ts` 未 seed `down.chr`（无害，口径一致）；一处注释与代码相反（`rhine.chr` 的 put 位置）。
- README 的 FAS 措辞已过期、`affinity.rs` 文件头注释的选核权重 0.2 与实际 0.4 不符、三个 `{soc}/feature.yaml` 的「整簇断电」注释与实际「只下线 prime」不符（均历史遗留，非本轮改动）。

验证：`cargo check` 通过（touch 强制重编译确认）；vitest **75 例全过**；svelte-check **0 error 0 warning**。

## FAS 内部避开 `fast`：`fast_decay_*` → `steady_decay_*`

用户先提醒「别把 FAS 的 fast 当改名部分」，随后要求「修改 FAS 内部函数名，避开 fast」——即档位改名不动 FAS，但 FAS 内部自己那一组 `fast` 也要换名（`fast` 已不是档位名，留着只会和「极速档」混淆）。

改动（纯重命名）：
- `src/fas_types.rs`：4 个字段（`steady_decay_frame_threshold` / `perf_threshold` / `max_step` / `min_step`）+ serde 默认值函数 `d_fd_*` → `d_sd_*` + `normalize` 的校验与文档注释
- `src/scheduler/fas/frame_pipeline.rs`：`apply_fast_decay` → `apply_steady_decay` + 5 处字段引用
- `src/scheduler/fas/policy_mgmt.rs`：4 处参数同步（热重载）
- `src/monitor/fps_monitor.rs`：注释里的参数名
- `module/config/normal/fas-example.yaml`：4 个 key + 段注释（附改名说明）
- `README.en.md`：yaml 示例、参数表 4 行、4 处 "fast decay" 描述文字

**没动的**（都不是 FAS 内部，用户也说清了边界）：`utils::FastWriter`（sysfs 写入器，FAS/CLG 共用）、`chiri/fast.rs` 的 `FastLock` + `chiri/mod.rs` 的 `fast_lock`（极速档锁频器）、i18n 的 `fast-*`（`[Fast]` 锁频器日志）、CLG 的 `down_fast_threshold` / `down_fast_mult`、`controller.rs` 里 "Rise fast, fall slow" 这类自然语言注释。yaml key 改名没有兼容负担：FAS 配置编译期嵌入、不落盘、无外部读取方。

验证：`cargo check` 通过；全仓库已无 `fast_decay`（只剩 `fas-example.yaml` 里那行刻意留的改名说明）。

## 第二轮审查：按逻辑分析（4 个子代理按逻辑域穷举状态组合）

用户要求「按逻辑分析审查」。派 4 个只读子代理分别穷举：rhine 收敛状态机（输入 × 锁定 × 快照 × 时机 × 事件来源）、DOWN 停摆状态机（启动/运行 × 模式类型 × 前台/屏幕变化 × 事件叠加）、档位与 FAS 改名后的模式名数据流（产生/比较/匹配/透出全链路）、WebUI 时序与边界。这轮挖出的多数是**状态组合类**问题，单看 diff 很难发现。

### 修掉的

1. **解析口径回归（我上轮引入）**：上轮把 `normalize` 统一成「剥行内注释 + 逐边剥引号」，但 daemon 实际是**两条规则**——模式名走 serde_yaml（成对引号才算标量，`"vector'` 解析失败=Bad），保留字 `off` 走 `is_force_off` 字面量比较（trim_matches 逐边剥）。统一规则导致 `"vector'` 两侧相反（界面=启用、daemon=非法重置）。已拆开：`isForceOff`（剥注释+逐边剥引号）与 `yamlScalar`（剥注释+成对引号剥一层，不成对→null）；**daemon 侧 `is_force_off` 补剥行内注释**（`off # 注释` 两侧都认）。测试改为分别断言两条规则。
2. **痕迹原因混淆（我上轮补痕迹的副作用）**：运行时写坏文件 → `watch_loop` 先 `read_state` 重置 → `converge` 再读只看到空文件 → 锁定态记 `close-rejected`，与刚记的 `state-reset` 互相矛盾。修：`converge` 拆出 `converge_from(root, meta_path, state, fix)`（用调用方已读好的状态），`just_reset` 时不再记 close-rejected（写回文件仍做）。
3. **panic 自愈漏 vector**：重建分支只挡 fas/特调，vector 落入 `get_clg_cfg`（无 vector 段 → enabled=false）→ 频率零接管、无告警。补 `vector → fast_lock.init()`。
4. **DOWN 退出三处**：① `down_resume_mode` 改为进入停摆时用**当时的实际模式**覆盖（原启动快照可能已被 app_modes/特调/FAS 改掉）；② 退出时 `last_load_event = Instant::now()`（否则看门狗秒释放刚重建的接管）；③ panic 自愈重建补 vector（同 3）。
5. **锁定期间换模式旧影响残留**：`reassert` 扩展 `prev` 参数——旧模式改过、新模式不涉及的项按**快照回落**（meta 三开关合并进同一次写盘；`global_mode`/`special_tuned` 运行时覆盖交回），消除「文件写新模式、实际还挂旧模式」的静默分叉。顺带提取 `read_backup()` 供 restore 复用。
6. **restore 半套用**：meta 写失败时不再清运行时覆盖（同一套改动要么全清要么都不清），且失败时返回 `None`（不谎报「已还原」）。
7. **WebUI 时序**：`loadOverview`/`loadLab` 从「in-flight 直接 return」改为**共享在飞 promise**（否则冷启动落 `#/config` 时后来者拿到未加载完的空状态，接管置灰就是这失效的）；`forceDisableLab` 的等待改为**轮询**（每 400ms 直接读文件，绕过 labJob 守卫，直到不再是 force-off 或超时 4s）；`labTakeover` 在文件读不出模式时用**锁标记里的模式**兜底（那一刻 daemon 正按标记 reassert，置灰不能松）。
8. **AGENTS.md 过时描述**：「`Config.fast` 字段保留、`get_mode("fast")` 仍返回 Some」已不符（上轮我改成了 vector）→ 更新为 vector 档口径，并列出 `fast_lock.init()` 的**六个入口**（启动/亮屏/ModeChange/ConfigReload/DOWN 退出/panic 自愈）。

### 报告但未修（触发条件窄或需设计决策）

- rhine：`on_startup` → `DirWatcher::new` 之间窗口内的写入会丢（毫秒级）；meta 被用户改成非法 → config_watcher 用内嵌默认整体重置后，锁定态不会自动 reassert（分叉要等下次改文件/重启才被发现）；meta 有 daemon 两个写者共用固定 tmp 名（撕裂，需两线程同时写）。
- down：corectl 失败核的 2s 重试被 `!halted` 切断（停摆期不重试反而符合「交回系统」）；panic 退避窗口内 watcher 已置位仍可能短暂接管；BOM（U+FEFF）JS `trim` 吞、Rust 不吞（两侧判停摆分歧，文件带 BOM 才触发）。
- monitor：`dynamic_enabled=false` 直返 global_mode 绕过 fas/akmode 门控；看门狗释放后 vector/特调无周期重建（fas 有 1s 巡检）。
- WebUI：`state.svelte.ts` 零单测（settleMs/守卫/草稿清理）；export 轮询 4 分钟不可取消。

验证：`cargo check` 通过（touch 强制重编）；vitest **75 例全过**；svelte-check **0 error 0 warning**。

## 分析：与 system_server / ThermalManagerService 的关联与死锁可能性

结论：**无关联、无死锁可能**。证据：① 全仓库无 binder/dumpsys/service call；`system_server` 只出现在 affinity 黑名单与 app_detect 过滤（主动规避，注释明说线程迁移系统进程会卡死）；② thermal 只读 `thermal_zone*/temp` 与 `battery/temp`，不写 cooling_device/cur_state → 不与 ThermalManagerService 抢 cooling；③ uprobe 用 `UProbeScope::OneProcess` 只挂前台 app 的 `Surface::queueBuffer`（fps_monitor.rs:159）；④ 无循环等待路径：`shared_config` 读锁内的 sysfs IO 无重入锁，governor 单线程独占，`down::is_down()` 原子量。

真正的风险是**竞争**非死锁：`write_cpuset_cpus`（触发内核 rebuild_sched_domains + 2s 保留核纠偏）与 system_server 的 cgroup 操作并发 → 偶发短卡顿；`shared_config.read()` 持锁内大量 sysfs IO → 内核侧慢时热重载延迟。

## 模式体系重构（plan：mode-system-refactor，已执行）

需求：重申模式特性 + 三家族分组（CLG/stardust/rhine）+ FAS 单实例（15s 延迟）+ contingency/babel 落地 + customize.sh 取消热更新自动启动。四项确认：stardust 仅分组概念、GPU 自动探测、FAS 删多实例 15s、babel 给启用入口；附加：新行为配置归嵌入配置不新增对外文件。

### 落地

1. **governor/gpu 层**（`src/chiri/governor.rs` / `gpu.rs` 新增，与 FastLock 同构、调度线程独占）：GovernorGuard 快照各 policy scaling_governor → performance、release 逐 policy 恢复、`cleanup_residue()` 启动清 SIGKILL 残留（performance 非任何厂商默认）；GpuGuard 启动探测 devfreq（kgsl-3d0 / gpu/kgsl/mali，available_frequencies 取硬件上限），lock 快照 min/max 写 hw_max。**不用 try_write_file**（吞错恒 Ok + chmod 0444 副作用），用 fs::write 判成败。
2. **FAS 单实例**：fas_manager 重写（instance: Option + exit_deadline + exit_delay + governor 内嵌）；`deactivate_delay_secs` 进 FasRulesConfig（default 15，normalize 夹 1..=600，fas-example.yaml 说明）；`request_delayed_exit`/`tick`；has_any_instance 语义 = 活跃；reap/TTL 删。**ModeChange 的 fas→非fas 拦截在 mode_clone 更新之前**（pending_mode_after_fas 记录目标模式），1s tick 超时后按它接管（lab 模式走 apply lab 分支、其余 apply_mode_takeover）。
3. **contingency/babel**：`apply_affinity_and_corectl` 的 lab 分支 → `AffinityManager::lab_static_apply/deactivate`（停迁移 + 组级 cpus：contingency 后台压 0-1、前台/顶部全核；babel 后台小核/前台顶部大核+超大核/系统进程大核；快照内部持有、周期重入即纠偏）+ `sync_lab_governor_gpu`（启动/ModeChange/DOWN 进出/tick/panic 收尾与重建，幂等）。corectl force_online_all + 回 NONE。
4. **stardust 语义**：apply 的 scenemode 分支 release + core_ctl 回 NONE——仅压频、全核、停迁移；`core_ctl.scenemode_offline` 字段停用（保留兼容，注释标注）。
5. **rhine-init**：contingency/babel 影响项（global_mode=自身 + fas/scenemode/special_tuned=false）；WebUI：ModeKind 加 'lab'、LAB_CATALOG（vector/contingency/babel）、LAB_ENABLEABLE 加 contingency/babel、LAB_TAKEOVER 三模式同集、locale 补 mode.contingency/babel（zh/en）与 lab.mode detail 更新。
6. **customize.sh**：删热更新后 setsid 重启 service.sh + 轮询验证段，统一打印 MSG_HOT_UPDATE_ABORT（手动 Action）。
7. 文档：AGENTS 模式家族段/FAS 小节重写 + 新增 lab 静态分组段；feature.yaml 三档注释按语义重申（参数核对后无需调数值）；memory 同步。README 未动。

### 验证

cargo check ✓（touch 强制重编）；vitest 76 例全过（lab/data-model 断言随新语义更新）；svelte-check 0 error 0 warning。

## 生产就绪审查（子代理终审 + 修复）

自查先发现一个关键顺序 bug：**ModeChange 的 sync（governor→performance）放在 CLG release 之前**——CLG release 会恢复它 init 时快照的 schedutil，把 performance 覆盖掉。已修：sync 拆两半——「离开 lab 的 release」放接管逻辑前（后续 CLG init 才能快照到 schedutil）、「进入 lab 的 activate」放接管逻辑后；息屏同样生效（接管逻辑跳过但调速器切换不受屏幕状态影响）。

子代理终审（生产视角：崩溃/状态机闭环/sysfs 边界/退化场景/一致性/脚本）确认崩溃风险面干净（无 unwrap/索引/溢出热点；panic 有 catch_unwind+退避+收尾；sysfs 写失败全降级；非 ChiRi 走 Yumi 不进新代码；8998 prime 空区间有守卫），但揪出 1 P0 + 3 P1，已全部修复：

1. **P0 lab 分组退出后 affinity 快照污染**：ModeChange 离开 lab 只 release governor/GPU，没调 `lab_static_deactivate` → lab 分组值留在各组，后续 boost/fas 的 ensure_snapshot 把 lab 值当「系统原值」记录 → top-app 永久停留大核、重启前无自愈。修：离开分支加 `lab_static_deactivate()`（在 governor/GPU release 前，顺序：先恢复组 cpus）。
2. **P1 FAS 延迟期同包返回不续期**：同包回前台走 PackageSwitch 同包去重/1s 巡检同包 no-op，exit_deadline 不清 → 到期把正在前台的游戏拆掉重建。修：`renew_if_same_pkg()`（fas_manager）+ 1s 巡检同包续期 + PackageSwitch 同包即时续期（借用问题先 clone active_pkg）。
3. **P1 FAS 延迟期息屏到期零接管**：tick 消费在息屏时跳过接管且 doze 未初始化 → 整夜零调度。修：tick 超时分支三分（lab 不门控亮屏 / 亮屏 apply_mode_takeover / 息屏 CLG doze 低功耗配置，与 fas_enabled=false 息屏分支同款）。
4. **P1 GpuGuard 无残留防护**：SIGKILL 在 contingency 时 GPU min=max=hw_max 残留，下次 lock 把残留值快照成「原值」固化。修：probe 记录 hw_min（available_frequencies 最低值，Adreno 缺失时 devfreq min_freq 兜底），release 时 orig_min==hw_max 视为残留、恢复 hw_min。

P2 记录（可接受）：DOWN/panic 不清 pending_mode_after_fas（DOWN 已顺手清，panic 靠下个 ModeChange 自愈）；panic 收尾缺 governor/gpu/lab 清理（靠重建块幂等收敛）；kgsl-3d0 与 devfreq 扫描可能收双节点（重复写同值无害）。

终态：cargo check ✓ / vitest 76 ✓ / svelte-check 0 error 0 warning。**结论：P0/P1 修完后可上生产**；真机首验建议覆盖「FAS 游戏→桌面→15s 内回」「FAS→息屏锁屏过夜」「contingency 启用→重启→governor/GPU 残留清理」三条链路。

## 补充：调速器变更的「由 -> 到」info 日志

应要求把 GovernorGuard 的聚合计数打点（governor-activated/released）改为**逐 policy 的由 -> 到**：activate 时 `governor-switched = [Governor] P{ $pid } 调速器 { $from } -> performance`、release 时 `governor-restored = [Governor] P{ $pid } 调速器 performance -> { $to }`（zh/en 同步，原两 key 删除）。残留清理（performance -> schedutil）保持 warn 级（异常语义）。FastLock/CLG 的 governor 写入（schedutil）未加同款日志——它们有各自快照恢复，需要时再扩展。

## 修复：导出日志「命令失败(1)且无额外提示」

根因：`pollExport` 单次失败（KSU 桥瞬时 errno=1，stderr 常为空——shellError 就只格式化出「命令失败(1)」）会把整个导出**立刻判死**，而后台 tar 多半还在跑。三层修复：

1. **轮询失败重试**：state.svelte.ts 的轮询循环加 `pollFails` 计数，连续 <3 次失败先继续轮（后台 tar 不被一次桥抖动错杀），连续 3 次才判 failed。
2. **错误文案带上下文**：export.ts 的 start/poll failed 消息加前缀（「启动导出失败：」「查询导出进度失败：」），failed 判定时追加 `overview.export.failed.hint`（「后台打包可能仍在进行，稍后可到 /sdcard/Download 查看产物」，zh/en）。
3. **顺带修复 mode.ts 的 CLG_CATALOG 回退**：用户 WIP 的 descKey 注释状态与我的 LAB_CATALOG 拆分叠加后 svelte-check 报错（clg.descKey 不存在）→ 恢复三档 descKey（vector 已归 LAB_CATALOG）。

验证：cargo check ✓ / vitest 76 ✓ / svelte-check 0 error 0 warning。PowerShell 下 svelte-check 输出经 `2>&1` 会变成对象数组，筛选要用变量中转（`$o = & node ... 2>&1; $o | ...`），直接管道 Select-String 会漏。


## i18n：en.ts 按 zh WIP 全量对齐

用户 WIP 把 zh 文案大幅改短（莱茵实验室、请在开发者指导下操作、彩蛋 detail 等），en.ts 只 +1 行严重落后。按 zh 修正 en（zh 一律不动）：约 34 条——短文案对齐、overview.export.failed.hint 补「代理打包出现失误」语义、lab.warn.no-backup 按 zh 新语义（缺备份→关闭将用默认配置覆盖）、config.daemonLanguage 与 zh 同值小写、asEnabled.hint 去 U+2011 连字符。模式名：zh 中文彩蛋、en 英文彩蛋（Slippery floor! / The evil spirit of Babel），旧「中英同值」约定作废（已更新 MEMORY.md）。

**遗留未动（已报告）**：overview.stop.confirm.effects zh/en 都还写「fast 模式」（档位已全局改名 vector，两边一致但语义过期，涉及用户 WIP 范围不擅动）。验证：svelte-check 0/0，vitest 76 全过。

## 8550 日志分析 → 特调参数组 + video/light 场景组

用户给 8550 的 5h 整夜日志（devimpbin/8550/logd.tar.gz，7 组 × dev+devimp zip），目标「平均功耗降一半、流畅度几乎不变」，授权新建特调模式/参数组/包名/场景组。解压全在 8550 目录（logd/ + ext/ 分组），分析脚本 analyze{1,2,3}.mjs 留同目录可复跑。

**数据结论**：亮屏 1.35W（bili 视频 61% 时间 1.27W）；prime 在视频/桌面场景大面积空转（88% 停 960M，占用中位 0.12）；CLG 无系统性低载高频浪费（0-4%）；息屏已极低。**「降一半」调度层不可达**（需屏幕/刷新率/解码器），可省的是大核空转+交互过量频率（估 -20~30%）。列坑详见 MEMORY.md。

**实施**：① 代码——Config 加 params HashMap + get_tuned_cfg（5 个调用点全改）+ SpecialTunedConfig 加 boost_affinity（省电特调不走 boost 亲和：不 cpuset 收窄、不 core_ctl 保大核，is_boost_mode/uclamp_override 两处过滤）；② 配置——normal/akmode.yaml 加 params（video: headroom 1.05/hold 300；light: 1.20/floor 0.10/hold 100，均 boost_affinity false）、special_tuned.yaml 加 video 组（bili+正则）与 light 组（18 精确 + launcher 正则）；③ 文档 AGENTS.md 三段更新；④ example 同步。**全局共享文件 → 所有 SoC 自动生效**。cargo check 通过（强制重编确认）；yaml 缩进/字符校验通过。

**待设备验证**：参数保守未实测，建议下一轮日志对比 bili/launcher 功耗与掉帧（devimp 的 max_util 与 tick 决策可对照）。回滚 = 删白名单条目。

## 8550 日志第二轮深挖（analyze4-7）：列语义反转 + 算法缺陷 + 参数定稿

**重大修正**：devimp tick 的 cur_freq_khz 是 CLG 的**动态上限**不是实际频率（akmode.rs L381 注释），上轮「prime 空转 960M」等结论全部重解读——CLG 压上限其实很积极（prime 上限/(mu×hw)=1.8-2.5）。实际频率数据里没有。

**离线回放（忠实复刻 akmode 算法，用日志 mu 序列）**：① 发现真实算法缺陷——down_target「档位不等即重置」在抖动负载下让上限卡高位降不下来（平滑 util 都降不动）→ 已修（只在目标明显回升时重置）；② 无平滑的 akmode 上限均值反而高于 CLG（1.1-1.4×）——「升频立即执行」被尖峰反复推高 → 加 util_smoothing（EMA，默认 1.0 兼容游戏）；③ 扫参定稿：video/light 用 hr 1.0 + hold 100 + α0.35/0.5 → 上限压到 CLG 的 0.90-0.96（省 4-10%）；hold 300 明显差；prime 压不动（≥0.99）。

**代码**：akmode.rs（降频计时修复 + ema_util）、config.rs（util_smoothing 字段 + normalize + Default）、akmode.yaml/example（参数定稿）、AGENTS.md。cargo check 通过、yaml 校验通过。

**诚实结论**：调度层剩余空间就是个位数 % 亮屏功耗；「降一半」在屏幕/刷新率/解码器层——数据（息屏 0.07W vs 视频 1.27W）与常识（屏幕是最大单模块）都支持这个边界。

## 命名规范化（用户要求：调度内标识符不得重名、特调命名要贴切）

用户的观察：light 与亮色主题/light use 等语义撞车；特调组与配置命名不贴切（akmode.yaml 里装的已是通用特调参数组）。已全量改名并做残留扫描（0 命中，仅 AGENTS.md 保留 {soc}/akmode.yaml 历史陈述）：

- 模式名：video→playback、light→daily（候选名先全仓库搜过 0 命中）
- 配置文件：normal/akmode.yaml→normal/tuned_profiles.yaml（git mv 保留历史）、akmode-example.yaml→tuned_profiles-example.yaml
- Config：params→tuned_profiles（字段+yaml 段）、get_tuned_cfg→get_tuned_profile（并删掉冗余的 get_akmode，取参唯一入口）
- 函数/标志：merge_akmode→merge_tuned_profiles、embedded_akmode_str→embedded_tuned_profiles_str、is/set_akmode_available→is/set_special_tuned_available、AKMODE_AVAILABLE→SPECIAL_TUNED_AVAILABLE
- 引用点：build.rs 断言、xtask 移除列表、app_detect.rs 两处判定、AGENTS.md 七处、webui 注释、记忆

**未改（既有专名，改动波及日志文案）**：AkmodeGovernor / src/chiri/akmode.rs / i18n 的 akmode-* 键 / 白名单里的 akmode 模式名——如需一并改需用户确认。验证：cargo check 通过（强制重编）、svelte-check 0/0、旧名残留扫描 0 命中。

## 二次审查轮：日志/命名/错配三类缺陷修复

**发现的真实缺陷（已修）**：① **日志文案误称「明日方舟特调」**——playback/daily 接管也打同样日志且不带模式名，排查会误导 → ftl 键 akmode-* → tuned-*、文案去「明日方舟」、日志带 { \ }（init/activated/no-clusters/cluster-skipped/deactivated/config-reloaded/tick-log）；② **执行器名不副实**（管所有特调却叫 AkmodeGovernor）→ TunedGovernor + src/chiri/tuned.rs + init_policies(mode, cfg) + 冷却变量/常量改 tuned_*；③ **白名单与参数组静默错配**——模式名拼错/漏配会静默用游戏参数（hr 1.15 + boost 亲和，省电反效果）→ common::special_tuned_mode_names() + merge 时校验 + tuned-profile-missing 告警；④ devimp util 列语义已变（写决策值）却在注释里没标 → 补注（否则离线回放会二次平滑）；⑤ EMA 时间常数实机 40ms(≈75ms) vs 回放 160ms(≈300ms) 的偏差未标 → 补注（实机收紧幅度会小于回放估计）；⑥ 清掉未使用 import。

**确认无误的审查结论**：boost_affinity=false 对 playback/daily 是**保持现状亲和语义**（这些场景日志里本来跑 CLG default = 非 boost 亲和），不是引入新风险；akmode 段作缺省回退 + 保留 akmode 模式名（配置 key 不可动）；降频计时修复的边界安全（每 tick 重新判断进入条件）。

**报告未改**：AGENTS.md L264 的升降阈值描述是旧四档方案遗留（与无档位实现矛盾），仅同步了其中的类型名，描述留给用户定。

验证：cargo check 通过；ftl 键 src 使用 10 个 = zh/en 各定义 10 个（对称无缺）。

（补记）总结特调体系时发现 tuned_profiles.yaml 缺省段注释残留旧段名 params，已修正为 tuned_profiles。

（补记）新建 mdocs/ModeInfo.md：模式总览文档——三套体系（CLG 档位 / 特调 akmode+playback+daily / 实验室 vector+contingency+babel+frozen）+ fas/down 特殊值 + 选择优先级（FAS 白名单 > 特调白名单 > app_modes > global_mode）+ 参数组字段表。风格对齐 socList.md（中英标题、简洁直接）。

## 生产可用性检查（对今日代码逻辑）

逐项复核（无代码改动，全部通过）：
- init_policies 顺序正确：release() 用旧 mode 打 deactivated 日志 → 再赋新 mode，日志归属不会错
- 降频计时修复自洽：目标回升未超 hyst 时 down_target 跟涨属保守方向（降幅变小而非误降），每 tick 重新判断进入条件，写失败保留计时重试；saturating_add 防溢出
- EMA 边界：未初始化（-1.0）首 tick 用原始值无冷启动滞后；热重载 1.0↔0.35 互切无跳变；clamp 0.05-1.0 防呆
- merge 校验：仅 Config::load 时执行一次（热重载频率低），重复告警可接受；akmode 例外正确
- 今天新增代码零 unwrap/expect/panic（tuned.rs 是 unwrap_or(&0)；扫描命中的 12 处 unwrap 全部为既有代码）
- 性能：tuned_boost_affinity 只在 2s 周期/事件路径（clone 6 字段结构），get_tuned_profile 只在模式切换路径，均可忽略
- 配置容错：段缺失→ Default/空 map + 告警；非法值→ normalize 钳制；回滚=删白名单条目

编译：cargo check 通过（无新警告）。
