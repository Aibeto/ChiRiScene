# 2026-09-26 工作日志

## 屏幕检测切换为 debug.tracing.screen_state（用户指令）

- sysfs 节点投票检测（enumerate/tally/uevent 屏幕分支）整体 [PAUSED]：块注释在 `src/monitor/screen_detect.rs`，恢复标记 grep `PAUSED`。踩坑：Rust 块注释嵌套，注释里的 sysfs 通配路径（`fb*/blank`、`leds/*backlight`）含 `/*`、`*/` 会破坏配对，已把路径改写成不含 `/*` 的形式。
- 现役判定：属性轮询线程 500ms（`monitor_screen_state_property`）+ verify 自愈；息屏值 = `screen_off_value`（meta 新字段，缺省 1，四份模板已加），`common::SCREEN_OFF_VALUE` 原子量由 `Config::load` 同步。
- uevent 线程保留只服务 CPU hotplug（`CPU_HOTPLUG_DIRTY`），函数参数带下划线待还原。
- customize.sh [screen-detect]：不保留配置路径下安装期 `getprop debug.tracing.screen_state` 实测为 1 → sed 把模板 `screen_off_value: 1` 翻成 0。
- WebUI：meta.ts 字段/校验、ConfigView 高级设置 ToggleField（1=息屏开/0=关）、state 层 setScreenOffValue、双语 i18n、contract.test.ts 用例。
- 验证：cargo check（aarch64-linux-android）0 警告；vitest 86 全过；svelte-check 0 错；vite build 成功（视觉/交互未实测）。
- 工作区另有用户未提交改动（core_ctl.rs、ftl、agentsdocs/01 等），未触碰；曾出现 core_ctl.rs E0382 编译错，后一次构建已消失（疑似用户编辑中途状态）。

## 追加：status.csv 新列 + 判定口径收紧（同日第二条指令）

- status.csv 末尾新增 `screen_prop` 列（22→23 列）：`debug.tracing.screen_state` 原始值，`read_screen_prop_raw()` 由 chiri 1s 采样轮询读取（无事件可听），缺失写 "-"；WebUI `data/status-csv.ts`、mock、data-parse.test.ts 同步。
- 判定口径（用户定）：当前值 == 息屏判定值 → 息屏，**其余一切值（含缺失/异常）一律亮屏**；每次读数 trace 日志（现值+判定）；属性缺失 warn 一条（去重、读到值复位）。
- 验证：cargo check 0 警告、vitest 86 全过（补了 data-parse 第二处硬编码 22）、svelte-check 0 错。
