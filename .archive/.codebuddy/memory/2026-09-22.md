# 2026-09-22

## gitignore 检查与修复（轮末小任务）

- `module/.gitignore`：`core/bin/yumi` → `core/bin/chiri`（产物改名后旧条目失效，chiri 构建产物曾会变 untracked）。
- 根 `.gitignore`：补 `node_modules/`（根 package.json 存在后防误装生成）与 `.codebuddy/teams/`（CodeBuddy 团队数据不入库）。
- 剩余 untracked 均为**应入库**文件，勿当遗漏：`webui/scripts/`（fetch-fonts.mjs，webui/.gitignore 注释引用它）、两个字体 `LICENSE-OFL-*.txt`。
- 根 `package.json` 疑似 npm init 误生成占位（description 指 README、默认 test 脚本），已提示用户可删。
- **复核（用户指出查得不细）**：根 package.json 三证确认是 `npm init -y` 残留——description 恰为 README.md 首行、
  `git log --all` 从未跟踪、全仓库无引用（仅 mdocs 提 webui 的）；建议删除，未动手。
- fetch-fonts.mjs 产物排查：`.subset-charset` 已被 webui/.gitignore 45 行覆盖（用户补的）；
  但 `save()` 中断残留的 `*.woff2.tmp` 不被 woff2 规则匹配 → 已补 `/src/assets/fonts/*.tmp`。
  **教训：gitignore 检查要顺着 package.json 新增脚本的产物链走，不能只看现有 untracked。**
- **合并为根单文件（用户要求）**：webui/.gitignore 与 module/.gitignore 已删除，规则全并入根 `.gitignore`
  （`webui/`、`module/` 前缀；`webui/.vscode/*`+`!extensions.json` 负模式保留）。用户已自行把根 package.json
  加进忽略 → 顺手锚定 `/package.json` 并补 `/package-lock.json`。删除时 EBUSY（IDE 占用），Remove-Item 成功。
  `check-ignore` 验证 8 关键路径命中一致、mono 字体/extensions.json 未误伤。子 gitignore 是 Vite 脚手架自带 +
  Magisk 产物本地约定，无文档引用，安全合并。

## 二十四轮：8550 + 8745 双机型日志分析（性能/流畅度/功耗平衡）

产物 `.codebuddy/docs/2026-09-22-8550-8745-power-balance.md`（只分析未改码）。

**数据筛选（关键，别混着用）**：

- 8550：72 个 devimp 文件**全是 A04-Canary88、全是 40 列** → devimp 48 列（CPU/GPU 实际频率）**这次没刷入**，
  本次只能看调度器写入的决策值。
- 8745：33 个文件**三个版本混着**（A02×2 / A03×10 / A04×21）。**A03、A02 的功耗列是坏的**：
  `batt_i` 出现 −369 / 656 A、`batt_p` 达 245 万 W（单位/口径错误）→ 这两版本的功耗不可用，
  但频率/util/tgtop 仍可参考。A04 正常（与 8550 同版本）。
- **充电过滤**：devimp **没有 charge 列**，只能靠 `batt_i < 0` 判充电。8550 多场景充电样本占大头
  （QQ 1657/2175、trim 562/1213、bili 625/1885）→ 不滤会把 39W 快充算进功耗
  （QQ 6.85W→3.03W、trim 14.31W→1.85W）。status.csv 有 charge 列但样本很少（8550 仅 35 行放电）。

**9-20 视频调参效果已验证（同口径滤充电）**：batt_p **2.83 → 2.08 W（−26.5%）**、p50 2.59→1.77、
batt_temp −2.0℃、cpu_temp −5.9℃、**不再触发 70% 限频**（cap 87→100）、
little cap p95 100→66.7、prime p95 83.8→46.1，而 **util 三簇几乎不变**（0.63/0.50/0.27 → 0.60/0.49/0.26）。
→ 省的是「频率高于负载所需」的部分；「压天花板 + 收紧 headroom + 滤尖峰」这条路可推广。

**剩余空间（按量级）**：

1. **日常 default（CLG）头号**：util 仅 0.5 左右，但 cap p95 **全是 100%**（launcher/tongyi/emmx/QQ 皆然）
   → 根因是 `default.cpu_load_governor.perf_ceil = 1.0`（无天花板）。功耗 launcher 2.08W、tongyi 2.28W、QQ 3.03W。
2. **8745 游戏（明日方舟 akmode）7.06W**：big cap p50 **100%**、util 0.72、gpu 74.1%；
   akmode 是 headroom 1.15 + util_smoothing 1.0 + **无 perf_ceil** → 尖峰直接顶满。
3. **8550 游戏（sgame fas）3.94W 且 cap 70%**：batt_temp 40.7 顶到 soft 41 → 陡峭 70% 限频（掉帧风险）。
4. **8745 待机 0.38W 已极低**，但 little util 0.71（cap 仅 22%）→ tgtop 显示 **logd 占 17.44%**（待机头号）。

**采集态开销警示**：logd 待机 17.44%、launcher 15.82%、游戏 9%；8745 待机 5h 产出 **16.7MB** devimp（≈0.9KB/s）。
日志里的功耗/负载**包含采集自身开销**——评估任何改动必须前后都在同一 dev_record 状态下比。

**方案**：P1 日常 CLG `perf_ceil 1.0→0.80` + `touch_boost_ms 350→400`（稳态降频、交互靠 boost 兜，别只降 ceil）；
P2 CLG 补 per_cluster（tuned 9-21 已有，CLG 只有全局一份）；P3 akmode 加 `perf_ceil 0.95` +
`util_smoothing 0.85`；P4 8550 游戏把陡峭 70% 改平缓（soft_cap 0.70→0.85，别动安全阈值本身）；
P5 采集开销：tgtop 30s→60s 可配（snap 1s 保留）；P6 devimp 补 `charge` 列 + 刷入 48 列版本。

fps 列全为 `-`（FAS 未采样），流畅度只能靠 cap% 与主观间接判断——后续若要量化需先让 FAS 采样可用。

## 二十五轮：实施 P1-P4（用户确认「都做，可分 SoC 调，全局受益就改全局」）

**分 SoC vs 全局的落地决策（重要，以后照此办）**：

- **P1 全局**：`default.cpu_load_governor.perf_ceil 1.0 → 0.80`，改\*\*根 `module/config/feature.yaml`
  - 三个 SoC 覆盖文件（8550/8475/8998）**——SoC 文件是覆盖层，只改根不生效。**8745 没有专属目录\*\*，
    自动继承根配置 ✓（这是「分 SoC」与「全局」的关键：无目录即走全局）。
- **P2 代码全局 + 配置只给 8550**：CLG 新增 `per_cluster` 覆盖能力（config.rs：`ClgClusterOverride`
  只覆盖 up/down 阈值、headroom、floor、ceil 五项 + `effective()` 返回覆盖后的配置副本）；
  配置只在 8550 写 `little 0.70 / prime 0.65`（big 不覆盖继承 0.80），8475/8998 **不写**（无数据不拍脑袋）。
  cpu_load_governor.rs 新增 `cluster_name_for_policy()`（policy 名 = 该簇首个 CPU id + `chiri_core_ranges()`，
  **不额外读 sysfs**），两处 `spawn` 改用 `self.cfg.effective(...)`，**Worker 内部零改动**。
- **P3 全局**：`normal/tuned_profiles.yaml` 的 akmode 加 `perf_ceil 0.95` + `util_smoothing 0.85`。
- **P4 仅 8550**：`Thermal.soft_perf_cap 0.70 → 0.85`（只有 8550 撞到断崖限频；安全阈值 batt_soft/hard 未动）。

**没动 touch_boost_ms**：8550 日常档本来就是 400ms（正是建议值）。**配套关系要记住**：ceil 压下去后
交互响应全靠 touch_boost，觉得不跟手应调 boost 而不是把 ceil 抬回去。

等价性：不写 per_cluster 时 `effective()` 返回自身克隆，逐位等同改动前。cargo check（android）通过。
数据订正：报告中 `com.trim.app` 保留包名原样（用户指出 8550 没装剪映，不做应用名猜测）。

改动文件：src/chiri/config.rs、src/chiri/cpu_load_governor.rs、module/config/feature.yaml、
module/config/{8550,8475,8998}/feature.yaml、module/config/normal/tuned_profiles.yaml。
报告 `.codebuddy/docs/2026-09-22-8550-8745-power-balance.md` 追加 `[done]` 节。

## 二十六轮：Canary90 首批日志验证 —— P1/P2/P4 生效，但功耗涨 47%（采集的锅）

产物 `.codebuddy/docs/2026-09-22-canary90-verify.md`。样本 `devimpbin/8550/logd_0922-020050.tar.gz`
（0922-00:52~02:00，52 文件、**48 列**、Alpha04-**Canary90**；上一批是 40 列 Canary88）。

**参数生效（好消息）**：日常 default 的 cap p95 从 100/100/100 → **72.4 / 82.9 / 69.9**，
与配置 little 0.70 / big 0.80 / prime 0.65 精确对应 → **per_cluster 确实按簇生效**。
游戏 fas 的 cap 从 70 → **85**（P4 的 soft_perf_cap 0.85 ✓），该场景 governor=performance 且 cur=min=max（FAS 锁频）。

**功耗全面上升（坏消息）**：

- 同活跃度（亮屏+放电，按 gpu 档）：gpu<10 档 1.86→**3.17W（+70%）**、cpuT 47.5→58.7℃。
- 同场景 B站 playback（gpu<12）：9-20 基线 2.39W → 00:14(C88) **1.76W** → 02:00(C90) **2.59W（+47%）**。
- **判据**：C90 与 C88 的 playback 频率上限完全一致（little/big p95 = 66.7/75.9），即该场景调参没动过，
  功耗却涨 47%，且 util（0.60→0.66）、psi（18.6→23.2）、cpuT（+6.9℃）同步涨 → **涨的是 C90 新加的东西**。

**根因**：C90 新增的 devimp 48 列采集里，**每秒读 GPU 频率节点 = 每秒把 GPU 从低功耗唤醒一次**
（读 Adreno gpuclk / devfreq cur_freq 需要 GPU 供电应答；表现为 gpu_busy 仅 5.2% 但功耗温度都涨）。
CPU 的 cpufreq 节点读取则廉价、无此副作用。devimp 既有设计里 place/core 8s、tgtop 30s，只有 snap 是 1s，
把「读 GPU 硬件状态」挂在 1s 上是这轮的设计失误。

**修复（已做，cargo check 通过）**：

1. `src/chiri/mod.rs` 新增 `gpu_snapshot_due()`：**GPU 快照节流到 10s**（未到点 4 列写 `-`），CPU 仍每秒采。
2. `src/chiri/gpu.rs` 修 48 列 GPU 数据的三个 bug：**单位**（cur_freq/gpuclk 是 Hz → 换算 kHz，
   原来写出 110352386 这种 110GHz 值）、**目录去重**（kgsl-3d0 同时存在于 devfreq 与 kgsl 两个路径，
   混读导致 min 680MHz 配 cur 110MHz，改为按设备名去重、devfreq 优先）、
   **governor**（只在标准 devfreq 读，Adreno 私有目录同名节点是频率值；纯数字判无效写 `-`）。

**待用户确认**：Canary90 除了这批改动是否还有别的（若有也要纳入嫌疑）；
刷修复版后复测 playback 是否回落到 ~1.76W（回落即确认是 GPU 采集的锅，P1~P4 收益可保留；不回落则二分排查）。

## 二十七轮：用户拍板「GPU 快照功能暂时关闭」

按用户要求止血：新增 `src/chiri/mod.rs` 的 `GPU_SNAPSHOT_ENABLED = false` 总开关，**GPU 那 4 列恒写 `-`**；
`devfreq_snapshot()` 保留实现并加 `#[allow(dead_code)]`，开关之下仍保留 `gpu_snapshot_due()` 的 10s 节流
（将来改回 true 即恢复「10s 一次」而非每秒）。CPU 那 4 列不受影响、照常每秒采。

踩到的小坑：把 `///` doc 注释写在函数内的 **let 闭包变量**上会触发 `unused doc comment` 警告 → 改用 `//`。
cargo check 干净（仅剩 nix 依赖的 future-incompat 提示，与本项目无关）。

文档同步：`mdocs/TechAnalyze.md` §9.4 说明 GPU 4 列默认关闭的原因与恢复方式；
`2026-09-22-canary90-verify.md` 的 [fix] 节改为「默认关闭（先止血）」。
**下一步**：刷入后复测 playback 是否回落到 ~1.76W，以确认功耗上涨确实只由 GPU 采集引起。

## 二十八轮：审查 DOWN 模式（调度停摆）是否实际可用

**机制**（`src/down.rs`）：模块根 `down.chr` 写保留字 `down` → 停摆；与 rhine.chr 同款、落盘持久化（重启仍停摆）。
`on_startup` 读一次 → `DOWN_ACTIVE` 原子量；`watch_loop` 用 inotify（CLOSE_WRITE|MOVED_TO|DELETE）盯文件。

**代码审查结论：实现完整**（`chiri/mod.rs` 1046-1101 状态机）：

- 进入停摆 release 清单：CLG、ak_governor(tuned)、fast_lock、FAS、affinity、lab_static、
  governor_guard、gpu_guard、core_ctl、`CpuScheduler::restore_system_tweaks()`（一次性调整还原），
  顺序是「先交回系统再写状态文件」。
- 退出停摆按 **monitor 实时模式**重建（非停摆前快照），并显式重建 fas/特调（事件不会补发）。
- 启动即停摆：`on_startup` 前置 + `down-boot-halted` 日志（否则与「调度线程没起来」同形）。
- 停摆期事件照收但不下发，仅吸收 `ScreenStateChange`；60s 一条心跳（区分「停摆生效」与「进程死了」）。
- `module/down.chr` 模板已随模块下发，写法清楚。

**实测证据：无**。三批日志（8550 00:14 / 8550b 02:00 / 8745）的 daemon.log **无任何 down 字样**，
devimp 的 mode 列只有 default/fas/playback → **DOWN 从未被触发过**，运行时可用性无证据。

**三条审查提醒（非 bug，是要知道的取舍）**：

1. **停摆期 eBPF 探针仍挂载**（采集照常）：`sched_switch` 每次上下文切换都跑 → 基线不是「完全干净的系统」。
2. **停摆期 devimp/status.csv/daemon.log 照常写** → 结合上次「logd 待机占 17%」的教训，
   **停摆期采集到的功耗基线包含采集自身开销**，做 A/B 时要知道这个偏差（或停摆期同时关 devimp）。
3. 启动时 `GpuGuard::new()` 仍会给 GPU 节点 chmod 0664（只改权限不改值，此前已确认可接受）。

## 二十九轮：完成 frozen 模式（待春归 / 春风来）

以 vector 为模板、方向相反：**锁硬件最低频**（vector 锁最高）+ 停掉一切额外开销。
界面显示名「待春归」、描述「春风来」（en: Waiting for Spring / The spring breeze comes）。

**核心代码**：

- `src/chiri/fast.rs`：`init(lock_min: bool)`；`LockedPolicy` 加 `target`（hw_min 或 hw_max）；
  **写序是坑**——升频「先 max 后 min」、降频「先 min 后 max」，否则出现 min>max 被内核拒绝；
  tick 重写用 `target` 而非 `hw_max`。
- `src/chiri/mod.rs`：新增 `is_fast_lock(mode)`（vector|frozen），**六个入口统一**（启动 / 亮屏恢复 /
  ModeChange / ConfigReload / DOWN 退出 / panic 自愈，含 `apply_mode_takeover`），每处 `init(mode=="frozen")`。
- **停开销**：`logger.rs` 两道闸——`devimp_active()` 按 `DEVIMP_MODE=="frozen"` 返回 false、
  `status_log_snapshot()` 开头 `mode=="frozen"` 即 return（**保留 daemon.log 便于排错**）；
  线程亲和/绑核与 core_ctl 由 rhine 的 `thread_bind: false` 交还系统（`config.rs:1124-1125`
  把 `affinity.enabled`/`core_ctl.enabled` 与 meta.thread_bind 取与，自动生效，无需改代码）。
- `src/notify.rs`：`frozen` 归入 RHINE 家族。

**配置与前端**：`rhine-init.yaml` 的 frozen 从 `{}` 改为 global_mode/fas/scenemode/special_tuned/**thread_bind=false**；
`webui/src/data/lab.ts`（`LAB_ENABLEABLE` 加 frozen、`LAB_TAKEOVER.frozen` 加 `thread_bind`）、
`mode.ts`（LAB_CATALOG 加 frozen，signal 用中性 info——它不是危险档）、zh/en i18n、`lab.test.ts`
（原断言「frozen 保持预留 not.toContain」已改为「都给启用入口」）；
`module/rhine.chr`、`agentsdocs/03-chiri.md` 同步。

**验证**：`cargo check`（android）通过；`npx vitest run` **85 passed**。
**未停的开销**：eBPF 探针仍挂载（sched_switch 等），用户态不再落盘但内核侧观测开销还在——
若要「完全干净」的基线需另做探针摘除（即报告里的 K3）。

## 三十轮：审查 frozen 改动（发现 1 个真 bug + 修了 3 处）

**真 bug（必现）**：`mod.rs` 的 ConfigReload 分支里有一处 `current_mode == "vector"` 没纳入 frozen
（2597 行附近）。frozen 落到下面的 `else if current_mode != "fas"` 会执行 **`fast_lock.release()`** ——
配置热重载（改 meta/rules 即触发）时把刚锁上的最低频又放掉，frozen 静默失效。
已改 `is_fast_lock(&current_mode)`；同文件另一处同类分支（1247）一并统一。
**校验手段**：`git grep -n '== "vector"' src` 结果应为空（现已为空，只剩注释与配置段名）。

**另两处修正**：

1. `logger.rs::devimp_active()` 原来用 `DEVIMP_MODE.lock()` 判 frozen —— 该函数是每条 devimp 行
   写入前的闸门（特调下 25 行/s/组），高频路径不该加锁。改为 `DEVIMP_FROZEN: AtomicBool`，
   由低频的 `set_devimp_mode()` 维护（与项目「高频只读原子量」的口径一致）。
2. `mod.rs::apply_mode_takeover` 里 `mode == "vector" || mode == "frozen"` 字面量 → 改用 `is_fast_lock(mode)`。

**确认无问题**：`fast.rs` 的 target/写序（升频先 max 后 min、降频先 min 后 max）；
contingency 的 `init(false)` 正确（它锁最高频）；frozen 不进 `is_boost_mode`、走
`sync_lab_governor_gpu` 的 `_` 分支（不锁 GPU）；亲和与 core_ctl 由 `thread_bind=false` 关闭。
cargo check 通过。**遗留风险（未改，需知悉）**：锁最低频后设备极慢、解除靠改 rhine.chr + 重启
（建议 adb 操作）；status.csv 停写后 WebUI 实时数据不更新（预期）；eBPF 探针仍挂载（内核侧开销）。

## 三十一轮：PowerBase 模式（Stardust 家族）—— 决策落地 + 配置层与调频器完成，**待接线**

显示名「涛声往复就已足够」、描述「随我来吧对错是伪命题」（en 待补）。

**四个决策（用户拍板）**：

- A 目标功耗：**8550 = 2.5W、8745 = 3.5W**（分 SoC）；根 feature.yaml 默认 3.0。
- B 开关：`meta.yaml` 的 `powerbase_enabled`（默认 false），放 WebUI 高级设置区。
- C 亲和积极性减半 = **promote 阈值翻倍**（`PROMOTE_UTIL_PCT` 25→50、`LITTLE_PROMOTE_UTIL_PCT` 10→20）。
- D 开启后 `current_mode.chr` **就写 powerbase**（接管者是它，写 CLG 档名会自相矛盾）。

**已完成（cargo check 通过）**：

- `module/config/feature.yaml` 加 `powerbase` 段；`8550/feature.yaml` 覆 2.5；**新建 `8745/` 目录**
  （整份复制根 feature.yaml 再把 target 改 3.5，避免丢段）+ `8745/meta.yaml`（复制根 meta）。
- 四份 meta.yaml 加 `powerbase_enabled: false`。
- `config.rs`：`Meta.powerbase_enabled`；新增 `PowerBaseConfig`（target_power_w / up_headroom_below /
  down_scale / overload_cores_pct / overload_hold_ms / touch_break_ms）+ 默认值 + `normalize()`；
  `Config.powerbase` 字段；`Config::load` 里 `set_powerbase_enabled()`。
- `src/chiri/power_base.rs`（新建）：照 fast.rs 结构的功耗调频器——功耗 < 目标放宽升频；
  ≥ 目标守住不升，除非「满占用核心占比 ≥ 50% 且持续 3s」；降频恒激进（down_scale 砍半）；
  触摸窗口内允许突破；5s 重写防篡改；快照/释放。
- `common.rs`：进程级 `POWERBASE_ENABLED` 原子量 + set/get（高频路径只读）。
- `app_detect.rs`：`determine_mode` 拆出 `determine_mode_raw`，对外入口把 CLG 家族
  （reduce/default/boost）的结果替换成 `powerbase`（FAS/特调/DOWN/实验室不替换）。
- i18n（zh/en）三键、`chiri/mod.rs` 注册 `pub mod power_base`。

**待接线（下一轮）**：

1. `apply_mode_takeover` 加 powerbase 分支（需传 `&mut PowerBase`，调用点同步）→ CLG 释放 + PowerBase 接管
2. `SystemLoadUpdate` 事件里驱动 `power_base.on_load_update(core_utils, power_w, touch_active)`
3. `affinity.rs` 按 `powerbase_enabled()` 把 promote 阈值翻倍
4. WebUI：`mode.ts`（stardust 家族位 + `mode.powerbase`）、zh/en i18n 显示名与描述、
   `ConfigView` 高级设置 Toggle、`meta.ts` 的 META_FIELDS
5. `notify.rs` family 映射 powerbase → Stardust；文档（03-chiri.md / TechAnalyze）

**踩坑**：build.rs 断言 SoC 目录的 `meta.yaml` 与 `feature.yaml` **必须成对**——只建 8745/feature.yaml
会让构建直接失败（`unpaired: ["8745（meta.yaml=false feature.yaml=true）"]`）。
另一坑：往 `pub struct Config` 前插结构时，把 `#[derive]` 与 struct 拆开了（重复 derive + Config 丢 derive），
已把头部三行（注释/config_root/derive）移回 Config 之前。

## 三十二轮：审查 PowerBase 改动（修 2 处，1 条接线前禁令）

**验证通过**：

- `8745/feature.yaml` 与通用 feature.yaml **只差 `target_power_w` 3.0→3.5**（`git diff --no-index` 逐行核对），
  复制没有夹带任何行为差异。
- `power_base.rs` 逻辑与需求逐条对应：功耗<目标→`util*up_headroom_below` 放宽；≥目标且未过载→
  `min(util, perf)` 守住不升；过载（满占用核心 ≥50% 持续 3s）→放行；降频恒激进（`target<perf` 时
  无条件 `*=down_scale`，不看功耗）；`power_w=None`（非放电）不设闸门；触摸窗口 `capped=false` 允许突破；
  写序升/降分明；写失败不前移 perf（下 tick 重试）。

**修的两处**：

1. `Config::load` 漏了 `config.powerbase.normalize()`——thermal/affinity 都在加载处钳制，只 PowerBase 拖到
   `init()` 才钳，口径不统一；已补。
2. `power_base.rs::init()` **注释与实现不符**：原注释写「起手按 perf_init 给一个中低频」，代码里根本没写
   频率（perf 从 0 起、接管瞬间保持系统现状）。已把注释改成实情并说明为何不给初值。

**接线前禁令（重要）**：当前状态下打开 `powerbase_enabled` 会**导致无调频**——`determine_mode` 已把
current_mode 改成 `powerbase`，但 `apply_mode_takeover` 尚不认识它 → 走 else → `get_mode("powerbase")`
返回 None → `enabled=false` → **CLG 被释放、PowerBase 未接管**，频率交回系统（不崩，但 ChiRi 不再调频）。
**必须接线完成后才能开这个开关**（默认 false，安全）。

**接线时的坑（提前记）**：PowerBase 的 `touch_active` 需外部传入，而 CLG 的触摸状态 `AtomicTouchState`
是它的私有字段——不能直接复用，得给 PowerBase 另备一份共享触摸标志（或由调度循环持有），
否则「触摸时允许突破功率上限」这条会静默失效。

## 三十三轮：PowerBase 逻辑审查 —— 修掉一个「静默永久失效」的真 bug

1. **★ 致命：接管时不写频率，会导致首次降频永久写失败**。原实现 init 只把内部 `perf` 置 0、**不动硬件**，
   于是首个 tick 判「升频写序」（perf 0 < target）→ 先写 max（低位），而 min 还是接管前的值
   （可能是 800MHz）→ **max < min 被内核拒绝** → 写失败 → `perf` 不前移 → 下一 tick 重试同样失败 →
   **PowerBase 完全不生效，且一行错误都不会打**。
   修：init 时**立刻把 min=max 写为硬件最高频**并把 `perf` 同步为 1.0（写成功才置 1.0，失败保持 0.0
   走升频写序重试）。取最高频起手而不是最低：接管瞬间通常有负载，先给余量防掉帧，
   紧随的 tick 会按功耗规则迅速压下来。
2. **振荡缺陷：`target *= down_scale` 一步砍半**。频率骤降 → 下一 tick 因利用率反馈滞后把 util 顶回去
   → 升回 → 再砍，来回振荡且平均功耗更高。修：在 `down_scale` 之上加单次降幅上限
   `MAX_DOWN_STEP = 0.25`（降频仍即时、不看功耗，但不会一步到底）。
3. **写入死区 0.01 → 0.05**（`PERF_DEADBAND`）：抑制抖动，并把 sysfs 写入从「每 tick 可能有」降到
   「只在真正变化时」。
4. 顺带修编译错误：`max_writer/min_writer` 必须 `mut`（`write_value_force` 取 `&mut self`）。

`cargo check` 通过。**仍未接线**（开关保持默认 false；接线前开开关会「CLG 释放、PowerBase 未接管」）。

**PowerBase 剩余问题清单（用户问「还有什么问题」时梳理，按阻塞度排）**：

- 阻塞：① `apply_mode_takeover` 不认识 powerbase；② `on_load_update` 无人调用；③ 两个入参没来源——
  功耗要从 scheduler_ipc 的 `power_now` 取（并判断放电）、触摸状态 CLG 那份是私有的需另备共享标志。
- 「看着生效其实没有」的坑：④ WebUI `meta.ts` 不认识 `powerbase_enabled`（meta 是 `deny_unknown_fields`，
  未同步会拒键/丢字段）；⑤ `mode.ts` 不认识 `powerbase` → 状态页显示 unknown（而 current_mode 就会写它）；
  ⑥ `notify.rs` 家族映射缺 powerbase → 通知栏 UNKNOWN。
- 需用户定的逻辑风险：⑦ **热保护对 PowerBase 无效**（thermal 靠压 CLG 上限工作，需决定接线时是否把
  thermal_cap 乘进 PowerBase，否则高温不压制，有真实过热风险）；⑧ 息屏 scenemode / doze 与 PowerBase 的
  关系待确认（倾向：息屏仍由 scenemode 接管，它是独立息屏轴且优先级更高）；⑨ `rules.yaml` 手写
  `global_mode: powerbase` 时 `get_mode()` 返回 None → 落进「无调频」分支，建议校验或文档说明。
- 收尾：⑩ affinity promote 翻倍未落地；⑪ 文档未同步（03-chiri.md / TechAnalyze）；⑫ status.csv 的
  `clg_active` 在 PowerBase 接管时为 0（正确，但看不出 PowerBase 在跑；devimp 的 mode 列已能看出）。

## 三十四轮：用户两条澄清 → 改为「内部替换」并接线（apply_mode_takeover 已通）

**用户澄清**：① 热保护失效是**预期行为**（不把 thermal_cap 乘进 PowerBase，不做处理）；
② **PowerBase 只是替换 CLG 调度逻辑，所有外部接口全部不受影响**
（→ 推翻了上一轮「current_mode 写 powerbase」的口径：模式名保持 default/boost）。

**据此的两处调整**：

1. **回滚** `determine_mode` 的 powerbase 替换（恢复原函数，并在注释里写明：PowerBase 不在这里出现，
   current_mode / rules.yaml / WebUI / 通知等外部接口一律不变）。
   → 顺带消掉了上轮那条「开开关 = 无调频」的禁令：模式名不再是 powerbase，所以能走正常分支。
2. `apply_mode_takeover` 改为**内部替换实现**：`clg_cfg.enabled && powerbase_enabled()` 时
   `cpu_governor.release(); power_base.init(&config.powerbase);`；否则先 `power_base.release()`
   再走原 CLG 逻辑（防两个调频器同时写 scaling_max_freq 打架）。签名加 `&mut power_base`，
   唯一调用点（1539 附近）同步；调度循环里新增 `let mut power_base = PowerBase::new();`。
   `cargo check` 通过。

**剩余接线（务必记住：还有别的 CLG init 路径没分流）**：

- **启动块**（977 附近）、**ModeChange 直接分支**（2213 附近）、**ConfigReload**（1247）、
  **亮屏恢复**（2018）、**lab 分组重建**（2697）——这五处原样 init CLG，PowerBase 开启时不会自动替换；
  表现是「启动即开启开关时，要等到一次模式变化才切到 PowerBase」。
- 事件驱动 `power_base.on_load_update(core_utils, power_w, touch_active)` 未接（功耗取 scheduler_ipc 的
  `power_now` + 放电判断；触摸需另备共享标志——CLG 那份是私有）。
- WebUI：`meta.ts` 加 `powerbase_enabled`（否则 deny_unknown_fields 会拒键）、ConfigView 高级设置开关
  （显示名「涛声往复就已足够」、描述「随我来吧对错是伪命题」）。
- affinity promote 阈值翻倍、文档同步。

**结论：现在仍不建议打开开关**（启动路径未分流 + 事件未驱动 → 接管了也不会调频）。

## 三十五轮：PowerBase 接线完成（Rust 侧闭环，cargo check 通过）

四步接完：

1. **`apply_mode_takeover` 内部替换**：`clg_cfg.enabled && powerbase_enabled()` → `cpu_governor.release(); power_base.init()`；
   否则先 `power_base.release()` 再走 CLG（防两个调频器同写 scaling_max_freq）。签名加 `&mut power_base`，
   唯一调用点同步；调度循环新增 `let mut power_base = PowerBase::new();`。
2. **ModeChange 分支**（退出特调/极速后的 else）同样分流。
3. **事件驱动**：`SystemLoadUpdate` 的优先级链加一段（fas > 特调 > **PowerBase** > CLG）；
   功耗取 `telemetry().batt_power_w()` 但**仅当电流 > 0（放电）**，否则传 None = 不做功耗限制。
4. **兜底纠正块**（`fast_lock.tick()` 之后，每轮执行）：开关与「谁在接管」不一致时拉齐——
   覆盖启动块 / ConfigReload / 亮屏恢复 / lab 分组重建这四条不会自动替换的旁路；
   停摆期一律交还。取锁顺序 mode → config（mode 的 guard 随语句释放）。

**现在功能闭环**：手改 `meta.yaml` 的 `powerbase_enabled: true` 即可生效（开机后由兜底块接管）。
**但触摸突破恒 false**（TODO 已写在代码里：CLG 的 AtomicTouchState 是私有字段，需另备共享标志）。

**剩余收尾**：① WebUI `meta.ts` 加 `powerbase_enabled`（`deny_unknown_fields` 会拒未知键）

- ConfigView 高级设置开关（显示名「涛声往复就已足够」、描述「随我来吧对错是伪命题」）；
  ② affinity promote 阈值翻倍（`PROMOTE_UTIL_PCT` 25→50、`LITTLE_PROMOTE_UTIL_PCT` 10→20，按 `powerbase_enabled()`）；
  ③ 文档同步（03-chiri.md / TechAnalyze）；④ 触摸共享标志（补 TODO）。

## 三十六轮：PowerBase 全部完成（用户要求「一步到位」）

上列四项全部做完：

1. **affinity**：promote 阈值按 `powerbase_enabled()` 翻倍（`PROMOTE_UTIL_PCT*2` / `LITTLE_PROMOTE_UTIL_PCT*2`）
   ——「亲和积极性减半」的落地口径。
2. **WebUI**：`meta.ts` 三处加 `powerbase_enabled`（META_FIELDS / WRITABLE_FIELDS / 校验的布尔列表 +
   `validateFieldValue` 的布尔 case——漏一处就会被 `deny_unknown_fields` 或校验拒掉）；
   `ConfigView.svelte` 高级设置区新增 Toggle（照 DOWN 开关写法）；`state.svelte.ts` 加
   `powerbaseEnabled` getter + `powerbasePending/Error` 字段 + `setPowerbase()`（照 `setPowerAvg` 模式，
   直写 meta 后回读）；zh/en 各加 `config.powerbase`（显示名）与 `config.powerbase.hint`（描述）。
3. **文档**：`agentsdocs/03-chiri.md` 补 PowerBase 说明（只换实现的定位、两处接管点、affinity 翻倍、
   触摸 TODO、热保护失效是预期）。
4. **触摸共享标志**：**未做，保留 TODO**（需给 PowerBase 另备一份触摸窗口状态，接入点在触摸事件处）。

**验证**：`cargo check`（android target）通过；`npx vitest run` **85 passed**。

## 三十七轮：审查全部未提交代码的逻辑（29 文件 / 656 增）—— 修掉兜底块两条致命遗漏

**★ 兜底纠正块缺两条排除（会让 PowerBase 与别人抢频率 / 夜里更耗电）**：

1. **缺 `!is_fast_lock(&cur_mode)`**：vector / frozen 走 `fast_lock` 硬锁，而 **vector 段在 feature.yaml 里
   是存在且 `enabled` 的**（`get_mode("vector")` 返回 `Some` → `clg_cfg.enabled = true`）→ 兜底块会判
   「该 PowerBase 接管」→ **PowerBase 与 fast_lock 抢着写 min/max**。
2. **缺 `is_screen_on`**：息屏应交给 doze（CLG 压低上限的专用配置）。PowerBase 的目标功耗是**亮屏口径**
   （8550 2.5W），而息屏待机只有 ~0.4W → 它会判成「远低于目标」而放宽升频，**夜间功耗反而上升**。
   两条已补，`cargo check` 通过。

**确认安全的**：特调 / FAS 的 `get_mode` 返回 None → `clg_cfg.enabled=false`，天然排除（它们优先级本就高于
PowerBase）；`apply_mode_takeover` 的 PowerBase 分支有调用点的 `is_screen_on` 保护；ModeChange 若在息屏触发
init 了 PowerBase，下一轮兜底块会 release（自愈）；WebUI 四处引用（ConfigView / state / i18n / meta.ts）齐备一致。

**提交提醒**：两个**未跟踪的新文件**必须显式 add，否则功能不完整——
`module/config/8745/`（SoC 配置目录：feature.yaml + meta.yaml）与 `src/chiri/power_base.rs`（调频器本体）。
`git add -A` 或显式 `git add module/config/8745 src/chiri/power_base.rs`。

## 三十八轮：二次审查（再修 1 处 + 调用点清单核对）

**修**：**停摆（DOWN）的 release 清单漏了 PowerBase**。冻结语义是「立刻交回系统」，而兜底块带 `!halted`
只会在**下一轮**把它收掉——中间那一轮里 PowerBase 仍在写频率。已把 `power_base.release()` 加进停摆清单
（`chiri/mod.rs:1088`，紧跟 `fast_lock.release()`）。

**调用点清单核对**（`git grep 'power_base\.' src`，五处覆盖完整）：
`apply_mode_takeover`（init + 两条 release）／**停摆清单**／**兜底纠正块**／`ModeChange` 分支／**事件驱动**
（`on_load_update`）。覆盖面 = 主路径 + 事件 + 兜底（兜底再覆盖启动 / ConfigReload / 亮屏恢复 / lab 重建）。

**靠推理确认安全、未改的**：

- **panic 自愈**：重建会按模式 init CLG，随后兜底块发现开关开着 → release CLG + init PowerBase ✓ 自愈闭环。
- **perf 口径与 CLG 不同**（PowerBase 用 `hw_min + 跨度×perf` 线性插值，CLG 用 `hw_max×ratio`）——设计选择，
  整体偏低、符合省电取向，已在 `freq_for` 注释写明。
- **8475 / 8998 的 feature.yaml 没有 powerbase 段** → 回退 `PowerBaseConfig::default()`（3.0W）✓ 不会崩
  （用户只指定了 8550=2.5 / 8745=3.5）。
- `PowerBaseConfig::normalize` 的钳制区间、`frozen` 与 PowerBase 共存（frozen 走 fast_lock，兜底块已排除）
  均无问题。

`cargo check` 通过。
**交付状态**：功能闭环 + 界面可开关。用户可在 WebUI「高级设置」里打开「涛声往复就已足够」；
真机验证建议：开开关后录 devimp，看 mode 列仍是 default/boost（外部接口不变）、
`batt_p` 是否向 2.5W（8550）/ 3.5W（8745）靠拢、降频是否明显激进、有无掉帧。

## 三十九轮：0922-0424 日志分析（8550）—— 场景特调评估结论：**不建议**

产物 `.codebuddy/docs/2026-09-22-0424-log-analysis.md`。样本 48 列（含 P1~P4）、100 分钟、249143 行。

**P1/P2 确认生效**：default 档 cap p95 = **72.4 / 82.9 / 69.9**（little/big/prime），与 `per_cluster`
配置 0.70/0.80/0.65 精确对应（前一版是 100/100/100）。

**场景特调评估（用户问的核心）—— 不建议为网易云建特调**，三条数据支撑：

1. **CPU 侧已贴负载**：little `cap/util = 63.6/59 ≈ 1.08`（B站 60/62 ≈ 0.97）→ 特调只能压 CPU 上限，
   这里没有余量可压，再压就是压性能。
2. **开销大头在 CPU 之外**：`gpu_busy 33.9`（B站 8.1，4 倍）；tgtop 显示 surfaceflinger 27.7% +
   display composer 15.2% = 42.9% 全在显示合成，app 自身只 25%。ChiRi 对 GPU/合成无手段。
3. **历史上它就是被移除的**：`src/chiri/special_tuned.yaml:40` 的「已移除条目」里明确有
   `com.netease.cloudmusic`（理由：实测与 CLG 基线相当、对跟手性敏感）。新数据没推翻该结论。

**正解**：若要省媒体场景的电，只有**给 GPU 设上限（旧 V4）**能碰到大头（GPU 33.9%、与功耗 r=0.851），
风险是掉帧，须真机测。

**★ 新发现：迁移率异常升高（差一个数量级）**。BpfStats 是 **2s 一次增量**（mod.rs:2712），换算后
迁移 ≈2800~3700/s、唤醒 ≈6000~21000/s，而 09-20 包同机型是 ≈278/s、≈486/s。可疑变量是 **P2 把三簇
上限拉开后的簇间落差**（EAM 在"小核不够快、大核被压"之间来回搬）→ 建议临时回退 `per_cluster`
录同场景对比来确认。

**其它**：bili 2.69W vs 上批 2.08W 不构成回归（样本 1051 vs 1885，子场景不同）；logd 在 launcher 占
**19.9%**（devimp 采集自身成本，A/B 必须同 dev_record 状态）。**本轮未改任何代码/配置**。

**四十轮：剩余优化空间评估（答复"还有没有优化空间"，未改码）**——已榨干：CPU 频率上限（cap/util≈1.08
已贴负载）、prime（util 0.18）、短交互（用户定调不做特调）、surfaceflinger/composer/system_server（无手段）。
**还剩三类，按性价比**：① 迁移/唤醒异常修复（回退 per_cluster 确认根因，估 0.1~0.3W）；② V4 GPU 上限
（cloudmusic GPU 33.9%，估 0.2~0.4W，须测掉帧一票否决）；③ 日常关 dev_record（logd 19.9% 是真实负载）。
①+② 全成则 cloudmusic 3.27W → 约 2.7~2.9W，即调度器层面可达的下限；再往下是 app/系统自身行为。
优先级：先 ①（便宜且可能双收益）后 ②。

## 四十一轮：A/B 执行——临时注释 8550 CLG per_cluster 定位迁移异常（用户「只做 1」）

- **an8.py 按分钟聚合补充证据**：迁移暴增是**全程性**（5300~16500/2s 无安静期），little cap 长期贴
  0.70 上限（60~72%）→ 支持「P2 簇间落差 → EAS 来回搬线程」假设。
- **改动一处**：`module/config/8550/feature.yaml` 的 `default.cpu_load_governor.per_cluster` 段临时
  注释（P1 `perf_ceil: 0.80` 保留），注释块内写明判定标准与收尾方案。cargo check（android）通过。
- **待真机**：刷入后同场景（cloudmusic）录 ≥10min → mig 回落 ≈300/s = 根因确认，倾向「恢复
  per_cluster + default 档加 migration_cost_ns」收尾；仍 2800+/s = 排除 P2，恢复配置转查探针口径。
- 注意：该 feature.yaml 还有一段**非本次改动**的未提交 `powerbase` 段（target_power_w 2.5）。

## 四十二轮：修 PowerBase 提交的两个真 bug（用户报「开了又被关 + 死循环 + 几千瓦」）

用户 5e94ee1 提交 PowerBase 后真机异常：meta.yaml 英文报错死循环重置、oplus/双电芯开关被抹、
WebUI 功耗显示几千瓦。排查确认**两个独立 bug**：

1. **死循环根因**：`common.rs::MetaYamlFile`（`deny_unknown_fields`）**缺 `powerbase_enabled` 字段**
   → WebUI 一写该键 serde 报未知字段 → 整 meta.yaml 判非法 → `sync_meta_snapshot` 嵌入默认整体
   覆盖（英文 warn "[Meta] meta.yaml fields invalid"）→ 用户所有 oplus/双电芯/校准设置连带被抹
   → WebUI 回读显示被关 → 再写 → 死循环。且 `ExternalMetaOverrides` 与 merge 链也缺 → 就算文件
   合法开关也读不进来。**修复**：MetaYamlFile + ExternalMetaOverrides + parse_disk_meta 构造器 +
   chiri/config.rs merge 四处补齐（common.rs:726 注释的「四处同步」清单就是防这个的，漏了）。
2. **几千瓦根因**：`batt_power_w()` = `batt_current_ma()` × 电压，**按安培消费**；但 divisor 默认
   `DEFAULT_UNIT_DIVISOR = 1000` 是旧「µA→mA」口径 → 默认配置电流虚大 1000 倍（4170000µA÷1000=
   4170"A"×4.17V≈17kW）。oplus_chg 开关被死循环抹掉后回退标准 µA 节点正好踩中。代码内注释三处
   两种语义互相矛盾（config.rs 说 ÷后得 mA、telemetry.rs 说得安培）。**修复**：统一口径为
   「divisor 除完 = V/A」，DEFAULT_UNIT_DIVISOR 1000→**1000000**（telemetry BITS 与 serde default
   自动跟随），config.rs/common.rs/telemetry.rs 注释、webui 两个 getter 缺省、meta.ts 文案、
   zh.ts 两条 hint 全链对齐（BCC mV/mA 机型填 1000/1000）。

**双电芯乱**是死循环连带（开关反复重置致读取口径时变），根因修好即愈。改动 7 文件，
`cargo check`（android）通过。**遗留观察项**（未改）：`chiri/mod.rs:2396` PowerBase 放电判定
`Some(i) if i > 0.0`——telemetry 注释说「µA 负值常见于放电方向」，若如此方向反了（充电按 39W
压频、放电 None 不限）；需真机日志确认方向后再定，用户确认其症状与此无关。

**真机指引**：重新编译刷入 → WebUI 重新打开 PowerBase / oplus_chg / 双电芯（这次不会被抹）→
校准值按新口径：标准节点留空（默认 1e6）或填 1000000/1000000；BCC 节点填 1000/1000 →
看 WebUI 功耗是否回到 W 量级、daemon.log 不再刷 [Meta] 重置。

## 四十三轮：审查上述修复——抓到关键遗漏并补齐（webui 只读未动，用户正在改）

**遗漏（会让修复完全失效）**：5 个 meta.yaml 模板（root/8550/8745/8475/8998）里全写着
`voltage_divisor: 1000 / current_divisor: 1000`——**模板值覆盖 serde 默认**，DEFAULT_UNIT_DIVISOR
改 1e6 对所有用模板的机器不生效；且模板 1000 + 新代码（启发式已删）= 几千瓦依旧。模板是按旧
口径（µA→mA）抄的，与新代码语义矛盾。**已改 1000000/1000000 并加口径注释**。

**审查通过项**：powerbase_enabled 链四处闭环（MetaYamlFile/ExternalMetaOverrides/构造器/merge +
消费点 config.rs:1214 + 模板已有该键 + WebUI 白名单）；DEFAULT_UNIT_DIVISOR 全部消费者（telemetry
BITS×2、sane 兜底×2、serde default×2、utils 定义）逐一核对均为 divisor 语义，1e6 统一后自洽。

**口径变化提醒**：divisor 1e6 后 status.csv/devimp 的 `batt_i` 列从 mA 量级变**安培**量级
（-500 → -0.5），**新旧日志不可直接对比**，离线脚本要适配；batt_v/batt_p 回归正常。

**webui 只读发现**：我改的 3 文件中 `zh.ts` 两条 hint 不在 git diff 里（疑似被用户正在进行的修改
覆盖，纯文案不影响功能，en.ts 口径本来就对）；module.prop 有用户自己的 2 行改动（未动）。
**残留语义提醒**：unit_divisor 旧键只兜底电压（common.rs:920 voltage 有 .or、current 没有），
若 meta 手写 unit_divisor: 1000 电压会错 1000 倍——模板无此行，仅手改旧键的机器要注意。
feature.yaml 的 A/B 注释已随 5e94ee1 入库（per_cluster 注释态即实验状态）。cargo check 通过。

## 四十四轮：口径二次修正（用户纠正：OPlus 节点放的就是 mV/mA，含标准节点）

用户指出模板 divisor 1000/1000 本来就对——**OPlus 机型（私有 bcc_parms 和标准 power_supply）
都报 mV/mA**，不是 Android ABI 教科书的 µV/µA（9-20 日志 batt_v=4.176 正是 mV÷1000 的证据）。
我上一轮把模板/默认改 1e6 是过度修正，已全部撤销：

- 模板 ×5 回 **1000/1000**，注释改为「OPlus 节点（含私有与标准）报 mV/mA 故填 1000；
  标准 ABI µV/µA 机型才填 1000000」
- `DEFAULT_UNIT_DIVISOR` 回 **1000.0**（目标机型全是 OPlus 系）
- config.rs / common.rs / telemetry.rs 注释同步为「默认 1000（mV/mA）；µV/µA 机型填 1e6」

**修正后的「几千瓦」完整解释**：BCC 主路径（mV/mA ÷ 1000/1000）本来就是对的；爆是因为
死循环把 `oplus_chg` 抹掉 → 回退标准节点 → OPlus 标准节点「单位也不保证」（common.rs:788 原注释）
→ 电流按 µA 量级进来 ÷1000 仍大 1000 倍 → 几千瓦。**死循环修好后 BCC 常驻**（telemetry「读到
有效值只认它」），回退路径几乎不再触发。

**保持不变的修复**：powerbase_enabled 四处补齐（死循环真根因）、batt_power_w 安培语义统一、
注释/文案对齐。**webui 待用户自己改**（用户在编辑，未动）：`state.svelte.ts` 两个 getter 缺省
被我改成 1_000_000，应回 1000（voltageDivisor / currentDivisor）。
途中曾引入双逗号编译错误（replace 尾逗号失误），已修，`cargo check` Finished。

## 四十五轮：读取层归一化（终版方案，用户补充「Android 标准节点一般是 µV/µA」）

用户两条陈述合起来暴露结构性问题：**私有节点 mV/mA、标准节点 µV/µA，两路单位差 1000 倍，
而 divisor 只有一对**——配 1000 时回退标准节点爆千瓦，配 1e6 时 BCC 读数小 1000 倍。
**终版修复：读取层归一化**——`read_standard_battery` 读入 µV/µA 即 ÷1000 变 mV/mA 等效值
（哨兵 i32::MIN 原样保留），两路共用 divisor=1000（回归纯量级校正用途）。
配套：字段改名 `batt_current_ua/batt_voltage_uv` → `*_raw`（存归一后共享值）、全链注释
（telemetry/config/common/utils/模板×5）统一为「读取层已归一，默认 1000 即正确」。
**webui 仍留给用户**：state.svelte.ts 两个 getter 缺省我此前改成 1_000_000，应回 1000。
`cargo check` 通过。教训：divisor 这类「单一旋钮」跨两种单位来源时，归一化要在读取层做，
不能指望用户手填区分。

## 四十八轮：thread_bind 全链清理（始终常开）+ 8475/8745 误写定位

**thread_bind 清理（用户指令：隐藏开关、清理外露配置、始终常开）**：

- daemon：`chiri/config.rs` Meta 删字段 + merge 删 + `affinity/core_ctl.enabled &= meta.thread_bind`
  两行删（只看机型子开关）；`common.rs` `rewrite_meta_toggles` 删参（三开关→两开关）、
  ExternalMetaOverrides 删字段、构造器删行；**MetaYamlFile 保留解析**（deny_unknown_fields 兼容
  旧 meta.yaml 的 thread_bind 行）；`rhine.rs` 全链 10 处删（LabDefinition/Backup 字段、eff_thread、
  apply/reassert/fallback/restore 的调用与 pick）；`chiri/mod.rs` lab 分支注释更新。
- 模板 ×5：删 `thread_bind: true` 行与 root/8745 的注释行。
- webui：ConfigView 删 derived+ToggleField、meta.ts WRITABLE_FIELDS 与 validateFieldValue case 删
  （META_FIELDS/布尔校验保留，兼容旧文件）、lab.ts frozen 接管列表删 thread_bind、i18n zh/en 删
  两条、state.svelte.ts 注释改。**兼容性**：旧 rhine-back.chr 快照的 thread_bind 字段 serde 默认
  忽略 ✓；旧 meta.yaml 带该行仍合法 ✓。cargo check 通过（用户已修好之前的 19 个中间态错误）。

**8475/8745 误写定位**：`module/config/8745/`（feature.yaml 139 行 + meta.yaml 41 行根模板复制）
是 **5e94ee1 整目录新增**；`CHIRI_SOC_HINTS = [8550, 8475, 8998]` **不含 8745 → 该目录永不生效**。
两份 feature.yaml 内容不同：8475（3880B，含 affinity/corectl/thermal，CpuIdle true）、
8745（4706B，有 [vector] 段、CpuIdle false、**缺 affinity/corectl/thermal 段**）。合并方向需用户
拍板（拍错毁调优）。`tuned_profiles.yaml:14` 注释 8745→8475 已改；devimpbin/8745 日志目录名
同属误写（归档侧）。

## 四十九轮：thread_bind 终态（用户纠正：实验室 frozen 的线程关闭要保留）

四十八轮的清理做过头了。**终态语义**：thread_bind 是**实验室 frozen 专用闸**——用户侧开关
（WebUI ToggleField、WRITABLE_FIELDS、i18n）保持移除/隐藏，但 daemon 生效链全部恢复：

- chiri config Meta.thread_bind + merge + `&= meta.thread_bind` 恢复（注释改「实验室专用」）
- common.rs rewrite_meta_toggles 恢复三参、ExternalMetaOverrides 字段与构造器恢复
- rhine.rs 10 处全恢复（LabDefinition/Backup/eff_thread/apply/reassert/fallback/restore）
- rhine-init.yaml 恢复文档与 frozen 的 `thread_bind: false`
- 模板 ×5 恢复 `thread_bind: true` 行（root/8745 带注释「实验室 frozen 专用、勿手改」；
  **模板必须有该行**——rewrite_meta_toggles 对缺失字段放弃写入，frozen 套用会失败）
- webui lab.ts frozen 接管列表恢复 'thread_bind'（ConfigView 开关保持隐藏）

净效果：用户侧无感（开关隐藏、不能手关），frozen 实验室期间 thread_bind=false 交还线程
亲和/绑核与 core_ctl，退出恢复。cargo check 通过。

## 五十轮：yaml 死字段/缺字段检查 + example 文件移出嵌入目录

**yaml 全量检查（17 份，node+js-yaml 脚本按"同类键集签名"分组对比）**：

- 无语法错误；**无死字段**（yaml 键 vs serde struct 逐键对照：CLG 20、meta 20、tuned 9、
  LabDefinition 5、PowerBase/Affinity/CoreCtl/Thermal 全集——feature/meta 的 serde 无
  deny_unknown_fields，死字段会静默忽略，故这份对照是唯一发现手段）；
- 无危险缺失：所有缺字段均有 serde default 兜底（root feature 缺 Thermal/Affinity 等段
  正是缺省函数存在的原因）。
- SoC feature 差异全部有解释：8550 独有 per_cluster（8550 实测特调）、缺 [vector]（fast_lock
  不读参数仅缺省值来源）、powerbase 只有 target_power_w（其余 default 1.15/0.5/50/3000/400）；
  **8998 缺 [powerbase] 整段 → default target 3.0W 兜底（d_pb_target_power），若 8998 认真用
  PowerBase 建议补显式段**；meta 模板 4 份键集完全一致（18 键）。
- 小瑕疵：tuned_profiles-example 的 akmode 缺 perf_ceil（示例滞后）、fas-example 的 pid 键
  纯示例性。

**example 文件移出嵌入目录（用户指令：构建不携带）**：module/config 整目录 include_dir! 嵌入
（宏不支持排除），`tuned_profiles-example` / `fas-example` / `config-example` 三个纯参考文档
（代码零引用，grep 确认）移至 **mdocs/**（嵌入体积 −27KB）；common.rs 嵌入注释加防回归说明
（\*-example.yaml 一律放 mdocs/）；文档引用同步 3 处（ModeInfo/TechAnalyze/tuned_profiles 头）。
**教训**：include_dir 整目录嵌入时，"参考文档"放目录内会静默进二进制。

## 四十六轮：0922-1306 包分析——功耗「提高」不成立 + 迁移之谜解开 + A/B 收尾

**样本局限**：仅 11:08~11:15 共 7 分钟（daemon 11:15:19 重启过、status.csv 只有重启后 22 行），
版本为 **40 列旧版**（无 P1-P4 天花板、无 PowerBase），场景海康/闲鱼（119 个放电 snap）。
**绝对功耗很低**（海康 1.56W、闲鱼 1.01W），不能支撑「功耗提高」。

**两个真发现**：

1. **cap 100/96.6/92.8 vs util 0.54/0.46/0.24**——旧版「cap>>util」浪费形态回归（闲鱼 little
   cap avg 88.6% 只服务 0.56 util）。用户若日常跑旧版，体感功耗上升最合理解释就是它
   （这正是 P1-P4 要修的形态）。
2. **★ 迁移率之谜解开（P2 嫌疑解除）**：这个无天花板的旧版本，亮屏 UI 场景迁移同样
   ~2500-5000/s（2s 增量实测非单调=增量口径）→ **高迁移是 surfaceflinger/system_server
   主导的亮屏 UI 固有形态**（tgtop：surfaceflinger 34-41%+system_server 35-55%），
   视频稳态才 ~300/s。之前拿 cloudmusic（亮屏 UI）对比 0920 视频稳态属**场景错配**。
   → feature.yaml 的 per_cluster 已恢复（A/B 收尾）。

**★ 放电判定（两次修正，终版=状态字符串）**：本包实测放电时电流为负，一度把 PowerBase 判定
改为 `i < 0.0`；**用户纠正：电流正负不可靠（部分设备充放都是正数），必须按状态判定**。
终版：复用 `read_battery_charge_state()`（`/sys/class/power_supply/battery/status` 归一为
charging/discharging/full/not_charging，函数注释本就写明「电流符号因厂商节点方向不一不可靠」），
`== "discharging"` 才传 `batt_power_w()`，与 PowerAVG 取样同口径；非放电传 None = 不做功耗限制。
**用户同时自己重构了 telemetry 读取层**（安装脚本检测 bcc_parms 写 divisor 1000、标准 ABI 缺省
1000000 的双口径方案，read_standard_battery 已无我的 ÷1000 归一化）——telemetry 层归用户维护，
勿再动。cargo check 通过。

**功耗评估方法论（再次验证）**：跨版本对比必须同场景+同版本+足够时长+同 dev_record 状态；
这包三条都不满足。干净的 A/B：刷修复后最新版跑 cloudmusic/bili 各 ≥15min 对比 0920/0424 基线。

## 四十七轮：0922-211322 包（130 文件/62 万行/48 列）——修复全验证 + salt.music 特调

**修复全验证通过**：① P1-P4 天花板精确生效（default cap p95 = 72.4/82.9/69.9 = 配置值；
playback = 66.7/80.9/50.0）；② meta 死循环消失（daemon.log 无 [Meta]）；③ **同场景功耗与基线
持平**：bili 2.75W（0424 基线 2.69）、cloudmusic 3.37W（0424 3.27）——「功耗提高」不成立。
迁移 ~4000-6000/s 又是亮屏 UI 固有形态（与上轮结论一致）。

**调优动作**：`com.salt.music`（椒盐音乐）加入 playback 白名单——28min 连续长稳态媒体、
2.24W、little cap 66.2/u0.64 贴负载、prime 26.9/u0.16 余量大、GPU 仅 12%（掉帧风险小），
复用 playback 参数组（special_tuned.yaml 带实测依据注释）。cargo check 通过。

**GPU 大头再次被数据指向**：cloudmusic 3.37W 超额全在 GPU（28.9 vs 椒盐 12.2）、
计算器 gpu 44.4%——V4（媒体场景 GPU 上限）仍是唯一能碰大头的杠杆，未做。
QQ 长驻 4208 样本 3.06W cap 已贴配置不动；微信 5.11W 仅 81 样本待观察。

**建议实测 5 步**：写 down.chr → 看 `down-enabled` + 60s 心跳 + devimp mode=down + current*mode.chr=down；
对比停摆前后 `scaling_governor`/`scaling_max_freq`/cpu_boost/cpuidle/IO/sched*\* 与 cpuset、在线核数是否交回系统；
删除 down.chr → 看 `down-disabled` 与按实时前台重建。

## 四十六轮：WebUI 字体改为「拉丁几何 + 中文人文」，构建期拉取（二进制不入库）

**用户需求（两轮澄清后的终版）**：拉丁/数字保持**几何主义**无衬线（Poppins），中文改**人文主义**无衬线
（Noto Sans SC / 思源黑体），字体文件**构建期在线拉取**、不入库。许可均 SIL OFL 1.1。

**为什么不用 `unicode-range`（重要，别再踩）**：声明范围过宽时，字体缺该字形会出豆腐块（.notdef），
而非回落下一字体。所以分离靠**文件本身**：中文子集只留 CJK 码位，拉丁字形物理上不存在 → 浏览器自动
回落 Poppins。**关键坑**：fontsource 的 `noto-sans-sc-chinese-simplified-*.woff2` **自带完整 ASCII（95/95）**，
原样内嵌会顶掉 Poppins、把英文也变成思源黑体 → 必须构建期裁掉拉丁。

**落地**：

- `webui/scripts/fetch-fonts.mjs`（新建）：从 jsdelivr 拉 `@fontsource/poppins@5.2.5`（3 档拉丁子集，
  原样用）+ `@fontsource/noto-sans-sc@5.2.5`（2 档，用 `subset-font` 按仓库用字裁成 CJK-only woff2）。
  字表 = `webui/src/**.{ts,svelte}` + `module/config/**/*.yaml` + `webui/index.html` + `config/i18n/zh.ftl`
  - `module/rules.yaml`（实测 1055 个 CJK 字 / 扫描 66 文件）；字表 <100 字直接 throw 拒绝产出残缺字体；
    已存在且 size>0 则跳过；写入用 **tmp→rename**（防半截文件被 vite 打进产物）。
- `webui/package.json`：`predev` / `prebuild` 自动跑该脚本；devDependencies 加 `subset-font`（仅构建期）。
- `webui/src/app.css`：`[fonts]` 换成 5 个 `@font-face`（Poppins 400/600/700 → `'ChiRi Sans'`；
  Noto Sans SC 400/700 → `'ChiRi Sans CJK'`，均 `font-display: swap`，**无 unicode-range**）；
  `--ak-font-sans` = `'ChiRi Sans', 'ChiRi Sans CJK', system-ui, ...`（系统栈只兜底）。
- `webui/.gitignore`：忽略 `src/assets/fonts/{poppins,noto-sans-sc}-*.woff2`。
- 许可：`LICENSE-OFL-poppins.txt`、`LICENSE-OFL-noto-sans-sc.txt`（后者版权行改为 Adobe / Source Han Sans 口径）。
- 文档：`agentsdocs/01-overview.md`（技术栈加「WebUI 字体」行 + tree 补 scripts/）、
  `agentsdocs/02-convention.md`（字体分工段落 + subset-font 依赖说明）、`webui/README.md` 脚本节。

**体积**：中文 145/148KB（整份 1.14MB/字重）+ Poppins 3×8KB。**验证**：`fonteditor-core` 探针实测
中文子集 `ASCII 0/95`、CJK 1039 字；Poppins `ASCII 95/95`、CJK 0 —— 完全分离。
`npm run build` 成功（dist 7 个 woff2）、`type-check` 0 错、`npm test` 85 passed。

**遗留**：`webui/src/assets/fonts/LICENSE-OFL.txt`（4301B，无版权行）是已移除衬线子集的遗留物、**已无引用方**，
待用户确认后删。**踩坑**：npm 默认缓存 `F:\npm-cache` 在沙箱外 → 所有 npm 命令需加
`--cache (Join-Path $env:TEMP '...\npm-cache')`；github.com 直链不可达，jsdelivr 可达。

## 四十七轮：未提交代码快速审查 + P2 修复（divisor 默认 1e6 / 安装期 OPlus 改写）

**用户指令**：先「快速审查所有未提交的代码逻辑」→ 我出 P1~P4 分级报告 → 用户拍板
「**P2 直接解决**：单位加校准默认缺省值改成 1000000，安装脚本里识别到有 oplus 节点时加一步把值改成 1000」。

**关键方向反转（务必记住）**：此前的「读取层归一化」终版方案（标准节点读入即 ÷1000 归一为 mV/mA、
divisor 默认 1000 两路通用）**被用户推翻并撤销**。新口径：

- **读取层不做任何换算**，私有节点的 ×1000 与标准节点的 ÷1000 两步全部删除，`batt_current_raw`/`batt_voltage_raw`
  存的就是节点原始值；
- **divisor 缺省 1000000**（正是标准 Android ABI 的 µV/µA 口径），两张基准靠这两个值区分；
- **安装脚本**检测到 `bcc_parms` 有内容时，除 `oplus_chg: true` 外**追加把两个 divisor 都写成 1000**（该节点报 mV/mA）。
  若还留着归一化，标准节点会被 ÷1000 再 ÷1000000、错 1e9 倍——这就是必须撤销的原因。

**落地**：`src/utils.rs`（`DEFAULT_UNIT_DIVISOR` = 1_000_000 + doc）、`src/monitor/telemetry.rs`（撤销归一 + 6 处注释口径）、
`src/common.rs` / `src/chiri/config.rs`（字段 doc）、`module/config/{,8550,8475,8745,8998}/meta.yaml`（值改 1000000 +
把原先 4 行并存的两套矛盾注释合并成 3 行）、`module/customize.sh`（`apply_oplus_switches` 加两条 sed + `MSG_OPLUS_DIVISOR`
中英文案；`apply_standard_divisor` 启发式保留为非 ABI 单位的兜底）、WebUI（`state.svelte.ts` 注释、`contract/meta.ts`
校验文案、zh/en 的 `battery.*Divisor.hint`、顺手修掉 zh 里多余的右括号）、文档（`agentsdocs/03-chiri.md` 199/200/202 三行、
`mdocs/TechAnalyze.md` 默认值表、`MEMORY.md` 电池段）。

**P2-4（字体缓存失效）**：`fetch-fonts.mjs` 原对中文字体也按「文件存在即跳过」→ 新文案的字永远进不了子集。
改为**字表 sha256 当缓存键**（`.subset-charset` 落盘，字体之后才写，中途失败下次仍重裁）。实测：删 stamp 重跑 → 重裁
（145/148KB）；再跑 → 「与当前字表一致（58d06b9b），跳过」。

**P2-5（meta.yaml 矛盾注释）**：5 份模板同一段并存「默认 1000」与「默认 1000000」两套说法，已合并。

**验证**：`cargo +nightly check -p chiri --target aarch64-linux-android` → Finished（仅 2 条既有 dead_code 警告）；
`npm run build` 成功、`type-check` 0 错、`npm test` 85 passed。

**撤回误报**：审查报告 P3-9「构建时对 `module/config/rules.yaml` 打无效警告」**是我误判**——该文件不存在，
脚本里显式列 `module/rules.yaml`（真实存在）是**必要**的，无噪音。

**遗留**：仓库根多出未跟踪的 `package.json`（`npm init` 误产物，name=chiri/license=ISC），`git add -A` 会带进仓库，
**用户未授权删除，保留待决**。

### 四十七轮补：校准倍数改成「两条路共用一个入口」（用户追问后）

**起因**：用户问「为什么安装脚本没改」。排查发现代码确实改了（`apply_oplus_switches` 里两条 sed），
但 `apply_battery_defaults` **只在「音量下键 = 覆盖配置」分支调用**——默认是「音量上键 / 超时 = 保留配置」，
那一支 `rm -rf "$MODPATH/config"` 后直接跳过，所以默认安装下一个字都不写。
（其余不写的情形：`active_config.chr` 缺失且 `ro.soc.model` 拼不出 `config/{soc}/meta.yaml` → `MSG_OPLUS_NO_META`；
无 sed → `MSG_OPLUS_NO_SED`。）

**用户口径**：「照原本没识别到 OPlus 私有节点的设备流程怎么改的校准比例，这个就怎么改」——
即两条路统一成**同一个入口**，别为私有节点硬编码 1000。

**改动**（`module/customize.sh`）：

- 新增 `apply_divisor_from_raw <meta> <电压原始值>`：按位数推算除数（n 位 → 1 后跟 n-1 个 0）、
  sed 写两个 divisor、打 `MSG_VOLT_APPLY` + `raw=.. -> divisor=..`；非法/首位非 3/4 → `return 1` 不写。
- `apply_standard_divisor` 瘦成薄封装（读 `voltage_now` → 调入口，失败打 `MSG_VOLT_FAIL`）。
- `apply_oplus_switches` 去掉硬编码的 `1000` 两条 sed 与 `MSG_OPLUS_DIVISOR`（EN/ZH 一并删），
  改为取 `bcc_parms` **下标 6（第 7 项）** 当原始值调同一入口；字段数（逗号数）提前算一次给
  「校准倍数」与「双电芯」两处共用（**`cut -f N` 字段不足时透传整行**，必须先数逗号）。
- 好处：私有节点报 mV/mA 推算出 1000，报 µV/µA 也自动算成 1000000；推算不出就不写——此时 daemon
  也用不了该节点、会回退标准节点，而缺省 1000000 正是标准节点口径，正好对得上。

**验证**：`bash -n` 语法通过；抽出三个函数用 Git bash 跑了 6 个用例（私有 mV / 私有 µV / 私有字段不足 /
标准 4382 / 标准 4382000 / 标准读数异常 999）**全 PASS**。

---

### 四十八轮：字体更换的遗漏（ak-ui 硬编码 font-family）

**起因**：用户「字体更换有遗漏」。

**根因**：`@yunyoujun/ak-ui@1.0.0` 的 dist 把 `font-family` **硬编码在 78 处组件规则**里
（`Noto Sans SC, Roboto, Arial, sans-serif` / `Noto Serif SC, serif` / `SFMono-Regular, Menlo, …` /
`"Syncopate"` / `"Arial Narrow"`），**完全绕开 `--ak-font-sans` / `--ak-font-mono` token** ——
内置的 Poppins / 思源黑体在这些组件里用不上，拉丁落 Roboto、中文落系统字体。`app.css` 此前
只对 `.ak-card__title`、`.ak-status__label` 两处做了先例覆盖，按钮 / 弹层 / 表单 / 开关 /
通知 / 进度 / 状态芯片 / 标签 / 分段器全部漏掉。（构建产物实测 77 处，确认进了 dist。）

**改动**（`webui/src/app.css`）：

- `[ak-adapt]` 段头注释补第 ③ 类问题（字体族硬编码）。
- 新增两条收回规则，一律带 `[data-ak-ui]` 前缀提升特异性（0,2,0 > 官方 0,1,0）：
  - 正文组 → `var(--ak-font-sans)`：`.ak-button`(`__label`) / `.ak-dialog`(`__title`/`__body`) /
    `.ak-field` / `.ak-input` / `.ak-select` / `.ak-textarea` / `.ak-choice` /
    `.ak-notice__title` / `.ak-notice__message` / `.ak-status__detail`
  - 等宽组 → `var(--ak-font-mono)`：`.ak-label` / `.ak-field__hint` / `.ak-progress__header` /
    `.ak-tag` / `.ak-dialog__eyebrow` / `.ak-segmented__item`
- `--ak-font-mono` 在 `'ChiRi Mono'` 之后**插入 `'ChiRi Sans CJK'`**：等宽组件（标签 / 字段名 /
  进度表头 / 日志）里也有中文，ChiRi Mono 只裁了拉丁，没有它中文会落系统默认字体、与其他位置不一致。

**连带补齐的遗漏**：

- `app.css [fonts]` 与 `fetch-fonts.mjs` 段头：许可正文漏列 `LICENSE-OFL-jetbrains-mono.txt`。
- `analyze/fonts/README.md:21`：仍写「webui 先例是 487 字、156KB 的**衬线**子集」——衬线子集
  2026-09-19 已删，改为「按仓库用字裁中文子集，约 145KB/字重」。
- `mdocs/TechAnalyze.md §12.5`：字体许可只列了 `analyze/fonts/`，补上 WebUI 三份（Poppins /
  Noto Sans SC / JetBrains Mono）+ 二进制是否入库。
- `agentsdocs/02-convention.md:141`：`[ak-adapt]` 口径从「只改颜色与表面」扩到「颜色、表面与
  字体族」，并写明 ak-ui 硬编码 78 处、新用官方类字体不对先查那两组。
- `.codebuddy/memory/MEMORY.md` 字体条同步。

**验证**：`npm run build` 成功；产物 CSS 内实测两条 `[data-ak-ui] :is(...)` 规则已落地，且
`--ak-font-mono:"ChiRi Mono", "ChiRi Sans CJK", ui-monospace, …` 生效（我们的 `:root` 在 ak-ui
之后，同特异性后者胜）。

**未动**：`analyze/` 页面（独立 PC 端 CSV 分析台，自带 `Analyze Sans/Mono` 子集与 OFL 许可，
与 WebUI 字体体系无关）。

## 四十九轮：1555 包分析 + CLG 决策振荡修复（util_smoothing）

样本 `devimpbin/8550/logd_0922-155505.tar.gz`（**48 列 Alpha05-Canary100**，QQ 6.5min 主样本 +
launcher/kernelsu 短样本）；同目录 `logd_0922-130643.tar.gz` 是四十六轮已分析的旧包，未重复。
**样本局限**：status.csv 显示 389/391 行 **charging**（battT 41.9℃、cap 85 全程是充电热）→
本包功耗/温度**不能**用于放电场景评估；fps 全 `-`。

**核心发现——CLG 决策振荡**（与充电无关的确定性问题）：QQ 大核 2048 tick 内方向反转 **458 次**、
16.9% 的 tick 改写 scaling_max_freq（单步 ~212MHz）；根因是 max_util 每 160ms 在 0.3~0.7 间摆动，
target_perf 跟着翻摆（决策链只防单 tick 尖峰、不滤连续抖动）。

**离线回放**（`devimpbin/8550/an49b.mjs`，基线与真机对拍 mismatch 8~10% = tick 心跳节流丢行所致，
模型可信）三选一验证：v1 `down_fast_threshold` 0.25→0.10 **无效**；v2 降频步长封顶 0.20 **反效**
（prime 写频 11.1→20.6%）；**v3 util EMA α=0.5 有效**：反转 458→194 / 324→149 / 192→149、
写频 −30%。α=0.35 可再降三成抖动但上限均值抬升更多（prime ceilAvg 22.9→26.4），取 **0.5 折中**
（同 tuned daily 先例；上限是 cap 不是实际频率，抬升的实际功耗代价有限）。

**改动**：CLG 新增 `util_smoothing`（EMA 系数，**缺省 1.0=关闭=逐位等价**，语义同 tuned）——
`config.rs`（字段 + `d_clg_util_smooth` + normalize + Default 四处）、`cpu_load_governor.rs`
（`ClusterState.ema_util` + EMA 段，尖峰抑制改吃平滑值；**devimp `max_util` 列保持写平滑前原始值**，
离线回放可试系数）；配置：root + 8550/8745/8475/8998 的 feature.yaml **default/reduce 配 0.5**、
boost 不开（响应优先）（SoC 文件整份覆盖根，五份都要改）；`config-example.yaml` 补键。
`cargo check`（android）通过。文档：`03-chiri.md` 补 util_smoothing 条，并修正 09-20
「禁止压 ceiling」过时条款（P1 已给日常档加 0.80 天花板，用户拍板）。

**顺带发现（未改）**：root/8745 的 feature.yaml 里 `smoothing_down / slow_down_scale /
down_fast_mult` 三个键 **config.rs 里根本不存在**（无 deny_unknown_fields 静默忽略）——03-chiri
写「已删除废弃参数」但 yaml 还留着，是误导性死键，待用户定夺删除。**待验证**：刷入后录
放电场景 devimp，对比决策反转率与写频率是否如回放下降。

## 五十轮：死代码/死配置清理——挖出 frozen 真 bug，撤回「死键」误判

清理面：cargo 警告、`#[allow(dead_code)]` 全量盘点、feature.yaml 键与**两侧**结构体对账。

**真 bug（顺手修）**：`fast.rs::tick()` 防篡改重写用的是 `hw_max` 而不是 `target`——frozen（锁最低频）
每 5 秒被拉回最高频，功能等于没有。cargo 的 `LockedPolicy.target never read` 警告就是这个错的回声，
三十轮审查说「fast.rs target/写序无问题」看走眼了。已改写 target（日志同口径）并删掉随之变死的
`hw_max` 字段。教训进 04-hard-lessons：never read 警告要当 bug 查，别当冗余删。

**死代码清理**：chiri/mod.rs 的 `probe_policy_capacity`/`auto_compute_capacity_weights` 删除
（FAS 禁用期遗留副本，真调用方全在 scheduler 侧）；scheduler/mod.rs 同名两函数其实**早就在被
fas/policy_mgmt 调用**，挂的 allow 和「恢复时启用」注释过时 → 摘掉改注；fps_monitor 两个
`latest_frametime` 方法（外层+state，均无调用方）删；power_base `BasePolicy.policy_id` 字段删。
**保留**：gpu.rs `devfreq_snapshot`（GPU_SNAPSHOT_ENABLED 开关下预留）、config.rs deny 需要的
占位字段、scheduler/fas 两个注释明示「预留能力」的方法（pid 热更/policy 热重载）。

**撤回「死键」误判（四十九轮说错了）**：`smoothing_down / slow_down_scale / down_fast_mult`
不是死键——是 Yumi `scheduler::config` 的活字段，root/8745 feature.yaml 与 README.en.md 归 Yumi
消费；chiri 侧忽略它们是分工不是残留。对账结论：chiri 侧 feature.yaml **无死键**（顶层
function/IO_Settings/CpuIdle/Affinity/CoreCtl/Thermal/Sched/scene_mode_delay_secs/五档全有主）。
03-chiri、MEMORY 口径已改（MEMORY 加「CLG 段双侧消费」条防再犯）。

cargo check（android）0 警告。AGENTS.md 索引无过时项，未动。

## 五十一轮：删除 Yumi 调度兜底（用户指令「删掉兜底用的 yumi，注意不要影响 ChiRi 及关联业务」）

**删了什么**：`src/scheduler/config.rs`、`cpu_load_governor.rs`、`scheduler.rs` 三个文件 +
`scheduler/mod.rs` 的 [consts]/[thread]/[cfgwatch]/[ipc]（mod.rs 缩成 81 行，只剩 `pub mod fas;`
与 policy 探测工具）；main.rs 的 Yumi 分支（`use crate::scheduler::config::Config` 删除，
lang_logger 统一走 `chiri::config`，[scheduler_start] 改为「非 ChiRi → warn + drop(rx) 不接管 CPU」，
新 i18n 键 `main-no-chiri-scheduler` 双语）；root/8745 feature.yaml 的 `smoothing_down /
slow_down_scale / down_fast_mult`（Yumi 参数，四档全清——四十九轮说它们「不是死键」当时对，
Yumi 一删它们就真死了）。

**刻意没动（ChiRi 关联业务）**：`scheduler/fas/` 全套（FAS 引擎，chiri/fas_manager 在用）、
`get_cpu_policies / probe_policy_capacity / auto_compute_capacity_weights`（fas/policy_mgmt 在用）、
共享层 `is_chiri_soc()` 分支（保留作「仅监控模式」，非 ChiRi 采样 200ms 口径不变）、
`yumi-ebpf`/`YUMI_SKIP_EBPF`/设备形态 `'yumi'`/README 上游引用。

**行为口径（拍板）**：非 ChiRi SoC 上 daemon 照常跑（监控/WebUI/日志/devimp），只是调度不接管 CPU，
启动打 warn `main-no-chiri-scheduler`。若要改成「非 ChiRi 直接不启动」再说一声。

**没删的孤儿**：i18n 有 19 个无引用键（affinity-_、fps-monitor-_ 等），用 git show 归因确认
**不是 Yumi 的**、是更早重构的遗留，未动。README.en.md 仍有 Yumi 参数文档（上游 fork 参考），
TechAnalyze 只在范围声明处打了「已删除、其余为历史快照」的补丁。

**验证**：cargo check（android）0 警告。agentsdocs 01/02/03/04 与 MEMORY 的双套架构、
Yumi 冻结、三侧同名等口径全部改写为单套架构；04-hard-lessons 的「Yumi 逻辑冻结」硬性约束
已替换为「已删除」说明。未跑 vitest（webui 无改动）。

## 五十二轮：全文检索 yumi 补漏（用户点名「安装脚本没调整」）

五十一轮删了调度本体，这轮把 yumi 字面量全仓过一遍（247 处/49 文件），补删可删项：

- **module/**：`rules.yaml` 的 `yumi_scheduler: true` 整链删除（Rust 侧早已无消费方）——
  `monitor/config.rs::RulesConfig` 字段、`webui/data/rules.ts` 的 yumiScheduler、AppsView
  「调度状态」行、zh/en 各 3 个孤儿 key（overview.device.yumi / apps.rules.scheduler /
  apps.notApplicableTag）、data-model/data-parse 测试夹具。两份 feature.yaml 头注释
  「# yumi 配置文件，不能动」改成如实描述（这就是用户说的「安装脚本」面）。
- **webui 设备形态**：`DeviceKind` 三态改两态（`chiri` | `unknown`），非 ChiRi 与读不到统一
  unknown（「无法判定」）；OverviewView/AppsView 的 yumi 分支删除。
- **注释口径**（src/ + webui/）：约 25 处「Yumi 无效/零开销/行为零变化/恒 false」改「非 ChiRi」。
- **杂项**：README.md 两处「保留回退兜底」「回退到 yumi 调度」改掉（上游链接与许可表保留）；
  .gitignore `yumi-*.zip` → `ChiRi*.zip`（打包名取 module.prop name="ChiRi Canary"）；
  .trae commit scope 表 scheduler 行更新。

**保留（非删除对象）**：`yumi-ebpf` crate 名/产物路径/YUMI_SKIP_EBPF、build.rs 编译链、
Cargo.toml workspace 成员、README.md 的上游链接与 GPL 许可行、README.en.md（上游 fork
说明书+许可参考，26 处 yumi 都是其正文）、agentsdocs 与日志里的历史叙述、TechAnalyze
历史快照。若要连 README.en.md 或 crate 改名都做，那是重命名/重写，另说。

**验证**：cargo check 0 警告；vitest 85/85、svelte-check 0 错（退出码非 0 是沙箱拦 npm debug
日志写入）。agentsdocs 03 的「设备形态三态」同步改两态。

## 五十三轮：复查「rules.yaml 只有 yumi 用」——记忆有误，但挖出 config.yaml 旧口径全仓残留

**rules.yaml 不删（给用户的结论）**：ChiRi 三处在消费——chiri/mod.rs 开机初始模式取
`global_mode`（注释里还记着 8550 default perf_init=1.0 锁满频的教训）、app_detect
`determine_mode` 的 app_modes > 特调回退 > global_mode 优先级、rhine 实验室对 global_mode
的覆盖兜底，外加 ConfigReload 热重载进 chiri 主循环。唯一 Yumi 专属的是 `yumi_scheduler`
键（五十二轮已删）。

**同类问题（本轮清理主体）**：

- `RulesConfig.fas_rules` 死字段删除——02-convention 自己写着「保留为 Yumi 冻结区向后
  兼容」，Yumi 删了它就该走；连带 `pub use FasRulesConfig` re-export。普通 Deserialize
  无 deny，旧磁盘 rules.yaml 残留 fas_rules 键不受影响。（FasAppFile.fas_rules 是
  normal/fas/<配置名>.yaml 的活结构，FAS 在用，保留。）
- **`config.yaml` 旧口径全仓清理**（文件早已拆成 meta.yaml+feature.yaml，main.rs 还在删
  磁盘遗留 config.yaml，但说法散布各处）：chiri/config.rs 13 处注释、chiri/mod.rs 3 处、
  common.rs 3 处（含「rules.yaml fas_enabled」→meta.yaml）、两份 feature.yaml 头注释、
  i18n 双语 scheduler-config-dirty-reload 文案、01-overview 目录树、02-convention 6 处
  （含「Config::load chiri 与 scheduler 各一份」「共 4 处」「scheduler config_watcher」等
  scheduler 残留口径）、03-chiri 2 处、README.md 1 处。
- main.rs 里清理遗留 config.yaml 的迁移代码保留（老版本升级残留仍需清）。

**验证**：cargo check 0 警告（RulesConfig 字段删除无连锁）。webui 未动不跑测试。

## 五十四轮：日志体系评估（devimp / daemon.log）与三项整改

**评估定调**：devimp 48 列 schema 与 daemon 键体系基本健康，不加新列/新日志——49 轮的
反转率回放就是拿现有 util*pct/decision/cur_freq/deb*\* 列算的；util_smoothing 平滑值
刻意不落盘（raw 落盘保留离线试验 α 的自由）。GPU 四列（44-47）在快照开关关闭下恒 "-"，
属预留，保留。

**执行**：

- 删除 19 个无引用 i18n 死键（zh/en 各 19 行，git 归因为更早重构遗留）。其中
  affinity-write-failed / affinity-pin-failed 恰是「线程写入失败观测」的现成文案，
  接线工作未做（见下），需要时从 git 历史找回。
- logger.rs devimp 表头注释「40 列定长」→ 48（9-21 追加 40-47 列后注释没跟上）。
- fast-init / fast-rewrite 的 `$max_khz` → `$khz`（frozen 修复后该值是锁频 target，
  vector=最高、frozen=最低，变量名会误导）；zh/en 文案补「（target）」。

**线程日志评估结论**（用户问是否需要给线程建类 devimp 日志）：不需要新体系——place/aff/
core/tgtop 四类 devimp 行已覆盖线程放置/钉核迁移决策/核状态/线程级 util。真缺口是
**写入结果观测**三处：① sched_setaffinity/cpuset 写入失败静默（affinity.rs 只在成功时
写 aff 行）；② uclamp 写入值/结果无记录；③ core_ctl min_cpus 写入/恢复无 devimp 行。
建议方案：aff 行 reason 扩展或 event 行 kind=thread_write 记录动作+结果，失败走
daemon.log warn，不建新文件。属新增观测功能，本轮未实施。

## 五十五轮：devimp 拆分重构（main* + aff*，计划全量落地）

计划 `.trae/documents/devimp-main-aff-日志拆分重构计划.md` 获批（附硬要求：注意性能
开销、不影响现有业务、写完审查逻辑）。brainstorming 定案：aff* 记线程全部动作+每秒
进程快照、devimp*→main\_ 全链改名（目录/归档/记账保留 devimp 名）、文本帧+二进制
扩展位、快照 top-N（10）+重点下钻、性能预算毫秒级。

**实施**：4 并行子代理按文件零重叠分工（A+B logger / C affinity+core*ctl / D+E
快照+配置+改名 / F+G webui+docs）。A+B 与 C 两个代理卡死（logger 的 aff* 块完全未写、
affinity/core_ctl 原封未动），B 的 [aff_writer][aff_api] 由我亲手补（AffWriter/
aff_action/aff_snapshot/aff_file_head/aff_token 净化），C 重派成功，另补 3 处覆盖
缺口（move_self_to_cpuset_root/group、force_online_all）。

**双 review 20 项发现全数处置**（逻辑审查+契约审查）：

- try_write_file→fs::write 丢失失败日志 → 新增 logged_write 回补（诊断关闭时失败
  仍可观测）；
- @S p/t 行 comm 未净化（换行会拆帧）→ aff_token 全字段净化，AFF_HEADER 口径同步；
- 下钻线程每秒两次 /proc/<tid>/stat → ThreadSample 同一次读顺带解析 processor；
- TGID_RUN_TIME 的 &mut/& 别名隐患 → 用户态句柄全链只读（compute_tgid_util 签名同步）；
- moved_group 失败不置位是全仓唯一控制流微修正（已注释记档）；
- webui META_FIELDS 注册 devimp_top_n+整数校验、mock 样例对齐真实值；
- 文档契约 15 项（act 删死枚举 unpin、ts 双口径分文件、锁清单补 AFF_TH_STATE、
  max_util 双语义、contingency/babel/frozen「预留」stale 等）。

**验证**：cargo check（android）0 错 0 警告；vitest 85/85；svelte-check 0 错。
**遗留**：真机 dev_record 录制验证（@A/@S 实录+失败注入）待刷机；tuned.rs main_rows
每 tick 构建（诊断关闭仍分配）为改前既有行为未动；子代理写入层有「成对下划线转
星号」的 markdown 归一化缺陷，本次已字节级校验修复。

## 五十一轮：0922-231918 包（MIUI/8475 设备）——FAS 扩容 speedmobile/lolm

**设备判明**：包内全是 MIUI 应用（miui.home/assistant/packageinstaller）→ **另一台 8475 设备**
（MIUI + 骁龙 8+ Gen1），devimpbin/8550 目录名是归档误放；今天合并的 8475 配置刚好被它用上。
115 文件/39 万行/48 列。

**游戏裸跑形态（重点）**：QQ飞车（speedmobile）2743 样本 **4.13W**、LoL手游（lolm）1286 样本
3.09W——**都在 default 模式裸跑**（FAS 白名单只有 endfield/sgame 两款）：prime 顶格 69.9/
util 0.61-0.67、迁移 1.2-2.1 万/s、gpu 3.8-32.1。轻应用（支付宝 1.45W、抖音 1.91W）形态健康。

**调优动作**：FAS 扩容——fas.yaml 白名单 + speedmobile/lolm 两行；新建 fas/speedmobile.yaml
（竞速：氮气/漂移尖峰）与 fas/lolm.yaml（MOBA 同 sgame），均抄 sgame 模板：fps_gears
[30,60,120]、target_fps [60,120]、perf_init 0.50、45℃ 电池护栏、perf_ceil 不覆盖。
**无实测帧率基准**（fps 列 "-"）——首刷后看 fps 列是否贴合 target 档，玩家开 90 帧则调
target_fps 为 [60,90,120]（配置注释已写验证步骤）。cargo check 通过（fas 目录自动收录）。

## 五十二轮：0922-231940 包——王者荣耀国服 FAS 缺口修复（联网确认包名）

**同一台 MIUI/8475 设备**（130 文件/11 万行/48 列）。**核心问题实锤**：
`com.tencent.tmgp.sgame`（**王者荣耀国服**，联网确认）1925 样本 **4.65W** default 裸跑——
FAS 白名单此前只有国际版 `com.levelinfinite.sgameGlobal`，国服包名不同 → FAS 从未接管。
形态：little 70.9/72.4 u0.69 + prime 64.6/69.9 u0.66 双顶格、迁移 **24965/2s**（全场最高）、
gpu 40.7。

**修复**：fas.yaml 加 `com.tencent.tmgp.sgame: sgame`；sgame.yaml 的 per_app_profiles 补国服
包名（target_fps [60,120] 同国际版）。cargo check 通过。

**包名联网确认对照**（web_search）：com.resukisu.resukisu = **ReSukiSU 管理器**（KernelSU 衍生
root 工具，3.50W/gpu 18）、io.github.aw1y2z.sesame = **芝麻粒 Sesame-M**（蚂蚁森林自动收取
Xposed 模块，1.39W 后台）、com.ss.android.ugc.aweme.mob = **抖音变体包**、hexin.plat.android =
同花顺、be.mygod.vpnhotspot = VPN Hotspot、mark.via = Via 浏览器、omarea.vtools = Scene 工具箱、
aiyu.kaipanla = 开盘啦。

**其它观察**：miui.home 短段 5.67W（49 样本、cpuT 68.2、mig 2.1 万）——桌面动画瞬时段，
样本少不足为据；quicksearchbox 4.43W（83 样本，gpu 29.8）同属短交互。QQ 2.82W 形态健康。

## 五十三轮：输入法语音启动延迟豁免（affinity_blacklist 扩容）

用户报告：语音输入常需等待才启动麦克风（处理器等待）。devimp 无 IME 粒度采样（snap 的
package 是前台应用，语音面板时前台仍是聊天 App）→ 无法用日志直接定位，改从机制修：

**ChiRi 侧三个真实嫌疑**：① IME 线程被 `pin_background` 钉小核（BACKGROUND_GROUPS 扫组内
tasks）；② IME 所在 cgroup 被 `cpu.uclamp.max=50` 钳制（BG_DEMOTE_GROUPS，组级无法按包豁免）；
③ CLG 升频限速（up_rate_limit_ticks + headroom）。其中 ①② 属 affinity 摆布 → 黑名单豁免。

**修复**：`src/chiri/affinity_blacklist.yaml` 加输入法段——8 条精确（讯飞/讯飞小米/搜狗/搜狗
小米/百度/百度小米/Gboard/微信输入法）+ `re:(?i)inputmethod` 正则兜底。命中进程全线程保持
全核、不做任何迁移与钉核。**边界写进注释**：cgroup 组级 uclamp.max 不受名单影响（多数 ROM
的 IME 在 foreground 组不吃钳制）；MIUI freezer/audioserver 启动不在 ChiRi 辖区。
cargo check 通过。**验证**：刷入后语音输入首字延迟应改善；若仍慢，复现时段录 devimp 看
place/aff 行里 IME 线程是否仍被钉（若在则说明 cmdline 未命中，补条目）。

## 五十四轮：FAS 稳帧低余量升档微调（用户：再激进一点点）

src/scheduler/fas/gear_state.rs 低 perf 稳帧升档路径的余量门槛 perf_index < 0.35 →
**0.40**（仅此一处，帧率稳/stddev/样本数条件不动），注释写明依据与「仍低于爆发路径 0.45」
的层次。cargo check 通过。

## 五十五轮：logger.rs 专项审查（48→44 列重写）

**重写内容**：devimp 行改名 main 行，MAIN*HEADER **44 列**（MainRow[44]，索引常量改 DM*\*
0-43 完整）；删掉 rom_core/ o_core/util_pct/pinned 四列；place/ff/core 三类行迁出
到同目录独立 aff 文件（AFF_HEADER 1 列标识）。STATUS_HEADER 22 列未变。

**机制审查通过**：U1 的常驻句柄 + LOG_VERIFY_BYTES 低频巡检 + metadata 校正 + 外部删除/
轮转自愈全部保留；循环轮转全程吞错无 panic；daemon/status/main/aff 四条流各自轮转与自愈；
enforce_dir_limits 对 logd/devimp 独立计量；新文件长度 0 时补写表头。

**唯一问题=文档滞后**（已修）：mdocs/TechAnalyze.md §9.4 仍写「devimp 的 48 列」+ 旧字段
→ 改为「main 行的 44 列」+ 新字段列表 + 行类型表删 place/aff/core + 注明脚本索引同步要点
（max_util=10、cur_freq_khz=15）。nalyze/index.html 的 util_pct 配色条目（新 schema 已无
此列）删除；新增 8 列（cpu_cur_khz…gpu_governor）映射名字未变仍有效。

## 五十六轮：daemon.log 增加模块版本行

`logger::init` 末尾调用新增的 `log_module_version()`：读 `module.prop` 的 name/version/
versionCode + `matched_soc_hint()` + `/proc/sys/kernel/osrelease`，以标准 info 行写入
（`[Main] 模块版本: ChiRi Canary Alpha06-04 (versionCode 102) | SoC 8475 | kernel ...`），
缺失字段一律 `-`、日志路径零 panic。**不动 `diag_meta()`**（它仍只供 devimp/aff 文件头），
避免与用户正在改的区块冲突。i18n 新增 `main-module-version`（zh/en 各一条，插在
`main-config-loaded` 之后）。cargo check 通过。
