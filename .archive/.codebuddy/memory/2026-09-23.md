# 2026-09-23

## 一：devimp 拆分重构后逻辑/算法深审（两子代理）——修 15 处自有问题，4 处历史既有只报告

- 对照 `git show HEAD` 划界「我的改动 vs 历史既有」，只修自己改动集：
  - 确定性 4：aff_write_block 重试改换新文件（防残块+重试块粘连破 @S 定界）、aff_open 同秒 99 候选耗尽改 pid 兜底、top-N tie-break（util 同值按 pid）、进程级 home 取最小有效值。
  - 数据完整性 3：aff_snapshot 丢弃非 p/t 行保帧计数恒等、enforce_dir_limit 保护活跃写入文件、p 行 util 取整统一。
  - 观测口径 4：AFF_HEADER `mask=` 注释正名（允许核 hex）、force_online_all @A reason 改传节点路径、scheduler-module-started 误报、「两套」等 8 处失真口径。
  - 配置防御 3：devimp_top_n clamp 1..=64 兼 Config::load 降级路径、meta.ts 改 Number.isSafeInteger（堵 1e21 让 serde 整文件重置）、名字缓存自愈阈值 4096→1024。
- 另修 clearArchives 注释/英文文案（归档口径 = 只删 logd/，devimp/ 是当前写入现场，启动即打包清空）。
- 遗留 4 处**历史既有问题**当时只报告未动手：comm 切片污染、pending 补偿多算、pin_self_dedicated/unpin_self 分叉、unpin_core/cleanup_thread 写失败清状态。

## 二：解决 4 处历史既有问题（用户「解决」）

1. **comm 切片污染（affinity.rs `sample_one_tid`）**：`text[1..close]` → 首 '(' 与末 ')' 定界取 `text[open+1..close]`。旧行为把「tid 尾部+` (`」混进 comm（tid 12345 → `"2345 (RenderThread"`），连锁 `is_key_thread`（KEY_THREAD_COMMS 全等）与 `is_affinity_blacklisted`（精确条目）恒不命中、@A/@S 帧 comm 失真。**行为影响（预期内）**：关键线程白名单（RenderThread/GLThread/GameThread/UnityMain/UnityGfxDeviceW）与黑名单精确条目从「形同虚设」恢复生效；正则黑名单本就 substring 匹配，行为不变。连带删掉 mod.rs 的 `clean_comm` 防御层（根因修复后它会误伤含 `(` 的合法 comm）。
2. **pending 补偿多算（cpu_monitor.rs `compute_tgid_util`）**：旧「raw 差分 + 当前 pending」恒多算 pending(t0)（上轮已计过），改 adj = raw + pending 差分（基线存 adj，与降级路径 `compute_thread_level_util` 同口径）；变量更名 `last_tgid_run` → `last_tgid_adj`。**行为影响**：前台 util 系统性高估消失，FAS 输入更准（util 略降属修正而非回归）。
3. **pin_self_dedicated/unpin_self 分叉（core_ctl.rs）**：新增 `self_pinned_tids` 成功清单（只记成功、每次重建），unpin_self 按清单逐个恢复、非 ESRCH 失败留清单重试；不再以 `self_pinned` 总开关短路——部分钉定失败时已钉线程永久滞留单核掩码的通路关闭。
4. **unpin_core/cleanup_thread/demote 写失败清状态（affinity.rs）**：非 ESRCH 失败保留状态（home/钉核计数/moved_group/条目）待 departed/gone/stale 路径重试，成功或 ESRCH（线程消亡）才清；内核掩码与状态表不再永久分叉。demote 的 `moved_group` 同族问题一并修（原无条件清除）。

验证：`cargo +nightly check -p chiri --target aarch64-linux-android` 0 错 0 警（webui 未动，vitest 免跑）。agentsdocs/03-chiri.md（FAS util 口径、亲和恢复失败语义、自钉成功清单）与 MEMORY.md 调度要点已同步。
遗留：刷机后 dev_record 实录验证 @A/@S 帧、失败注入 `e{errno}`、归档双文件；本轮 4 修复的真机观察点 = 关键线程落点变化（白名单生效后 UnityMain/RenderThread 会真正优先进 prime）与 FAS util 略降。

## 审查（0923，全未提交）：结论 = 无新逻辑问题 + 两个入库提示

未提交仅 10 文件（用户那批 Rust 重构已提交）：我的 logger.rs 模块版本行+ftl 两条、TechAnalyze/ModeInfo/analyze 文档同步、
memory；用户 agentsdocs 02-convention（锁序更新为 diag*/main*/aff\_ 六锁命名）与 03-chiri（新增「前台 util 口径修正 2026-09-23」：
compute_tgid_util 以 adj=raw+pending 差分，修掉旧写法重复计入上一轮 pending 导致的 util 系统性高估；max_util 双语义更新）。

验证：clippy 无 error（3 处 identical blocks = 写序敏感误报，勿合并）；vitest 85/85；en.ftl 第 19 行为完整单行（此前 diff 折行是显示问题）。

**两个入库提示**：① mdocs/ 下 3 个 \*-example.yaml 仍未被 git 跟踪（移出嵌入目录后从未 add）——提交时易漏，example 文档会丢；
② .codebuddy/memory/2026-09-23.md 未跟踪（memory 目录是跟踪的，应 add）。

## 项目级 skill：devimp-log-analysis（0923 新增）

把本会话重复十余次的日志包分析工作流固化为项目 skill：
.codebuddy/skills/devimp-log-analysis/SKILL.md + scripts/analyze.py。

内容：五步流程（解压含内层 tar → 现场判定[目录名不可信] → 按文件名时间戳筛最新组 →
跑聚合脚本 → 同口径对比）；48/44 两套列索引速查；判读口径（决策值 vs 实际频率、max_util
双语义、BpfStats 2s 增量、迁移率的场景基线、thermal soft_perf_cap 对照、充放电勿用电流符号）；
场景功耗基线表；已知坑清单（含：Select-Object 截断致统计失真、PowerShell 引号、pyyaml 损坏）。

脚本特性：自动识别 48/44 列 schema；充放电方向双向启发式判定（严格排除 batt_i==0，
不确定时报 [!] 提示人工核对）；--since 过滤新旧混装；输出场景聚合+三簇 cap/util+tgtop+行类型分布。
已用 0923b 真实数据验证通过（Windows GBK 控制台需 stdout.reconfigure utf-8，已内置）。

### skill 目录标准化（兼容 Cursor，0923 补）

用户要求 skill 安装目录兼容 Cursor。联网确认 Cursor 惯例：**项目级 .cursor/skills/<name>/SKILL.md**
（遵循 Agent Skills 标准，frontmatter name/description + 可选 scripts/），全局为 ~/.cursor/skills/。
已把 devimp-log-analysis 整目录复制到 .cursor/skills/，与 .codebuddy/skills/ **逐字一致**
（node 校验通过，frontmatter name 与目录名一致）。两份 SKILL.md 顶部均加「双路径镜像说明」：
改任一份必须同步另一份。.cursor 未被 .gitignore 排除，随仓库提交、团队共享。

### skill 位置修订（0923，用户：「.agents/skills 才是那个 skill」）

**正式位置改为 `.agents/skills/devimp-log-analysis/`**，`.codebuddy/skills/` 为 CodeBuddy 项目级逐字
镜像；`.cursor/skills/` 已被用户移除、不再维护。联网复核 Cursor 官方文档（cursor.com/docs/skills）：
Cursor 自动加载 **`.agents/skills/` 与 `.cursor/skills/`（项目级）**、`~/.agents/skills/` 与
`~/.cursor/skills/`（用户级），另兼容 `.claude/skills/`、`.codex/skills/`；SKILL.md frontmatter 必填
`name`（须与目录名一致）/`description`，可选 `paths`/`disable-model-invocation`/`icon`/`color`/
`metadata`；支持 `scripts/`、`references/`、`assets/` 子目录；嵌套 `skills/` 会自动按目录限定作用域。
两份文案已改（镜像说明 + 脚本命令统一为 `.agents\skills\...`），逐字一致校验通过。
**仍未 git add**：`?? .agents/`、`?? .codebuddy/skills/`、`?? .codebuddy/memory/2026-09-23.md`。

## 五：回看两轮日志分析——调度还剩什么（用户追问「都没有可改进的地方吗」，未改码）

**先纠正两处前提**：

1. **0922-130643 包不能比功耗**：status.csv 仅 22 行（≈22 秒有效采样，主体是短会话），放电样本 22/18 个、
   avg 2.66W 无统计意义 → 两轮里真正能作调度判据的只有 **0424 包**（1216 status 样本 + 4505 snap）。
2. **迁移异常结案**：1306 包（40 列、无任何天花板）亮屏 UI 迁移同样 ~2500-5000/s → 高迁移是
   **亮屏 UI 固有形态**（surfaceflinger/system_server 主导），视频稳态才 ~300/s；之前拿 cloudmusic
   对比 0920 视频稳态属**场景错配**。结论已写进 `module/config/8550/feature.yaml:119-123`，
   `per_cluster` 已恢复（P2 嫌疑解除）。

**0424 包新挖的 CLG 判定数据**（an12.py：over/under=组内 util 过 up_threshold / 低于 down_threshold 的核数，
语义见 `cpu_load_governor.rs:358-372`）：

- cloudmusic：little over/under **0.1/1.7**（u0.59）、big **0.1/3.6**（u0.46）、prime 0.1/0.9（u0.18）
  → **负载高度集中**：4 个性能核里平均 3.6 核低于降频阈值，只有 ~0.1 核过升频阈值，而 CLG 按
  max_util 给整簇升频 → 「单核瓶颈、其余空转」是调度层真正的结构性问题（与 affinity 钉核策略张力）。
- 决策分布 cloudmusic：up 33% + up_wait 21% / hold 19% / down_wait 19% + down 6%；
  **deb_up 触发 55%**（升频意图被 `up_rate_limit_ticks=2` 挡掉一半）。
- launcher：up 51% + up_wait 17%、deb_up 69%、psi_cpu 21.4 而 little cap 均值仅 48.7%（未贴顶）。
- bili（playback 特调）：over/under/tgt 恒 0/0/0 是 `tuned.rs:479-501` 的已知口径（特调路径不写这些
  列），非数据缺失，勿误读为异常。

**代码事实（两处，本轮确认）**：

- `psi_cpu` / `gpu_busy` / `over_cores` 在 `src/chiri` 里**只被日志输出消费，不进任何调度判定**。
- `GPU_SNAPSHOT_ENABLED=false` + `GPU_SNAP_INTERVAL_MS=10s`（`mod.rs:267-282`）：8550 实测每秒读 GPU
  频率节点把 GPU 从低功耗拉起，playback 场景 **1.76W → 2.59W（+47%）**；但 `telemetry_loop`
  仍**每秒读** kgsl `gpu_busy_percentage`（`telemetry.rs:301-308`）——采集成本的待验证项。

**剩余可改进项（按性价比，全部需真机 A/B）**：① GPU busy 采集开关对照（最便宜，直接解释「功耗变高」）；
② `up_rate_limit_ticks` 2→1（deb_up 55% 量化了升频时延）；③ big 簇负载集中 → 钉核/铺开策略（收益最大、
风险最高）；④ psi_cpu 入判定（唯一新增信号，但 psi 会被采集本身抬高，阈值须保守）；⑤ FAS 扩容
（speedmobile/lolm/sgame）+ 升档余量 0.40 仍无真机 fps 数据。
**已无空间**：CPU 上限（cap/util 1.08~1.16）、prime（上限已跟 util 0.18）、场景特调（不建议，理由已给）。

### 五补：用户提醒「两份日志均为旧版、版本不明」——定版结论（重要）

**版本其实在日志里现成**：devimp 文件头三行 metadata（`# module=` / `# soc= board= model=` / `# android= kernel=`）。
本轮实际定出：

| 包                 | 模块版本                                            | 机型 / 系统                               | 列数         | git 归属                               |
| ------------------ | --------------------------------------------------- | ----------------------------------------- | ------------ | -------------------------------------- |
| logd_0922-042407   | **Canary92**                                        | PHB110（一加 11）/ A16 / kernel 5.15.180  | 48           | `71c99c6`+（per-cluster 引入批）       |
| logd_0922-130643   | **Canary88**                                        | **MEIZU 21 Note** / A15 / kernel 5.15.119 | 40           | `71c99c6` 之前（09-20 `74eddfc` 一侧） |
| 0922-2319（8550h） | Alpha05-**Canary100**                               | **2210132C** / A17 / kernel 5.15.194      | 48           | 更晚（FAS 扩容批）                     |
| 当前工作区         | Alpha06-04（versionCode 102，`module/module.prop`） | —                                         | 44（未提交） | 四个全不一样                           |

**同一 SoC（8550/kalama）下三台不同机型**（PHB110 / 魅族 21 Note / 2210132C）→ 跨包功耗
绝对值一律不可比；`batt_i` 放电方向在三个包里判定都不一致（脚本两次报「方向需人工确认」）。

**skill 已同步升级**：`analyze.py` 加第三档 schema（40 列旧 = 无 `cpu_cur_khz`）、文件头定版输出
（`# module=` / `# soc= board= model=` / `# android= kernel=`，前 8 个文件多值即报混版本）、glob 兼容
`main_*.log`；两份镜像逐字同步已校验。

**两包不只是"旧"——版本差 4 档且机型/系统都不同** → 功耗数字（3.27W vs 2.66W）根本不可比，
"功耗是否提高"这个方向连判都判不了。之前 ①"迁移率结案"反而更硬：**跨版本（92/88）、跨机型
（PHB110/魅族 21 Note）都成立** = 版本无关结论 ✓。列数↔版本映射：40 列 = 71c99c6 前；
48 列 = per-cluster 批；44 列 = 未提交拆分版。

## 六：亮屏/息屏检测节点扩容（0923，联网检索后落地）

`src/monitor/screen_detect.rs` 原只有 4 类源（backlight / `*backlight*` leds / fb*/blank / DRM enabled）。联网核对内核 ABI
（stable `sysfs-class-backlight`：bl*power=FB_BLANK**、actual*brightness、brightness；testing `sysfs-class-lcd`：`lcd_power`
同为 FB_BLANK*\*；DRM connector 的 `enabled` / 老内核 `dpms`）后扩容：

1. 新增 `LcdPower`：`/sys/class/lcd/*/lcd_power`（三星 Exynos `panel/lcd_power` 那类只有 LCD class 的机型，0=亮 / 4=灭）；
   `power` 别名用 `is_file()` 判定（sysfs 设备目录都有 `power/` 运行时 PM 目录，用 `exists()` 会误枚举）。
2. 新增 `DrmDpms`：内屏 connector 无 `enabled` 时退 `dpms`（On / Off|Standby|Suspend），解析严格、其他值返回 None。
3. backlight 增加 `brightness` 兜底：驱动未实现 `get_brightness`（actual_brightness 读不到）时看 `brightness > 0`。
4. leds 匹配从「只含 backlight」放宽为关键字 `backlight/lcd/panel/wled/disp`（`panel-backlight`、`aw22xxx-backlight`、
   `wled-backlight`、`sprd-backlight` 等厂商命名），同时排除非面板 LED（keyboard/keypad/button/keys/charge/battery/
   notify/torch/flash/indicator/breath/rgb）——键盘背光/按键灯/充电灯随充电视亮灭，计票会永久挡住息屏；
   枚举与 uevent 两条路径统一走 `is_backlight_led_name`。

验证：`cargo check -p chiri --target aarch64-linux-android --offline` 通过（0 错 0 警）。
同步：i18n `screen-detect-no-source`（zh/en）、agentsdocs/03-chiri.md（源列表 + tally 构成 + uevent 口径）、mdocs/TechAnalyze.md。

**结论边界（回看上一条回答）**：版本无关可保留 = 迁移率形态、GPU 节点读取代价（8550 实测注释）、
CPU 上限贴负载的**形态**；版本相关不可外推 = cap/util 具体值、deb_up 55%、`psi` 不入判定、
telemetry 每秒读 gpu_busy、`up_rate_limit_ticks`——只对「当时代码 + 当期配置」成立，
要判当前版本必须用当前版本的包。

## 七：logd_0923-020304 日志包分析（0923 凌晨，Alpha06-04 / PHB110）

包：`devimpbin/logd_0923-020304.tar.gz`，内含 6 轮导出（`<MMDD-HHMMSS>.tar` = logd 侧
`daemon.log`/`status.csv`/`service.log`/`watchdog.pid`；`devimp_<MMDD-HHMMSS>.tar` = `main_*` + `aff_*`）。
时段 01:27:50 → 02:03:00（35 min），2 轮 daemon 会话（01:27:50 起 / 01:59:23 起，后者是导出重启）。
27 个 devimp 文件版本指纹全一致：**ChiRi Canary Alpha06-04 (versionCode 102) / 8550 kalama PHB110 /
A16 sdk36 / kernel 5.15.180**；schema 44 列新（拆分版，主文件无 `tgtop`）。

场景与功耗（devimp snap 与 status.csv 双向核对一致）：

| 场景                        | 时段                           | P_avg                                              | 帧率 / 温度                                                                      |
| --------------------------- | ------------------------------ | -------------------------------------------------- | -------------------------------------------------------------------------------- |
| 王者 FAS（120 档位）        | 01:37:27–01:48:12（10.75 min） | **4.34 W**（status n=643）/ 4.32 W（devimp n=641） | fps 121.3 avg / p50 122.2 / p95 124.3 / min 43.6；cpuT 62.4℃（峰 78.1）；cap 100 |
| bili playback（4 段）       | 01:32–01:59 内共 ≈21 min       | **2.78 W**（n=790）                                | cpuT 52.7℃；01:48:20 起 cap 85（batt 42.1℃ 触发），该段 2.5~2.7 W                |
| kernelsu default            | 01:27–01:32 / 02:02:47 后      | 2.8~3.1 W                                          | 管理器界面                                                                       |
| launcher / QQ / mt / skland | 各 0.5~1.5 min                 | 1.0 W（桌面）~6.3 W（QQ 尖峰）                     | 样本 20~30 行，仅参考                                                            |

异常与判读：

1. **FAS 频率不匹配 55 次**（policy 0/3/7 = 22/12/21，偏低 5.7%~26.5%，p50 8.8%）——
   `policy_controller` 只校验下沿，含义是 thermal/QoS/vendor 把频率压到目标之下，靠重写夺回；
   同期帧率达标 121 fps，**不能判 FAS 失效**。
2. **`scaling_governor` 写入 EPERM**：每轮启动 + FAS 激活各报 3 次
   （`Permission denied (os error 13)`）→ 内核本就是 `schedutil`，意图已满足；但若 OEM 换 governor，
   ChiRi 切不回来，属环境约束待查。
3. FAS 档位在 0.80/0.90/1.00 间摆动（`gear_state` 降档加速 + 快车道），帧率未受影响。
4. daemon 重启会把热保护状态重置回 cap 100（新会话从零升温）→ 跨重启的 cap 序列不可连读。
5. `handle_cpufreq_transition` 探针仍挂载失败 → `freq_trans` 恒 0；`touch-boost-disable` /
   `perfmgr-disable` 节点缺失（本机型无）。
6. 01:30:13/19 backlight 节点「息屏票不足」两次后按设计退役。
7. aff `@A` 结果：ok 3796 / e0 105（errno 不可得）/ e3 84（ESRCH 正常）；013453 轮另有 e22 ×2。
8. 迁移率：bili 播放态 mig≈9000/2s（**4500/s**，wake≈7000/s）、王者 ≈18000/2s（**9000/s**）、
   亮屏 UI 3000~6000/s —— 与旧「视频稳态 300/s」不同场景（本包 `@S` 帧显示 `bili:web` +
   `surfaceflinger` 同时活跃，属 UI 量级）。

skill 更新（两份镜像已同步，`.codebuddy/skills/` 曾整体缺失，本次补建）：
`ts` 无日期（过滤用 `^\d{2}:\d{2}:\d{2}\.\d{3},`）、44 列主文件无 `tgtop`、**FAS 段无 tick**
（`analyze.py` 已改为无 tick 段输出 `-` 而非 `0.0`）、`wakeups/migrations` = 2s 增量、
解压改到项目内、rg 搜不到 `*.log`、daemon.log 含 U+2068 隔离符需先剥离、`@A result=e0` 非成功。

### 七节修订（同日，用户口径变更）

上节末尾「skill 两份镜像已同步、`.codebuddy/skills/` 补建」**作废**：用户随即明确
「不用在 CodeBuddy 再写一次 skill」。已删除该镜像目录，skill 只在
`.agents/skills/devimp-log-analysis/` 维护（单副本）；`MEMORY.md` 的「项目 skill 位置」条目已就地
改为单副本口径，`SKILL.md` 的双路径说明与已知坑第 12 条同步改写。

## 八：调度优化实施（0923，依据第七节的日志分析）

**抢频排查（用户问「是不是 CLG 在跟 FAS 抢频率」）——结论：不是。**
证据：① status.csv 的 `clg_active` 在 FAS 段 643 行全 0；② FAS 段无任何 `[CLG]` 日志（停用都发生在
模式切换那一秒）；③ 代码侧 CLG 的所有旁路（热保护 atomic、5s 模式文件自愈、config_dirty、
负载投喂）都被门控或 `fas_mgr.is_active()` 抢断，`release()` 会 join 全部 worker 线程。
（排查由 code-explorer 子代理做，覆盖 tuned/fast_lock/PowerBase/core_ctl/affinity/down/scenemode/
tweaks 等全部写频点，只有 FAS 自身在写。）

**P7（新发现）：`fas-freq-mismatch` 是校验时序自噪声，不是外部压制。**
`apply_freq_locked` 升频「先写 max 后写 min」、写完立刻 `do_verify_freq` 读 `scaling_cur_freq`；
内核调频异步，读回的是**写入前的旧档** → 判定「被压制」并 `re_unmount` + 重写（无效，因为目标本没错）。
实测 55 次的实际值全部 = 旧档（低 1~2 档，5.7%~26.5%），近旁 snap 的 cur==max、fps 121 达标。
修复：新增 `verify_at` + `VERIFY_SETTLE=200ms`，改为「在下一次写入前抽查上一次目标」；
`do_verify_freq` 重构为 `verify_prev_freq`（真实压制仍会抓到）。

**已实施的优化**：

| 项  | 文件                                                               | 改动                                                                                                                     |
| --- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| P1  | `module/config/normal/tuned_profiles.yaml`                         | playback `hysteresis` 0.04→0.06、`down_hold_ms` 100→150（死区必须 ≥ 频表步长）                                           |
| P2  | 同上                                                               | playback 补 `per_cluster.big.perf_ceil 0.85`（原无覆盖，尖峰 2707 MHz）                                                  |
| P3  | `src/fas_types.rs` / `src/chiri/fas_manager.rs` / `fas/sgame.yaml` | FasRulesConfig 新增 `migration_cost_ns`；FasManager 接管期写 `sched_migration_cost_ns`、deactivate 恢复；sgame 配 400000 |
| P7  | `src/scheduler/fas/policy_controller.rs`                           | 校验延后一拍（`verify_at` + `VERIFY_SETTLE`）                                                                            |

`cargo check -p chiri --target aarch64-linux-android` 通过（0 错 0 警）。

**撤销一项（纠正此前误读）**：P4「FAS 亲和搬动过频」不成立——`overload_hold` 是「想搬但滞回不满足」
的打点（`affinity.rs` 的 `else if core != home` 分支，30s/tid 节流），不是搬动记录；
`OVERLOAD_MARGIN 0.15` + `RETURN_COOLDOWN 16s` 已在起作用，无证据支持改门槛，**不动**。

**P5 仅记录（用户要求不改）**：sgame `core_temp_threshold 45℃` 本次 10.75 min 未触发（电池峰值
42.1℃），FAS 靠档位收敛把功耗从 4.8W 降到 3.0~4.1W 而 fps 保持 121~123 —— 护栏留作更长对局的兜底。

**P6 口径修正（写进 skill）**：热保护 `cap` 压的是天花板，对贴负载特调（playback）几乎无感——
cap 85 段 bili 功耗未变（上限本就在 85% 之下）。别把「cap 生效但功耗没降」当成 bug。

**待实测验证**：P1 看 big 改频/s（应 <2）与 P_avg；P2 看弹幕尖峰是否掉帧；P3 看同局迁移/s 与
fps p95/min；P7 看 `fas-freq-mismatch` 是否基本消失。均为凉机对照。

**P7 复核修正（同日）**：首版实现把计时起点放在「每次写入」，目标高频改动时校验会被无限推后
（`at.elapsed() < 200ms` 恒成立 → 永不抽查，等于把校验关掉）。已改为计时起点取「目标值最后一次
**变化**」：目标稳定 ≥ 200ms 才抽查一次，频繁改频期间跳过——既消除旧档误报，又保留对真实压制
（thermal/QoS 覆写）的检测。`cargo check -p chiri --target aarch64-linux-android` 通过。

## 九：logd_0923-124300 分析 + 亮屏功耗紧缩（0923 下午）

包内 6 轮（10:35→12:42，约 2h7m），版本 **Canary Alpha06-06**（PHB110/A16），44 列新。

**场景画像（status.csv，全 3553 行均放电）**：

| 场景              | 时长        | P_avg         | 备注                      |
| ----------------- | ----------- | ------------- | ------------------------- |
| 息屏待机（nopkg） | ≈100 min    | **0.45 W**    | 已很好，无优化空间        |
| launcher 亮屏     | 12:28-12:42 | **1.30 W**    | GPU 1.8%、psi 15          |
| 微信              | 12:21 段    | 3.30 W        | GPU 15%、psi 20           |
| QQ                | 12:15 段    | **4.92 W**    | GPU 24%、psi 30，p95 9.7W |
| 酷安              | 12:20 段    | 4.24 W        | GPU 19%、psi 32           |
| bili playback     | 12:21 段    | 4.59 W        | GPU 18%、psi 40           |
| kernelsu / mt     | 短段        | 5.42 / 1.99 W | 样本少                    |

**关键机制认知（本次实测确立）**：

1. CLG 的 `target_perf = clamp(util × headroom, floor, ceil)`——**与 up_threshold 无关**（它只管
   headroom 的 ramp、升频速度和 over/under 计数）。要降平均频率，主旋钮是 **perf_ceil 与 headroom**。
2. **实际频率紧贴决策上限**：QQ big 实际 p50 / 决策 p50 = 92%（酷安 93%、bili 93%）→ 压 ceil 直接生效。
3. big 簇上限尖峰占比高：QQ 有 18.6% 的 tick 顶在 2300 MHz（ceil 0.80 落点）、kernelsu 34%。
4. 触摸升频地板 = `perf_init + tiers × 0.05`（`compute_touch_boost_floor` 用 perf_init 做基线），
   且被 perf_ceil 钳制——所以「稳态压低 + 触摸拉满」的上限就是 ceil。

**已实施（8550 default 档 + playback 特调）**：

| 项                                         | 旧 → 新                                         |
| ------------------------------------------ | ----------------------------------------------- |
| default `perf_ceil`                        | 0.80 → **0.60**                                 |
| default `per_cluster.little / big / prime` | 0.70 / 继承0.80 / 0.65 → **0.50 / 0.60 / 0.45** |
| default `headroom_factor`                  | 1.05 → **1.00**                                 |
| default `perf_init`                        | 0.25 → **0.35**（喂触摸地板）                   |
| default `touch_boost_ms / tiers`           | 400 / 1 → **600 / 4**（地板 0.55，接近 ceil）   |
| default `spike_decay`                      | 0.25 → **0.15**                                 |
| playback `headroom`                        | 0.95 → **0.85**                                 |
| playback `little / big perf_ceil`          | 0.65 / 0.85 → **0.55 / 0.75**                   |

**一阶估算**（tick 序列按新参数重算上限，频率表取本段出现值；未实机验证）：
QQ -24%、酷安 -23%、bili -18%、微信 -18%、mt -13%、launcher -6%（均为 **SoC CPU 部分**的
功耗比，f^1.5 近似）→ 折到整机（屏幕 ≈1.2W + GPU/DRAM 不变）约 **-8~12%**。

**可达性结论（重要）**：整机功耗里屏幕、GPU、DRAM 都不归 ChiRi 管（GPU 节点实测读不到，
`gpu_cur_khz` 全空）。CPU 侧压 40% 频率只能换来整机 10% 上下，**「平均功耗减半」单靠 CLG 参数
做不到**。唯一现成的达标机制是 **PowerBase**（以功耗为闭环目标，8550 `target_power_w: 2.0`，
开关在 meta.yaml `powerbase_enabled`），代价是它会主动压性能——需用户决定是否启用。

## 十：息屏仲裁改多数票（0923）

`screen_off_confirmed` 由「固定 2 票（`OFF_QUORUM=2`）」改为「**OFF 票 > 有效票数的一半**」：

- 1 票 OFF（只暴露一个可读节点的机型）仍确认；0 个有效读数仍不确认；
- `2 OFF + 2 ON`、`3 OFF + 3 ON` 这类票型不再确认（原来 2 票就够）；
- `2 OFF + 1 ON` 仍确认（多数）。

`OFF_QUORUM` 常量已删；同步 `src/monitor/screen_detect.rs` 三处注释、`src/chiri/mod.rs`
PowerAVG 采样处注释、`agentsdocs/03-chiri.md` 息屏判定段、`MEMORY.md` 息屏轴条。
`cargo check -p chiri --target aarch64-linux-android` 通过。

## 十一：取消息屏检测节点退役（0923）

按用户要求删掉整条退役链，改「票数为唯一判断标准」：

- 删除静态量：`SCREEN_SOURCE`、`SCREEN_READ_FAILS`、`READ_FAIL_WARNED`、`RETIRED_NODES`、
  `ALWAYS_ON`/`ALWAYS_ON_LOGGED`、`LAST_OFF_REVIEW`、`INCONSISTENT_SINCE`、
  `VETO_EPISODES`/`VETO_EPISODE_LAST` 与相关常量（`SCREEN_READ_FAIL_LIMIT`、
  `OFF_REVIEW_INTERVAL`、`INCONSISTENCY_SWITCH_DELAY`、`VETO_*`）。
- 删除函数：`select_primary_node`、`screen_source`、`inconsistency_due_for_switch`、
  `veto_episode_due_for_switch`、`reset_veto_episodes`、`enter_always_on`、
  `retire_primary_and_switch`；区块 `[switch]` 一并去掉。
- `verify_screen_state` 重写为**每轮全节点投票**：OFF 多数 → 校正 OFF；ON 多数 → 校正 ON；
  票数平手或全部不可读 → 不改判（并 info 一条 `screen-detect-no-source`，恢复后复位）。
- `update_state_if_changed` 去掉恒亮屏早退、退役触发与计时清理，只保留「票不够就保持亮屏」+
  `screen-off-vetoed` 打点。
- i18n 删 3 键（`screen-detect-read-failed` / `screen-detect-nodes-exhausted` /
  `screen-detect-node-switched`），`screen-off-vetoed` 文案去掉「15s 后退役」；zh/en 同步。
- 文档：`agentsdocs/03-chiri.md` 的检测源段与「两阶段判定」段改写为多数票 + 无退役，
  删「驳回 episode 退役」条；`mdocs/TechAnalyze.md` 的仲裁段与两处常量表同步。
- `cargo check -p chiri --target aarch64-linux-android` 通过（0 错 0 警）。

**代价（已知）**：某节点失真时不再有「换节点」兜底，全靠多数票——同向失真的多数节点仍会误判；
反过来也不再出现「候选被逐个退役 → 恒亮屏 → 永不息屏」。

## 十二：logd_0923-165409 复核（16:42-16:54，12 min，Alpha06-06）

**配置已生效的证据**：CLG（default）段 tick 的 `tgt_perf` 上限恰好 = 0.500 / 0.600 / 0.450
（little / big / prime），与 `8550/feature.yaml` 的 per_cluster `perf_ceil` 完全一致。
（cap 落点 1100/1900/1700 MHz 也自洽：CLG 的 ratio 是归一化到 `[fmin, fmax]`，
不是 `ratio × hw_max`——算落点别忘了加 fmin 偏移。）

**功耗对比 logd_0923-124300**：

| 场景             | 12:15 包          | 16:42 包                          | 变化                                 |
| ---------------- | ----------------- | --------------------------------- | ------------------------------------ |
| QQ default       | 4.92 W            | **3.35 W**（p50 2.75 / p95 7.55） | −32%                                 |
| bili playback    | 4.59 W            | **2.71 W**（p50 2.34 / p95 6.55） | −41%                                 |
| kernelsu default | 5.42 W            | 2.94 W                            | 样本不同                             |
| 亮屏整体         | —                 | 3.30 W                            | —                                    |
| 息屏             | 0.45 W（100 min） | 1.06 W（45 s，刚息屏）            | 未进 scenemode（延迟 300 s），不可比 |

**剩余功耗为什么还高**：

1. **天花板被负载顶满**：QQ 段 little 有 49%、big 有 14~30% 的 tick 处于 `tgt_perf == ceil`
   且 `cur_perf == ceil` 状态 → 功耗由应用负载决定，不是参数太松；再降天花板等于直接砍性能。
2. GPU busy 17.8%（QQ）/ 7.7%（bili）→ GPU 无可控面（`gpu_cur_khz` 本机读不到）。
3. QQ 段 decision：up 2533 / hold 609 / down 293 → 升频占比 60%，仍以追负载为主。

**本轮再收紧（低风险）**：`background_uclamp_max_pct` 50→35（后台让路，前台无感）、
`spike_decay` 0.15→0.10（只削单 tick 尖峰，不动稳态）。均为 TODO 待复核。

**未动、待定**：① 天花板再降一档（三簇各 −0.05，约再省 10~15% CPU 功耗，但顶满场景会直接变慢）；
② 启用 PowerBase（以功耗闭环，8550 `target_power_w: 2.0`，开关在 meta.yaml）。

## 十三：方向修正——不封顶，改控「高频门槛 + 动态范围」（0923 傍晚）

用户指正：除部分特调外，任何在用模式都应保留**随时到最高频**的能力，不能用静态 `perf_ceil`
封顶；要调的是「使用高频的门槛」与「高频的动态定义范围」。

**8550 default 档改动**：

| 参数                    | 上午值                              | 新值     | 作用                                              |
| ----------------------- | ----------------------------------- | -------- | ------------------------------------------------- |
| `perf_ceil`             | 0.60                                | **1.00** | 撤销静态天花板，保留满频能力                      |
| `per_cluster` 覆盖      | little 0.50 / big 0.60 / prime 0.45 | **删除** | 三簇上限都交给负载动态决定                        |
| `up_threshold`          | 0.78                                | **0.88** | 高频门槛：只有 util ≥ 0.88 才算高负载、走全速升频 |
| `headroom_ramp`         | 0.12                                | **0.08** | 高负载加成的过渡带收窄                            |
| `smoothing_up`          | 0.75                                | **0.60** | 高负载升频步长变小                                |
| `up_rate_limit_ticks`   | 2                                   | **3**    | 升频需连续确认                                    |
| `up_jump_threshold`     | 0.30                                | **0.45** | 尖峰更难瞬间把上限拉高                            |
| `slow_up_scale`         | 0.03                                | **0.02** | 滞回带内爬升更慢                                  |
| `down_threshold`        | 0.55                                | **0.62** | 降频更早触发                                      |
| `down_rate_limit_ticks` | 3                                   | **2**    | 回落更快落地                                      |
| `down_fast_threshold`   | 0.25                                | **0.30** | 低载立即降的范围更宽                              |
| `spike_decay`           | 0.15                                | 0.10     | 削单 tick 尖峰（本轮早先已改）                    |

**离线模拟**（同一 tick 原始 util 序列跑旧/新两套参数；简化模型，相对值可信）：
QQ −3.7~−5.9%、kernelsu −2.5~−5.2%、bili −2.6~−6.6%；同时新参数下出现「cap ≥ 70% 硬件」
的时刻（bili big 8.9% / prime 2.1%），而封顶的旧参数恒为 0% → **既压均值又保留冲高频的能力**。

playback 特调保持封顶（little 0.55 / big 0.75 / prime 0.50，用户口径「部分特调」除外）。

## 十四：logd_0923-190037 分析（18:50-19:00，Alpha06-09）+ 热压制空转修复

包：单簇导出，44 列新，**Alpha06-09 (10609) = 当前工作区 module.prop**、8550/PHB110/A16。
场景 = mm default（2 min）+ bili playback（8 min），全程放电亮屏、**batt 40.7→41.4℃ 热态、cap=85**。

| 场景          | n   | P_avg  | p50 / p95    | 备注                                                                                      |
| ------------- | --- | ------ | ------------ | ----------------------------------------------------------------------------------------- |
| bili playback | 492 | 3.27 W | 3.20 / 5.51  | 分钟序列 4.35→2.27，稳态 2.3~2.8 W 在基线内；mig 4798/s（弹幕 UI 量级）                   |
| mm default    | 123 | 3.43 W | 2.46 / 10.31 | 尖峰 = util 1.0 真实负载把 big+prime 同时顶满（18:50:32、18:51:05 两串），属 race-to-idle |
| launcher      | 5   | —      | —            | 模式切换过渡秒，无统计意义                                                                |

**关键发现：热压制软限档完全空转。** `free_above` 0.80 被 normalize 抬到 0.85 = `soft_perf_cap`
→ 压制带 `(0.85, 0.85)` 为空 → **cap=85 时段零压制**（实测 launcher little 22/22 tick 写满频、
mm prime 28 tick 写 3187 MHz）。此前 P6「cap 生效但功耗没降——上限本就在 85% 之下」的解释不成立：
真因是软限档自 2026-09-22 的 0.70→0.85 起就没生效过；「对特调无感」倒是对的，但原因是热压制只作用于 CLG。

**修复（3 处）**：

1. `cpu_load_governor.rs` flush：热压制改「只钳写频、不回写 `current_perf`」——回写会让升频步长够不到
   豁免档时状态永久卡死在 cap，注释承诺的「持续高负载平滑涨过豁免档」原本不成立。
2. `8550/feature.yaml`：`free_above` 0.80→**0.95**（压制带 `(0.85, 0.95)`，`>=0.95` 保留全速；
   8475/8998 的 0.80 > 0.70 带宽正常，未动）。
3. `config.rs` normalize：「豁免档过低或相等」统一抬到 `soft_perf_cap + 0.10`（旧逻辑只抬「过低」，
   相等仍空转——正是本次成因）。

**其余验证**：playback per_cluster ceil 生效（超 ceil 仅模式切换那 1 tick）；P1 hysteresis 0.06 复核 =
big 改频 2.1 次/s（略高于 <2 目标，未再动）；mm touch=1 占 tick 7.5%。
**未动待定**：触摸地板 0.55/600ms（交互 vs 功耗，用户定）；单核满载顶整簇频率（结构性）；PowerBase。

`cargo check -p chiri --target aarch64-linux-android --offline` 0 错 0 警。
**待实测**：41℃ 时段 cur∈(0.85,0.95) 段写频应落在 ≈0.85 折算点（big≈2458 MHz），而持续满载 2~3 tick
（≈0.5s）后仍到满频；温度回落 38℃ 以下恢复无压制。同步：agentsdocs/03-chiri.md 豁免档条、
MEMORY.md 调度要点、skill 判定要点（`free_above` 正名为性能豁免档）。

## 十五：devimp-log-analysis 由 skill 转为命令（0923 晚，用户指令）

用户要求「从 skill 转为命令 /devimp-log-analysis，然后删除 skill」。已执行：

- `.cursor/commands/devimp-log-analysis.md` 填入 skill 全文：去掉位置说明/镜像修订记录，
  头部加 `$ARGUMENTS` 问询与「维护位置」说明（命令文件即唯一副本）；已知坑原第 12 条
  「勿建 skill 副本」随 skill 删除而移除，其余 11 条原样保留。
- 聚合脚本 `git mv` 到根 `scripts/devimp-analyze.py`（`scripts/` 为新建目录；docstring 的
  `../SKILL.md` 引用与用法路径已同步改为命令文件与新路径）。
- `.agents/skills/devimp-log-analysis/` 整目录删除；SKILL.md 带未暂存改动被 `git rm` 拒删
  → 删盘上文件后 `git add` 补暂存（status 显示 ` D` 转 staged）。`.agents/` 随之为空、已移除。
- `MEMORY.md`「项目 skill 位置」条目改写为命令口径。

**注意**：`.cursor/` 此前未被 git 跟踪，本次随提交入库（命令团队共享）。
（0923 晚补注：「十六」条目所记计划已按环境口径迁至 `.cursor/docs/daemon-perf-opt-plan.md`
与 `.cursor/memory/`，`.codebuddy/` 不再承载该计划文件。）
