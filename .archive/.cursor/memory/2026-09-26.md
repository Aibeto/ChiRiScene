# 2026-09-26

## 0926-162821 包（Alpha07-02 首包）归因分析

- 数据整理由子代理 [整理 0926-162821 包数据](b3ce711d-2b58-41c8-a842-40c5b9cd4e2f) 完成（`organized.md`，七项计数+附录 A 2075 行 clamp_change 清单）；归因由主线程亲自完成，产出 `devimpbin/0926-162821/0926-162821-verify.md`，并回填 `kernel-analysis/device-todo.md`（§0 新包行 + §4 回填表）。
- 核心结论：
  - P1-2 clamp_change 旁证机制成立（2075 行，变化才落行）。dcvsh_freq_limit 恒满频 → 本会话无硬件 DCVS/LMh 压（T4 部分定案）；msmp=- 恒定 → msm_performance 参数节点不可读（T5 部分定案）；horae_qmi 存在（Horae 实体在，推翻「零痕迹」）。
  - corectl 字段序 = min_cpus/max_cpus/enable；**仅 big 簇 enable=1**，little/prime 禁用——P1-4 压 little/prime 时 core_ctl 不可用，注意。
  - cap=85 空转嫌疑在 Alpha07-02 复现（37min 窗口决策仍满档 + dcvsh 满频）→ T8 优先级上调。
  - FAS 未激活，T10 仍无数据。
- 检索陷阱（已沉淀 devimp-log-analysis.md 坑 17/18/19）：
  1. daemon.log 是 fluent 中文文案不是 key（搜 key 恒假阴性）；
  2. Grep 工具对该 daemon.log 彻底读不动（positive control `chiri` 零命中），零命中一律按未检索处理；
  3. clamp_change 各字段值域与 `-` 语义。
- 子代理教训（继承）：rg 在本机挂起（Windows Store stub），数据整理走 PowerShell StreamReader 显式枚举；fluent key 检索陷阱整理代理也踩了（其 daemon.log 三项零命中不可靠，已在报告 §1.4 标注）。

## 收口：4 代理改动统一编译自证 + 文档回填（当日收口）

本轮 5 处改动（收口代理已逐项核验在源）：

1. `src/chiri/core_ctl.rs` — core_ctl enable 感知：`CoreCtlCluster.enable: Option<bool>`，`discover()` 快照读 `{dir}/enable`（读失败记 `None`）；`shrink_prime_via_max_cpus` 对 `enable == Some(false)` 复用 `max_cpus_warned` warn 一次后 `return false` 走逐核 offline 兜底；i18n `corectl-enable-off`（zh/en 各一）。
2. `src/chiri/cpu_load_governor.rs` — 热限幅恒钳重构：进程级 `static CLAMP_HEAVY`（默认 `true`）+ `set_clamp_heavy`；钳制点 `eff_perf = if clamp_heavy || current_perf < free_above { current_perf.min(cap) } else { current_perf }`；`current_perf` 平滑/状态链未改（不回写）；新增 `clamp_apply` 跃迁事件（diag 门控）+ `dev_clamp_binds` 字段。
3. `src/chiri/config.rs` — 开关透传：`ThermalGuardConfig.clamp_heavy: bool`（serde default true）+ Default 实现 + `Config::load` 同步 `set_clamp_heavy`（启动/热重载均生效）。
4. `src/chiri/mod.rs` — msmp 读取减法（【临时】）：`MSMP_FAIL_STREAK` / `MSMP_SKIP_TICKS`（AtomicU32），FAIL_LIMIT=8（≈16s）、RETRY_TICKS=64（≈128s）；退避期不读文件、记 `-`，恢复可读即清零。
5. `module/config/8550/feature.yaml` — `Thermal.clamp_heavy: true`；`touch_boost_tiers: 3→2`（【临时待 A/B，仅 8550】）；`free_above` 注释改为「仅 clamp_heavy=false 时生效」。

统一编译自证（终端 4 次，含报错定位自证；scratch `devimpbin/tmp_closeout.txt` 已删）：

- 4 个 rs 文件 + `feature.yaml` mtime 前移后 `cargo check -p chiri --target aarch64-linux-android` → `Compiling chiri v2.0.2`（build.rs 重编、config 重新嵌入）+ `Checking chiri v2.0.2` + `Finished` + **exit 0 + 0 rustc warning**（仅 build.rs eBPF 占位噪音）。
- 报错定位自证：临时在 `set_clamp_heavy` 插 `let _: u32 = "x";` → rustc `error[E0308]` 明确指向 `src\chiri\cpu_load_governor.rs:29`，还原后重查通过。

cap85 复核关键数字（详见 `kernel-analysis/device-todo.md` §4.1）：

- 窗口 15:49:13.703 ~ 16:26:11.726（n=2217s），转换点 100→85 @15:49:13.703、85→100 @16:26:15.962。
- 第 16 列 `max_freq_khz`（scaling_max 上限）窗口内恒满档、降档计数 0；第 15 列 `cur_freq_khz`（决策选频）有变化（1670400/1900800/1593600/1286400…）。
- 豁免带覆盖率 5.5% / 2.3% / 3.1%（little/big/prime）；`(85,95)` 开区间 tick = 0；功耗 cap85 2.7844 W vs cap100 3.1845 W（未场景归一化）。
- 数据文档：`devimpbin/0926-162821/power-profile.md`、`devimpbin/0926-162821/cap85-band.md`。

**教训（本轮最易读错）**：devimp tick 第 15 列 `cur_freq_khz`（决策选频，有变化）与第 16 列 `max_freq_khz`（scaling_max 上限，恒满档）语义不同，**必须分列陈述、不可混算**——「cap=85 空转」要看第 16 列降档计数，不是第 15 列是否变化。

遗留：`clamp_change` 内直接加 `smax <= cap 档位` 校验未做（当时 `mod.rs` 被另一代理占用），改用 governor 侧 `clamp_apply` 事件作行为级证据；如需直接校验需后续单独做。
