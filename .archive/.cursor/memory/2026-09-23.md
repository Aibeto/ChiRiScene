# 2026-09-23（Cursor 环境）

## daemon-perf-opt 计划 + 协作文件夹改按环境口径（0923 晚）

- 起因：语言替换评估（C/Go 无收益）→ 追问改进点 → 五阶段性能优化计划（tokio 瘦身 / FastReader /
  写频 dwell / 轮询改事件 / 杂项），见 `.cursor/docs/daemon-perf-opt-plan.md`。
- 用户指令两条：①任何 AI 产出文件不得离开项目文件夹（否则后期整理找不到）；②协作文件按环境写入
  对应文件夹（Cursor → `.cursor/`），AGENTS.md 已改口径。本次会话早前误写入 `.codebuddy/` 的计划与
  日志条目已迁至 `.cursor/`，`.codebuddy/memory/` 两处误写已还原。
- 计划要点与依赖：Phase 3 前置 `.trae/specs/harden-scenemode-and-screen-detect` 的 hold 三分支；
  明确不做换语言 / eBPF 探针 / 双进程拆分；不加 panic=abort（看门狗依赖 catch_unwind）。
