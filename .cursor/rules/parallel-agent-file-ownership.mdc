---
description: 任务需要多个子代理/AI 并行修改本仓库文件、或分批实施跨多文件的改动时（拆任务、分派、禁改清单、收口校验）
alwaysApply: false
---

# 并行子代理的文件所有权

并行落改动按文件所有权切分（2026-09-24 五子代理并行实施的固化口径）。
本规则约束 AI 协作行为，与 `git-commit-message.mdc` 同属流程类规则。

## 硬规则

1. 按**文件所有权**切分任务：同一文件同一时间只归一个子代理。分派时明确
   「可改清单 + 禁改清单」，禁改清单写进任务描述，不是口头约定。
2. 跨任务的契约数字、命令口径、架构约定变更由**主线统一**同步到
   `docs/agents/` 与本环境 `.cursor/memory/MEMORY.md`；子代理不各自改这两个地方，
   只在交付里说明「哪些口径需要同步」，避免并行写入口径分叉。
3. 具体改动经过只写本环境当日日志（`.cursor/memory/YYYY-MM-DD.md`），主线汇总。
4. 收口必做全局自证：所有批次完成后 mtime 前移 + `cargo check`
   （见 `cargo-check-self-verify.mdc`），确认每个子代理的改动都真的编译进来。
5. 中止/中断子代理时先复核它的改动是否已落地（可能写完才被中止），
   复核后再决定重派或跳过，不要按「已中止 = 未改动」假设。

## 正反例

```text
[BAD] 两个子代理同时改 logger.rs（一个改归档、一个改零写入），后写覆盖先写。
[GOOD] logger.rs 归属零写入任务独占；归档语义任务只出报告与文档同步项，冲突面归零。

[BAD] 每个子代理各自更新 docs/agents/02-convention.md 的契约数字。
[GOOD] 子代理在交付里列出「应同步的数字与出处」，主线核实后一次性落文档。
```
