# 2026-09-24（Cursor 环境）

## daemon-perf-opt Phase 1 + Phase 4 落地（0924 凌晨）

- **本轮禁终端，未编译验证**（用户硬性约束；验证全靠代码审读，下次会话先跑
  `cargo check -p chiri --target aarch64-linux-android` 补验）。
- Phase 1（sysfs 读侧减负）：
  - `src/utils.rs` 新增 `// [fast_reader]` 区块：FastReader（keep-open File +
    seek(0) 读入复用 Vec<u8> buf + 读错误丢 fd 重开重试一次；`read_raw`/`read_u32`
    返回 Option，`state()` 查 ReadState 三态 Missing/Failed/Ok；per-pid 节点禁用）。
  - `src/chiri/core_ctl.rs`：`reassert_offline`（原 :422）与 `restore_online`（原 :474）
    的 cpuN/online 读 → `online_reader()`（HashMap<u32, FastReader>，路径构造期缓存），
    消除每 2s 的 format! + open/close；三态口径逐字节保持（None 跳过 / 计 false）。
  - `src/chiri/affinity.rs`：`ensure_snapshot` 的 uclamp.min/max 读（原 :833/:840）→
    AffinityManager 持 2 个 FastReader；快照语义（读失败/空值→""）不变。
  - **未改**：`cpu_load_governor.rs`、`cpu_monitor.rs`——计划描述的「:759 防篡改重读」
    「/proc/stat 类逐采样读」实际不存在（CLG tick 只写不读、:759 是 spawn 初始化期
    read_affected_cpus；cpu_monitor 负载来自 eBPF map，仅 per-pid /proc/<pid>/* 读，
    按口径不动）。
- Phase 4（杂项）：
  - `Cargo.toml` codegen-units=1 **本已存在**，无改动（panic=abort 按约定不加）。
  - `src/common.rs` 特调白名单精确条目加 `// [exact_index]` HashMap 索引（or_insert
    保文件顺序第一条；两段式查找顺序、special_tuned_entries 导出顺序、regex 线性
    匹配均不变）。
- Phase 2（写频 dwell/死区）、Phase 3（轮询改事件驱动）**待真机验证后再启动**；
  Phase 2 前置（on_load_update hold 三分支）已落地，直接叠加即可。
- docs/agents 未同步（本轮按指令只动 .cursor/；FastReader 属内部优化，未动契约数字/
  命令口径，后续如升格为约定再补）。

## Phase 3 轮询改事件驱动（00:41 完成，未编译/未真机验证）

- 四文件审计仅改 `src/chiri/touch_detect.rs`：poll 超时 200ms→-1（消灭 5 次/秒空唤醒，
  约 43 万次/天）+ revents 补 POLLHUP/POLLERR/POLLNVAL 异常位走既有重枚举。
- 不动清单（附论证）：telemetry.rs（PSI/GPU 无事件源，1s 节拍必须保留；事件化会把
  PowerAVG 从等间隔加权变成事件加权，均值窗口不等价，契约数字不动）；app_detect.rs
  （前台切换无可靠事件源，1500ms/1s/100ms 绑定切换延迟与 verify 自愈节拍）；
  screen_detect.rs（sleep 均为事件后沉降/错误退避，非轮询）。
- 附带发现（未动手）：touch_detect 设备热插拔实际不感知（注释称重枚举但仅 read
  0/Err 触发），补齐需 inotify /dev/input，属行为增强。

## Phase 2 写频滞回 dwell+死区（01:17 完成，未编译/未真机验证）

- CLG `write_freq` 加滞回 gate：dwell 缺省 320ms（2×160ms tick）、死区
  `write_deadzone=0.03`（同 tuned hysteresis 口径）；tuned `gated_write` 80ms
  （2×40ms 特调 tick）。config.rs 新增 `write_dwell_ms`/`write_deadzone`
  （CpuLoadGovernorConfig）与 `write_dwell_ms`（SpecialTunedConfig），serde 缺省兜底、
  normalize 钳 0..5000ms / 0..0.2，0=关闭；feature.yaml 模板不写（同 devimp_top_n 做法）。
- 豁免：触摸提频（floor 升向）、极低负载立即降频（fast_down 降向，消费制清零）、
  模式接管/释放（spawn 初写/restore_policy/reload/fast.rs 锁频不经 gate）、写失败补写
  （last_failed 置位下次豁免）。热保护 clamp 零豁免通道。decision 标签（up/up_wait/
  down/down_wait/hold）赋值未动。
- 核正：「防篡改 force 重写」实无独立条目——flush 单一 write_freq，write_freq 顶部值
  去重使同值重写是 no-op（run()/hold 分支注释宣称的每 tick 重写 ceiling 实际不生效，
  维持现状未新增 force 写）；tuned.rs:85「120ms」为陈旧数字。

## 编译验证 + 代码审查（01:25，cargo check 通过）

- `cargo check -p chiri --target aarch64-linux-android` **exit 0**（eBPF 占位警告为本机预期）。
- 审查发现 **1 个待修逻辑缺口**：CLG 死区在 write_freq 写频层（<3% hw_max 直接吞写），
  但 on_load_update 的 hold 判定是 `target_freq == current_freq` 精确比较——小差距被
  死区永久吞掉后 target≠current 恒成立，稳态会 down_wait→down 循环、deb 计数增长，
  重演 harden spec 修过的「假 down」模式。修法：hold 判定加同款死区（或被吞写视为
  hold），与 tuned「死区进决策层」口径对齐。medium 负载小差距场景触发；低负载小差距
  会被 fast_down 豁免写关掉，不受影响。
- 次要：touch_detect POLLHUP/ERR 路径 break 后无退避，若设备持续 poll 报错会紧循环
  （建议复用既有 500ms 退避）；decision 标签与实际写频在 dwell 延迟/死区吞写时漂移
  （devimp 写次数口径注意）；hold 分支「flush 每 tick 重写 ceiling」注释失真（既有）。
- 确认等价：FastReader 三态映射、exact_index 保序、core_ctl/affinity 替换链、tuned
  hysteresis 比较式未动。

## 审查缺陷修复（02:42，cargo check exit 0）

- 缺陷 A（CLG 死区吞写 × hold 精确比较断链）**确认成立**并修复：hold 判定改死区比较
  （|target_freq−current_freq| ≤ 死区频差即 hold），死区频差两层共用新 `deadzone_band()`
  唯一换算点（决策层 + write_freq 吞写 gate 同源）；hold 三分支结构与四标签赋值点未动，
  豁免路径/热保护零豁免原样；`write_deadzone=0` 退化为旧行为。失真注释两处改准。
  确认时另发现对称形态「假 up/up_wait + 吞写」（有界，数 tick 后自然收敛进 hold），同修。
- 缺陷 B（touch_detect 异常 revents 紧循环）**确认成立**并修复：异常 revents 分支
  `break 'poll` 前补 500ms 退避。**残留边缘**：`Ok(0)|Err(_)` 读失败分支（:83 修复前）
  同一外层无退避路径未动（按范围约束），驱动持续读错的极端场景仍可能空转，待用户定夺。
- 修复仅动 cpu_load_governor.rs 与 touch_detect.rs；`cargo check -p chiri --target
  aarch64-linux-android` exit 0（eBPF 占位为本机预期）。

## 二审修复两处（02:58，cargo check 通过）

- touch_detect `Ok(0)|Err(_)` 读失败分支补 500ms 退避（与异常 revents 同款，堵同族紧循环残留）。
- CLG up 判定加 epsilon（`target_perf > old_perf + 1e-6`）：修浮点停滞致稳态永久
  "假 up + up_wait 无限累加"（α ≤ 0.5 慢升分支逼近 target 差 1 ulp 时增量舍入为 0），
  停滞态落入 else 由 deadzone hold 收尾。正常升降路径不受影响。
- 备忘未修：FastReader 依赖 seek(0)（不支持 llseek 的节点会读失败，新落点需注意）；
  异常 revents 退避顺延同批次其它设备事件 500ms（不丢失）；state() 首读前误报 Missing。

## 三审修复 last_failed 悬挂（03:33，cargo check 通过）

- 备注 1 修掉：`write_freq` 值去重早退分支补 `last_failed = false`（目标达成则补写
  诉求消失，防标记悬挂致未来某次真实写白豁免滞回）；tuned.rs hold 分支同款泄漏一并修
  （目标收敛进死区即清标记）。两侧同因同修，语义一致。

## CI 提交跳过闸（13:30）

- 需求「提交信息含 skip ci 时跳过本次提交的 CI」落地于 `.github/workflows/build.yml`：
  新增 `check-skip` 作业（push/tag 取 `github.event.head_commit.message`；pull_request
  载荷无提交信息，checkout 后 `git log -1 %B <head.sha>` 取 PR 头提交，取不到放行），
  `grep -qi 'skip ci'` 不分大小写命中即置 `should_skip=true`；`build` 作业加
  `needs: check-skip` + `if: should_skip != 'true'`（fail-open）。提交信息经 env 传入，
  不做 shell 插值（防注入）。
- 口径：裸 `skip ci` 即命中（含 `[skip ci]` 等方括号形态，方括号形态另有 GitHub 原生
  整 run 跳过）；`workflow_dispatch` 手动触发恒运行。注意副作用：提交信息里只是提及
  该字样（如描述本功能本身）也会跳过。
- 同步：`docs/agents/01-overview.md` 树注、`.cursor/memory/MEMORY.md`。
- 工作区已有、本轮未动：`.gitignore` 修改（用户 WIP），`.codebuddy/`、`.trae/`、
  `.workbuddy/`、`.cursor/rules/` 若干未跟踪文件。

## Dependabot PR #5（typescript 7）合并（16:20）

- 失败根因：PR 把根 `typescript` 升到 ^7.0.2，超出 `svelte-check` 对等依赖 `^5.0.0 || ^6.0.0`，
  npm 严格对等解析 ERESOLVE，CI 的 `npm install` 即挂（main 不受影响）。
- 兼容布局（svelte-check 自检报错明示）：根 typescript 保持 ^6.0.3，TypeScript 7 经 npm 别名
  `@typescript/native@npm:typescript@^7.0.2` 接入，`type-check` 追加 `--tsgo` 走 tsgo 语言服务；
  别名与 `--tsgo` 成对（ts-version-check.js 拒绝根 typescript 为 7）。
- 验证（`.ts7-wt` 工作树，PR 分支上）：免旗标 `npm install` 通过；`npm run type-check`（tsgo）
  0 错 0 警，与 TS 6 基线一致；vitest 85 例全过；`npm run build` 正常。
- 合并：追加提交 6cc2360 到 PR 分支并 push，本地 `--no-ff` 合并（62285fd）push main，PR #5
  自动置 Merged；工作树已删。
- 同步：`docs/agents/01-overview.md` 命令注释、`docs/agents/02-convention.md` 依赖现状、
  本文件与 `MEMORY.md`。

## devimp 日志分析 logd_0924-163354（16:5x，功耗归因）

- **版本核对**：单一版本 `ChiRi Canary Alpha06-09 (versionCode 10609)`，30 个 main_* 头三行 +
  daemon.log `[Main] 模块版本` 一致，**无混版本**；8550/kalama/2210132C、android=17 (sdk 37)
  kernel 5.15.194——与基线（Alpha06-04 / PHB110 / A16）机型与系统都不同，**功耗绝对值不可比**，
  本包只做包内归因。44 列拆分 schema（表头 + `# ts-column=local format_now`）✓；
  `thermal_free_pct=95` 全程 → free_above 0.95 修复已在包内；FAS 修复无法行为验证（全程
  mode=default 无 FAS 段、fps 列全空）；0924 凌晨 daemon-perf-opt 尚未出包，归因不计入。
- **场景（15min 亮屏浏览，凉机 batt 26.6-28.3℃ / cpu 38.6-65.6℃ / cap=100 无热事件，status.csv
  charge=discharging 全程）**：P_avg（status batt_power_w）抖音 2.14W（n=555, p50 1.61 / p95 5.39）、
  QQ 2.45W（245, 2.10/5.33）、桌面 1.71W（82, 0.83/7.21 尖峰=切换过渡）、负一屏 3.66W（17）、
  market 4.66W（4）、nopkg 0.41W。均在基线量级带内；迁移 2.4k-7k/s = 亮屏 UI 固有形态。
  归因：内容型负载 GPU+触摸地板主导，调度侧 cap（aweme little/big/prime 57.6/51.7/33.1%，贴 util）
  无显著冗余；无热/环境因素。
- **口径发现（供命令文档后续补充）**：44 列 devimp 的 `batt_i` 列疑似整数化（值域仅 0/1/2/3），
  「batt_i==0 严格排除」会误删 58% 样本（恰是 <0.5A 轻载秒）→ 脚本 P_avg 系统性高估
  （aweme 3.35 vs 真值 2.14）；`batt_p` 列正常。该机型功耗应以 status.csv 的 charge + batt_power_w 为准。
- **deb 计数核查**：deb_up/deb_down = up_wait/down_wait 连续方向 tick 计数。本包最长 up run 89 tick
  （14s 慢爬升，cur_perf 0.28→0.44 持续上行=真实爬坡）、down run 27 tick（QQ big 触摸地板钉住期），
  均有界、方向翻转即复位，**无「稳态无限增长」假 down 形态**（hardens 修复的同 OPP hold 正常）。
  QQ big 出现「decision=down 但 cur_freq 钉 1785600 不动」= touch floor 0.55（perf_init+4×0.05）
  钉住，非假 down，按此口径解读。
- **触摸地板量化**（big 簇 tick）：touch=1 占 22%（QQ 45%），其中 tgt<0.50 仍被钉 0.55 的占 9.4%
  （QQ 26%）；touch=1 秒的实际 policy3 均 1.41GHz vs 无触摸 0.94GHz。→ 已落改动：
  `module/config/8550/feature.yaml` default 段 `touch_boost_tiers: 4→3`（地板 0.55→0.50），
  依据/预期（QQ 类触摸重场景 big 上限 −1 OPP，场景级约 −2~4%）/回滚（改回 4）见汇报；
  touch_boost_ms 600、util_smoothing、up_threshold 等证据不足只建议 A/B 未动。
- 解压与中间产物保留 `devimpbin/tmp_0924/`（probe1-10.txt、probe2b/2c 聚合表、diff.txt）。

## devimp 日志分析 logd_0924-173105（17:4x，充电包+调度基线）

- **版本核对**：单一版本 `Canary Alpha06-10 (10610)`（4 个 main 头一致，无混版）；**PHB110 / A16 (sdk 36) / kernel 5.15.180**——与 16:33 包（Alpha06-09 / 2210132C / A17）机型、系统、版本全不同，**不可比**；回到 PHB110 设备族（同 0923 Alpha06-04 基线机）。44 列拆分 schema ✓。
- **包内只有 devimp 侧**（外层解出仅 `devimp_0924-173058.tar`，无 logd 侧 tar）：无 daemon.log/status.csv → 无 fps/FAS/charge/PowerAVG。
- **全程充电，无放电功耗样本**：batt_v 4.2→4.5V、batt_i 恒负、batt_p 23W 平台→平滑衰减至 1.7W + battT 44→35.8℃ = CC→CV 涓流曲线；**该机型负电流=充电方向**。场景 P_avg 不可测。
- **触摸地板仍 0.55（tiers=4）**：big 簇 touch=1 且 tgt<0.55 共 158 tick，其中 87 钉 0.55（Alipay 热段 touch=1 的 39% 钉 0.55）→ 仓库侧 4→3 改动未进本包固件，A/B 验证仍待出包。
- **Phase 2 前基线（轻载稳定窗 17:20-17:30 Alipay idle）**：little cur_perf 变化 1.7-1.9 次/s、decision 翻转 2.0-2.3 次/s，p50 0.28 但 p95 反复 1.00（UI 尖峰整簇拉满再落回）；prime 决策翻转 2.5 次/s 而实际写频仅 0.15 次/s（up_wait 43%+hold 36% 标签振荡）→ 支持「死区进决策层+dwell」方向，本包留作改动前对照。
- **aff 簿记 churn（新发现）**：@A 39687 帧/45min，`bind_release reason=stale` 35845（12.9 次/s）、distinct tid 8593、result=ok、pid=0——线程绑定短命 worker（DefaultDispatch/ART daemon 线程）退出即 stale 释放；建议核对是否伴随 sysfs 写、考虑批量/限频清理（需改 affinity.rs）。bg uclamp `bg_apply` 2602 次 ≈ 1 次/s 恒频 reassert。
- 热状态：cap=85 自 16:46 前持续到 17:16:29 battT<38 解除；期间 big 4068 tick 仅 87 个 tgt 落压制带 (0.85,0.95]、107 超 free，行为正常。17:16:29 同秒有 activate event + cap 归 100（daemon 重启或恰过解除线，无 daemon.log 无法区分）。
- deb 健康：up run 最长 73 tick（真实爬升）、down max 7，无稳态无限增长。
- 解压与 probe1-9 留 `devimpbin/tmp_0924b/`。

## 排查：173105 包缺 daemon.log/status.csv（18:1x，打包逻辑修复）

- **不是日志写入问题，是 logd/ 预算清理的保底盲区**（`logger.rs` `enforce_dir_limit`）：
  归档线程打包顺序 logs→devimp→清理，devimp tar mtime 最新；旧清理「保最新一个文件、其余删到 <96MB」只保住了 114MB 的
  `devimp_0924-173058.tar`，同批 ~1MB 的 `0924-173058.tar`（daemon.log/status.csv/service.log/
  watchdog.pid 唯一载体）被划进「其余」删掉；96MB 目标被单个 114MB 文件顶穿时旧逻辑会删到
  只剩最新一份——导出包（17:31:05，清理后 1~2s）只剩 devimp tar，与实况完全吻合。
  旧批次（16:33 批）被删属预算正常淘汰。
- **修复**（`src/logger.rs`）：logd/ 改走新增 `enforce_logd_limit`——**按归档批次原子删**：
  `<ts>.tar` 与 `devimp_<ts>.tar` 同进退（`logd_batch_key` 剥 `devimp_` 前缀与 `-N` 去重后缀）、
  最新批次永不删；最新批次自身 ≥96MB 时退化为收到 128MB 即停（不为凑 target 陪葬旧批次）。
  devimp/ 目录清理语义不变。`cargo check -p chiri --target aarch64-linux-android` 通过。
- 同步：`docs/agents/02-convention.md` 归档条 + 新增「被挤掉」历史坑；`.cursor/memory/MEMORY.md`
  契约要点；`.cursor/commands/devimp-log-analysis.md` 已知坑 12。
- 放大器（未修，backlog）：aff `bind_release stale` churn 使 devimp 归档达 114MB/45min——
  批量化后归档变小、预算更宽裕。

## 预算扩容 + 门限重启逻辑排查（19:0x）

- **改动 1（用户指定数值）**：`logger.rs` 目录预算 `LOGD_MAX_BYTES`/`DEVIMP_DIR_MAX_BYTES`
  128→**256MB**、`LOGD_TARGET_BYTES`/`DEVIMP_DIR_TARGET_BYTES` 96→**200MB**。
  注：`LOG_RESTART_THRESHOLD_BYTES`（打包门限 128MB）与 `DEVIMP_MAX_BYTES`（单文件软上限 128MB）
  **未动**——门限未在用户所给的 128/96 配对内，且保持 128MB 门限使单批归档 ≤128MiB、
  256MB 预算可容 2 批，留存更优；如需一并调整另行说明。
- **改动 2（排查结论）**：173105 会话实写 114,037,068B（108.75MiB）**< 门限 128MiB（134.2MB）**，
  该会话从未达门限，17:30:58 的重启是外部重启（手动/异常），门限重启未触发属「条件未达」。
  但代码里确有两处会让门限逻辑「达到也不生效」且零痕迹：watchdog_pid() 只查
  comm∈{sh,mksh}——①调试直跑（交互 shell comm 也是 sh）误判有监督 → 达门限 exit(0) 后
  无人拉起、调度停了；②busybox/ash 父进程或 /proc 读失败（SELinux）误判无监督 → 清零计数、
  门限永久失效。**修复**：watchdog_pid 改「pidfile 铁证（logs/watchdog.pid==getppid()）∪
  脱管 shell（detached_shell_parent：comm shell 家族 + /proc/<ppid>/stat 祖字段==1）」；
  `ensure_watchdog_pid_file` 重建判定同用脱管 shell 判据；被抑制时打点新 i18n key
  `logger-log-restart-suppressed`（zh/en）。`cargo check -p chiri --target aarch64-linux-android` 通过。
- 同步文档：02-convention（预算数字、门限判定、watchdog.pid 自愈判据）、MEMORY.md 契约要点。

## devimp 日志分析 logd_0924-190130（19:0x，浮窗掉帧调查——包内无王者段，降级结论）

- **版本核对**：单一版本 `Canary Alpha06-10 (10610)`，17 个 main 文件头三行一致，无混版；
  **PHB110 / A16 (sdk 36) / kernel 5.15.180**——同 173105 包（回到 PHB110），与 163354 包
  （2210132C/A17）机型不可比。44 列拆分 schema ✓。
- **包完整性：logd 侧再次缺失**——外层解出仅 `devimp_0924-190127.tar`（126.8MB，逼近 128MB
  门限），同批 `<ts>.tar`（daemon.log/status.csv/service.log/watchdog.pid）又被挤掉，与 173105
  形态一致。属预期：`enforce_logd_limit` 批次原子清理是今天 18:1x 才落仓库，设备固件仍是
  Alpha06-10，**修复版未出包**。无 fps/charge/PowerAVG/daemon.log。
- **王者段不存在**：aff 全程 18:19:39–19:01:25（2501 个 @S 帧、203 个进程）**零 sgame 痕迹**
  （用户后称设备玩的是国际服；findstr 阳性对照验证过——同目录 AlipayGphone/bili 均能命中，
  `sgame|levelinfinite|garena` 在 main_*/aff_* 全部 0 命中，**该包确实不含游戏段**，非检索遗漏）。
  国际服 `com.levelinfinite.sgameGlobal` 本就在 FAS 白名单（normal/fas.yaml:11 → sgame.yaml
  per_app_profiles），若真机对局仍见 default 裸跑即 app_detect/FAS 接管问题，待带游戏段的包归因。
  main 文件仅 default/playback 两模式、无任何 `mode=fas` tick。场景构成：bili playback 三段
  ≈33min、Alipay ≈8min、idlefish ≈7min、QQ/微信短段、kernelsu 收尾。也无游戏助手/浮窗类
  进程。→ **浮窗掉帧归因（任务核心）无法从本包得出**，FAS 决策/aff 挪核/CLG 干预三条路径
  均无从验证。
- **touch_boost_tiers=3 未进包**：default big `touch=1` 且 tgt 钉 0.55 的 tick 有 41 个
  （4→3 改动后地板应为 0.50）→ 设备固件仍是 tiers=4；仓库侧 8550 feature.yaml 已改 3，
  A/B 验证仍待新固件。注意本包 default big touch=1 tick 中 32.4% tgt<0.55（触摸地板只
  在 Worker flush 时 clamp、不反映在 tgt 列，勿按 tgt 列全量推断地板命中）。
- **aff churn 持续**：@A 50108 帧/42min，`bind_release reason=stale` 33798 = **13.5 次/s**
  （上包 12.9）；`bg_apply` 2450 ≈ 1 次/s；move_group 仅 33、cpuset_cpus 1。stale 批量化/
  限频清理（affinity.rs）仍是 backlog——它是「浮窗出现→短命线程涌入→绑定/释放写放大」
  假设里唯一在本包可见的相关信号，但不能替代王者段证据。
- **热态全程**：cap=85 自 18:19:40（batt 41.9/cpu 58.0）到包尾未解除（battT 39.5–42.1），
  仅 1 条 thermal_change，无 daemon 重启迹象；功耗数字全部属热态且 devimp batt_i 整数化
  （P_avg 系统性高估），仅作包内相对参考。
- **决策翻转率（Phase 2 改动前基线，续 173105）**：全包 big 2.76 次/s、prime 1.97 次/s、
  little 0.66/s（little 低是本包 playback 段占比高所致，与 173105 的 2.0–2.3 不直接可比）。
- **下次采包要求（复现浮窗掉帧）**：① 用含 18:1x 两修复的新固件；② meta.yaml
  `fas_enabled=true` 且 fas_whitelist 命中 sgame，确认进过 `mode=fas`；③ 王者前台开启
  弹幕通知/游戏助手浮窗复现掉帧并记录大致时刻；④ 尽量凉机（避开 cap=85 热段混淆）；
  ⑤ 导出后先 `tar -tzvf` 确认外层两个 tar 都在。
- 解压与 probe1-6 留 `devimpbin/tmp_0924c/`。

## 190130 包延伸：其他可改进点（19:4x，probe_a–f）

- **口径纠正（推翻旧假设）**：`bind_release reason=stale` 33798 事件/42min 中，distinct tid 7233
  但**仅 291 个（4.0%）被 pin/move_group/restore 实际动过** → 其余走 `restore_group_mask`
  的 `group_bind==None` 早退（`Ok(())`，零 sysfs）、`thread_comm` 读内存 → **不是写放大**，
  纯日志噪声：清理类帧（stale/departed/gone）43.5k 行/42min ≈ 17/s，占 @A 5.59MB 的 86%。
  backlog 里「stale 批量化」的动机改写为**日志减量**，优先级下调。
- **aff 体积构成（probe_d）**：t 线程行 1,934,080 行 = **114.14MB（94%）**；p 进程行 1.16MB
  （25.6k 行，top12 多为 surfaceflinger/system_server/logd/rcu_preempt 等系统进程）；
  @A 5.59MB；@S 帧头 0.08MB。合计 121MB，**每帧 47KB / 773 行**。
  t 行 49.4% `u=0`；distinct comm 4590；**54% 行 `pid=0`（归属未知，1.04M 行）**；
  comm top = 每进程一套 ART 常驻线程（DefaultDispatch 90k、ThreadPoolForeg 33k、
  Signal_Catcher/FinalizerDaemon/HeapTaskDaemon/ReferenceQueueD… 各 ~17k）。
- **根因（`affinity.rs`）**：`THREAD_STALE=30s` + fg 扫描「见即建档」（:1308 `threads.entry`）
  → 线程表稳态滞留 ~700 条（与每帧 773 行吻合），其中多为短命 app worker 与 ART 常驻线程；
  @S 每帧把整表落盘，30s 后逐条清理并各打一行日志。
- **可改进候选**：① @S 线程下钻集合收窄为「前台进程线程 ∪ 真正动过的条目（home≥0/moved/
  group_bind≠None）」——t 行体积可降约一个数量级；② 清理类事件改汇总计数打点（@A 噪声 −86%）；
  ③ 建档时补全 pid（54% 行 pid=0 是分析可用性问题，非体积）；④ `write_bg_uclamp_max`
  （:79 无变化判断、`apply` 内每秒调用 ≈1 次/s）加「值未变跳过」守卫。
- **门限关系（新证据）**：本包会话 18:19:39–19:01:25 = 41.8min、tar 126.8MB ≈ 128MB 门限 →
  **dev_record 开启时每约 42min 必然撞门限强制重启一次**（即打断使用）。预算已 256/200MB，
  门限仍 128MB；建议先做 ①（减量，重启频率大降且归档变小），再单独评估门限是否上 256MB。

## 开销/功耗优化批量实施（19:5x–20:1x，5 个子代理并行）

用户口径：能改善开销与功耗的都要；devimp 全目录受 `dev_record` 约束（关闭时零写入、不产生任何数据）；
优先事件驱动。先跑只读体检（10 条候选），再分批实施（按文件所有权切分避免冲突）：

- **FAS 防篡改重写**（`fas/{policy_mgmt,policy_controller,controller}.rs`、`fas_types.rs`）：节拍由
  **帧计数改 `Instant` 时间基准**（`freq_force_reapply_interval` 单位帧→秒，默认 30、`normalize()` 钳 ≥1，
  旧实现 0 会 `% 0` panic）；删 `force_reapply()` 两次无用的 `re_unmount()`（只 umount2 不重开 fd，
  对写入零贡献；`verify_prev_freq` 那次保留）。120fps 稳态：24 写/s + 24 umount2/s → 0.2 写/s、0 umount2；
  副作用：`freq_hold_frames` 的 hold 不再被高频 force 击穿。文档已同步 `docs/agents/03-chiri.md`、
  `mdocs/TechAnalyze.md`、`README.en.md`（原写「帧」与代码不符，一并修正）。
- **「先读回 scaling_max_freq 再决定写」未实施**：内核同值写是否真触发重新锁频需真机实测，收益也被节流摊薄。
- **affinity 减量**（`src/chiri/affinity.rs`）：见 MEMORY.md 契约要点 aff 减量条（差分/汇总/pid/is_fg/uclamp 守卫）。
  复核了 `is_fg` 等价重构（departed/gone/后台跳过/`managed_pids` 四处判据 + 收养分支置位），语义等价。
- **@S 差分帧**（`src/chiri/mod.rs` + `logger.rs` 注释）：见上条；`AFF_HEADER` 增 3 行注释说明差分语义。
- **监控侧**（`src/monitor/*`、`src/chiri/tuned.rs`，**已完成**）：屏幕节点清单缓存（稳态 0 次枚举，
  省约 27–50 syscall/s；节点本身仍每秒新鲜读，投票/自愈口径不变）、`app_detect` 去掉每轮整份克隆
  （核实嵌入 rules.yaml 仅 14 行、app_modes 空表 → 实测 5→2 次小分配/轮，表变大时才明显）、
  **`pid_watcher` 线程删除**（500ms 空转 → 改在 `set_current_package` 生效点就地推送；已复核
  `CURRENT_PID` 唯一写入点使发送条件与原线程逐位一致；稳态唤醒 2 次/s→0、广播延迟 0~500ms→即时）、
  特调 tick 的克隆与格式化移入 `diag_active`（关闭时 25 tick/s 的分配归零）；顺带清了
  `cpu_monitor.rs` 两处「由 pid_watcher 广播」的陈旧注释。未做：`fps_probe` 待机 500ms（`fas_active`
  写入点在允许范围外，需换 Condvar 载体，另派人做）。
- **devimp 零写入 + 模式文件**（`logger.rs`/`main.rs`/`chiri/mod.rs`，**已完成**——该任务被中止时其实
  已落地，中止前我逐条复核过）：`devimp/` 目录**唯一创建者 = `main_open`/`aff_open`**（都在
  `diag_active()` 内），`diag_prepare` 目录不存在即早退、启动归档不预建、短会话清空后连空目录
  `remove_dir`（`:2056-2059`）；`set_diag_package` 加 `diag_active()` 门控（关时零锁零分配，重开后
  下一秒照常按包名切 main_ 文件）；`current_mode.chr` 5s 自愈改 `ModeFile`（`heal` 只在记账**且**
  磁盘内容都一致时跳过：稳态 5 syscall → 1 read；`remove()` 清记账防 5s 文件空窗）。
- 已知遗留/暂缓：FastLock/PowerBase 的 5s 盲写与 FAS 频率读回都需先真机确认 cpufreq 节点支持重复读
  （FastReader 依赖 `seek(0)`）；`cpu_freq_snapshot` 复用句柄同因暂缓；`docs/agents/02-convention.md`
  与 `.cursor/commands/devimp-log-analysis.md` 的 aff 判读口径已同步（已知坑 13/14）。

### 收口（20:5x）

- **fps 探针待机事件化**（`monitor/mod.rs`、`fps_monitor.rs`、`fas_manager.rs`、`chiri/mod.rs`、`main.rs`）：
  新增 `monitor::FasSignal`（`Mutex<()>` 谓词 + `Condvar`；`set` 的 store 与 `notify_all` **同锁**，
  并存唤醒方先拿锁/先入睡两种交错的推理注释），取代裸 `Arc<AtomicBool>`；`fps_probe` 待机与
  `mod.rs` 首激活门控改无超时 `wait_until_active`。唤醒 2 次/s → **0**（只在 FAS 状态跃迁时通知）。
  **核实纠正**：`cpu_monitor`（读的是启动期一次性的 `FAS_FG_UTIL_ENABLED`）与调度线程（走
  `FasManager::is_active()`）**都不读共享标志**，热路径读只有那两处门控 → `is_active()` 保持裸原子读完全等价。
  启动前 `sleep(500ms)`/`sleep(1s)` 待机在 `fps_monitor.rs` 已归零；活跃期 100ms poll（承担「发现 FAS
  去激活→detach」）按指令未动。
- **全局收口校验**：把 `src/monitor/mod.rs`/`src/main.rs` mtime 前移强制重编译后
  `cargo check -p chiri --target aarch64-linux-android` 输出 `Checking chiri v2.0.2` + `Finished`，
  **exit 0、0 条 rustc warning**（仅 build.rs 的 eBPF 占位提示）。本轮 5 个子代理的改动全部就位。
- **新增 backlog（未做）**：① `fps_monitor.rs:388-391` 挂载失败时活跃期每 500ms 重试并 `warn!`
  一行——`libgui` 符号长期缺失时 2 条/s（约 7200 条/h），会加速 daemon.log 50MB 轮转；建议「同 PID
  只告警一次，仅在 PID 变化或 FAS 重新激活时重试」。② 活跃期 100ms poll 可再事件化（`set(false)`
  已 notify，可用 eventfd/自管道唤醒 mio `Poll`）。③ FastLock/PowerBase 5s 盲写读校验、
  `cpu_freq_snapshot` 复用句柄——都需真机确认节点支持重复读。④ 真机验证清单：FAS 段
  `fas-freq-mismatch` 是否增多（强制重写节拍由帧改秒）、`@S` 帧行数与 `*_bulk` 帧、
  dev_record 关闭时 `devimp/` 目录不出现。

## 口径修正 + 注释漂移 + 节点缓存 TTL（21:1x）
- 口径统一「ChiRi 默认全局唯一调度程序」：TechAnalyze 7.2/fast/FAS、03-chiri FAS/fast/core_ctl、02-convention
  事件循环条、README.en、devimp-log-analysis 的「厂商对抗/厂商篡改」前提措辞改为「默认无竞争者；防篡改=异常
  兜底（残留旧模块、手动调试、内核异常态）」，机制与数字未动；webui sources.ts:70 devimp 注释漂移修正（启动
  即建目录→惰性创建、dev_record 关闭零数据含空目录）。
- `screen_detect.rs` 节点清单缓存 10 分钟 TTL 已确认落地（`SCREEN_NODES_TTL=600s`），MEMORY.md 补口径条 +
  TTL 入「前台 PID 广播改推送」条目。
