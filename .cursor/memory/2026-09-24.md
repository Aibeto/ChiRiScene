# 2026-09-24（Cursor 环境）

## daemon-perf-opt Phase 1 + Phase 4 落地（0924 凌晨）

- **本轮禁终端，未编译验证**（用户硬性约束；验证全靠代码审读，下次会话先跑
  `cargo check -p chiri --target aarch64-linux-android` 补验）。
- Phase 1（sysfs 读侧减负）：
  - `src/utils.rs` 新增 `// [fast_reader]` 区块：FastReader（keep-open File +
    seek(0) 读入复用 Vec<u8> buf + 读错误丢 fd 重开重试一次；`read_raw`/`read_u32`
    返回 Option，`state()` 查 ReadState 三态 Missing/Failed/Ok；per-pid 节点禁用）。
  - `src/chiri/core_ctl.rs`：`reassert_offline`（原 :422）与 `restore_online`（原 :474）
    的 cpuN/online 读 → `online_reader()`（HashMap<u32, FastReader>，路径构造期缓存），
    消除每 2s 的 format! + open/close；三态口径逐字节保持（None 跳过 / 计 false）。
  - `src/chiri/affinity.rs`：`ensure_snapshot` 的 uclamp.min/max 读（原 :833/:840）→
    AffinityManager 持 2 个 FastReader；快照语义（读失败/空值→""）不变。
  - **未改**：`cpu_load_governor.rs`、`cpu_monitor.rs`——计划描述的「:759 防篡改重读」
    「/proc/stat 类逐采样读」实际不存在（CLG tick 只写不读、:759 是 spawn 初始化期
    read_affected_cpus；cpu_monitor 负载来自 eBPF map，仅 per-pid /proc/<pid>/* 读，
    按口径不动）。
- Phase 4（杂项）：
  - `Cargo.toml` codegen-units=1 **本已存在**，无改动（panic=abort 按约定不加）。
  - `src/common.rs` 特调白名单精确条目加 `// [exact_index]` HashMap 索引（or_insert
    保文件顺序第一条；两段式查找顺序、special_tuned_entries 导出顺序、regex 线性
    匹配均不变）。
- Phase 2（写频 dwell/死区）、Phase 3（轮询改事件驱动）**待真机验证后再启动**；
  Phase 2 前置（on_load_update hold 三分支）已落地，直接叠加即可。
- docs/agents 未同步（本轮按指令只动 .cursor/；FastReader 属内部优化，未动契约数字/
  命令口径，后续如升格为约定再补）。

## Phase 3 轮询改事件驱动（00:41 完成，未编译/未真机验证）

- 四文件审计仅改 `src/chiri/touch_detect.rs`：poll 超时 200ms→-1（消灭 5 次/秒空唤醒，
  约 43 万次/天）+ revents 补 POLLHUP/POLLERR/POLLNVAL 异常位走既有重枚举。
- 不动清单（附论证）：telemetry.rs（PSI/GPU 无事件源，1s 节拍必须保留；事件化会把
  PowerAVG 从等间隔加权变成事件加权，均值窗口不等价，契约数字不动）；app_detect.rs
  （前台切换无可靠事件源，1500ms/1s/100ms 绑定切换延迟与 verify 自愈节拍）；
  screen_detect.rs（sleep 均为事件后沉降/错误退避，非轮询）。
- 附带发现（未动手）：touch_detect 设备热插拔实际不感知（注释称重枚举但仅 read
  0/Err 触发），补齐需 inotify /dev/input，属行为增强。

## Phase 2 写频滞回 dwell+死区（01:17 完成，未编译/未真机验证）

- CLG `write_freq` 加滞回 gate：dwell 缺省 320ms（2×160ms tick）、死区
  `write_deadzone=0.03`（同 tuned hysteresis 口径）；tuned `gated_write` 80ms
  （2×40ms 特调 tick）。config.rs 新增 `write_dwell_ms`/`write_deadzone`
  （CpuLoadGovernorConfig）与 `write_dwell_ms`（SpecialTunedConfig），serde 缺省兜底、
  normalize 钳 0..5000ms / 0..0.2，0=关闭；feature.yaml 模板不写（同 devimp_top_n 做法）。
- 豁免：触摸提频（floor 升向）、极低负载立即降频（fast_down 降向，消费制清零）、
  模式接管/释放（spawn 初写/restore_policy/reload/fast.rs 锁频不经 gate）、写失败补写
  （last_failed 置位下次豁免）。热保护 clamp 零豁免通道。decision 标签（up/up_wait/
  down/down_wait/hold）赋值未动。
- 核正：「防篡改 force 重写」实无独立条目——flush 单一 write_freq，write_freq 顶部值
  去重使同值重写是 no-op（run()/hold 分支注释宣称的每 tick 重写 ceiling 实际不生效，
  维持现状未新增 force 写）；tuned.rs:85「120ms」为陈旧数字。

## 编译验证 + 代码审查（01:25，cargo check 通过）

- `cargo check -p chiri --target aarch64-linux-android` **exit 0**（eBPF 占位警告为本机预期）。
- 审查发现 **1 个待修逻辑缺口**：CLG 死区在 write_freq 写频层（<3% hw_max 直接吞写），
  但 on_load_update 的 hold 判定是 `target_freq == current_freq` 精确比较——小差距被
  死区永久吞掉后 target≠current 恒成立，稳态会 down_wait→down 循环、deb 计数增长，
  重演 harden spec 修过的「假 down」模式。修法：hold 判定加同款死区（或被吞写视为
  hold），与 tuned「死区进决策层」口径对齐。medium 负载小差距场景触发；低负载小差距
  会被 fast_down 豁免写关掉，不受影响。
- 次要：touch_detect POLLHUP/ERR 路径 break 后无退避，若设备持续 poll 报错会紧循环
  （建议复用既有 500ms 退避）；decision 标签与实际写频在 dwell 延迟/死区吞写时漂移
  （devimp 写次数口径注意）；hold 分支「flush 每 tick 重写 ceiling」注释失真（既有）。
- 确认等价：FastReader 三态映射、exact_index 保序、core_ctl/affinity 替换链、tuned
  hysteresis 比较式未动。

## 审查缺陷修复（02:42，cargo check exit 0）

- 缺陷 A（CLG 死区吞写 × hold 精确比较断链）**确认成立**并修复：hold 判定改死区比较
  （|target_freq−current_freq| ≤ 死区频差即 hold），死区频差两层共用新 `deadzone_band()`
  唯一换算点（决策层 + write_freq 吞写 gate 同源）；hold 三分支结构与四标签赋值点未动，
  豁免路径/热保护零豁免原样；`write_deadzone=0` 退化为旧行为。失真注释两处改准。
  确认时另发现对称形态「假 up/up_wait + 吞写」（有界，数 tick 后自然收敛进 hold），同修。
- 缺陷 B（touch_detect 异常 revents 紧循环）**确认成立**并修复：异常 revents 分支
  `break 'poll` 前补 500ms 退避。**残留边缘**：`Ok(0)|Err(_)` 读失败分支（:83 修复前）
  同一外层无退避路径未动（按范围约束），驱动持续读错的极端场景仍可能空转，待用户定夺。
- 修复仅动 cpu_load_governor.rs 与 touch_detect.rs；`cargo check -p chiri --target
  aarch64-linux-android` exit 0（eBPF 占位为本机预期）。

## 二审修复两处（02:58，cargo check 通过）

- touch_detect `Ok(0)|Err(_)` 读失败分支补 500ms 退避（与异常 revents 同款，堵同族紧循环残留）。
- CLG up 判定加 epsilon（`target_perf > old_perf + 1e-6`）：修浮点停滞致稳态永久
  "假 up + up_wait 无限累加"（α ≤ 0.5 慢升分支逼近 target 差 1 ulp 时增量舍入为 0），
  停滞态落入 else 由 deadzone hold 收尾。正常升降路径不受影响。
- 备忘未修：FastReader 依赖 seek(0)（不支持 llseek 的节点会读失败，新落点需注意）；
  异常 revents 退避顺延同批次其它设备事件 500ms（不丢失）；state() 首读前误报 Missing。

## 三审修复 last_failed 悬挂（03:33，cargo check 通过）

- 备注 1 修掉：`write_freq` 值去重早退分支补 `last_failed = false`（目标达成则补写
  诉求消失，防标记悬挂致未来某次真实写白豁免滞回）；tuned.rs hold 分支同款泄漏一并修
  （目标收敛进死区即清标记）。两侧同因同修，语义一致。

## CI 提交跳过闸（13:30）

- 需求「提交信息含 skip ci 时跳过本次提交的 CI」落地于 `.github/workflows/build.yml`：
  新增 `check-skip` 作业（push/tag 取 `github.event.head_commit.message`；pull_request
  载荷无提交信息，checkout 后 `git log -1 %B <head.sha>` 取 PR 头提交，取不到放行），
  `grep -qi 'skip ci'` 不分大小写命中即置 `should_skip=true`；`build` 作业加
  `needs: check-skip` + `if: should_skip != 'true'`（fail-open）。提交信息经 env 传入，
  不做 shell 插值（防注入）。
- 口径：裸 `skip ci` 即命中（含 `[skip ci]` 等方括号形态，方括号形态另有 GitHub 原生
  整 run 跳过）；`workflow_dispatch` 手动触发恒运行。注意副作用：提交信息里只是提及
  该字样（如描述本功能本身）也会跳过。
- 同步：`docs/agents/01-overview.md` 树注、`.cursor/memory/MEMORY.md`。
- 工作区已有、本轮未动：`.gitignore` 修改（用户 WIP），`.codebuddy/`、`.trae/`、
  `.workbuddy/`、`.cursor/rules/` 若干未跟踪文件。

## Dependabot PR #5（typescript 7）合并（16:20）

- 失败根因：PR 把根 `typescript` 升到 ^7.0.2，超出 `svelte-check` 对等依赖 `^5.0.0 || ^6.0.0`，
  npm 严格对等解析 ERESOLVE，CI 的 `npm install` 即挂（main 不受影响）。
- 兼容布局（svelte-check 自检报错明示）：根 typescript 保持 ^6.0.3，TypeScript 7 经 npm 别名
  `@typescript/native@npm:typescript@^7.0.2` 接入，`type-check` 追加 `--tsgo` 走 tsgo 语言服务；
  别名与 `--tsgo` 成对（ts-version-check.js 拒绝根 typescript 为 7）。
- 验证（`.ts7-wt` 工作树，PR 分支上）：免旗标 `npm install` 通过；`npm run type-check`（tsgo）
  0 错 0 警，与 TS 6 基线一致；vitest 85 例全过；`npm run build` 正常。
- 合并：追加提交 6cc2360 到 PR 分支并 push，本地 `--no-ff` 合并（62285fd）push main，PR #5
  自动置 Merged；工作树已删。
- 同步：`docs/agents/01-overview.md` 命令注释、`docs/agents/02-convention.md` 依赖现状、
  本文件与 `MEMORY.md`。
