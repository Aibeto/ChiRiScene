# down.chr: DOWN 模式（调度停摆）。没启用时这个文件只有注释，视为关闭。
#
# 启用：在下面写一行 down（去掉注释符号即可，大小写都认）
# down
#
# 启用后调度会：
#   - 释放全部接管：CLG 调频、akmode 特调、FAS、fast_lock 锁频、线程摆放（affinity）、
#     core_ctl 核心在线接管、息屏场景模式，全部交回系统；
#   - 只往 current_mode.chr 写 down，并且不再覆盖它（覆盖就等于退出停摆）；
#   - 采集与日志照常：eBPF、status.csv、daemon.log、devimp 都继续。
#     记下来的就是「ChiRi 不工作时」设备自己的调度情况，方便和接管时对比。
#
# 手改这个文件同样生效，改完不用重启调度（守护进程监听着它）。
# 文件在磁盘上，所以重启设备后仍然停摆，只能改回本文件解除。
