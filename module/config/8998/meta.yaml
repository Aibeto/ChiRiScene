# meta.yaml: [meta] 用户可修改的抬头字段（守护进程逐字段严格校验）
name: "config_8998"
author: "ChiRi"
language: "zh"
loglevel: "INFO"
dev_record: true
fas_enabled: true
scenemode_enabled: true
thread_bind: true
# 功耗口径（PowerAVG.chr，仅 ChiRi）：false = 参考值（相邻两值取半递推，偏近期）；
#                                   true  = 累计平均值（等权全史）。热重载即时生效。
power_avg: false
# 耗电读数满量程（W，WebUI 状态页仪表盘用；可选，默认 12）：只影响显示，不参与调度。
power_max_w: 12
