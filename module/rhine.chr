# rhine.chr: 实验室状态。实验室没启用时这个文件只有注释（视为空），启用了就写入模式名。
#
# 可写的模式名（key 直接点明它做了什么，界面上显示的是另一个名字）：
#   vector        全局模式切到 vector + 关掉 FAS / 息屏场景模式 / 场景特调（界面：Vector Breakthrough）
#   contingency   预留，当前无任何效果（界面：Contingency Contract）
#   babel         预留，当前无任何效果（界面：Babel）
#   frozen        预留，当前无任何效果（界面：Frozen）
#
# 手改这个文件同样生效，改完不用重启调度。格式不对或写了未知名字，守护进程会用
# 内置默认内容覆盖本文件，并在日志里告警。
#
# 注意：实验室一旦启用，本次开机内**只能切换模式、不能关闭**——清空本文件会被守护
# 进程按原样写回（关闭必须重启设备）。
#
#   off         强制关闭（无视风险）：清掉锁定标记、按快照还原原值、回到启用前的调度。
#               本文件随后被写回「未启用」。不重启就关闭可能留下运行时残留，
#               建议尽快重启设备复核。
#
# 实验室会动的东西：全局模式、FAS 总闸、息屏场景模式总闸、场景特调、线程绑定总闸。
# 改动前的原值存在 rhine-back.chr，关闭实验室或重启设备时按它还原。
