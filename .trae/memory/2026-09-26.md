# 2026-09-26

## Git 历史改写：清除 traeagent 署名

- 起因：GitHub 贡献者列表出现 `traeagent <traeagent@users.noreply.github.com>` 且 0 提交。实为 `96690a1`（2026-08-28 "chore: pre-termination backup"）commit message 里的 `Co-authored-by:` 尾注——GitHub 会解析尾注并计入 Contributors，git 本身（shortlog / log --author）不统计，故本地查不到它有任何提交。
- 处理：`git filter-branch --msg-filter "sed '/^Co-authored-by: traeagent/d'" -- 96690a1^..main` 只删该行，工作区先 stash（未提交的 `.github/workflows/build.yml`）后恢复；随后 `git push --force-with-lease ChiRiScene main`（filter-branch 自动留的 `refs/original/refs/heads/main` 备份 ref 已删，reflog 仍可回溯）。
- 结果：**2026-08-28 之后含该条共 160 个 commit 的 hash 全部变更**；HEAD `275642d` → `ff27c01`，`96690a1` → `637170b`（tree / parent / 作者 / 日期四项不变）。校验：新旧 HEAD `git diff` 为空，commit 数 389、merge 数 50 均未变。
- 影响：**此后一切旧 hash 引用作废**，其他克隆需重新 clone 或 `reset --hard`；远端 dependabot 分支仍挂旧链，其 remote-tracking ref 本地仍可见旧提交（属正常，dependabot 自行 rebase 后更新）；贡献者列表为 GitHub 侧缓存，重算后 traeagent 条目消失。